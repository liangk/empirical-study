node_modules/
dist/
.env
.repos/
.fixtures/

# Per-study outputs (gitignored by default; commit selectively)
**/results/
**/content/

# stories/ is the public half of this repo: the evidence has to ship with it,
# otherwise the write-ups reference files a reader cannot see. Scan output there
# is trimmed to the findings themselves rather than full CLI output.
!stories/**/results/
!stories/**/content/

# Planning docs (keep temp/ for backward compat)
temp/