{
 "repository": "baptisteArno/typebot.io",
 "filesScanned": 227,
 "findings": [
  {
   "file": "packages/scripts/src/readCsvAndDoSomething.ts",
   "line": 44,
   "severity": "high",
   "description": "Found 2 database queries (Prisma.findUnique(), Prisma.update()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const { id } of topTypebots.data) {\n    bar.update(i++);\n\n    const publicTypebot = await prisma.publicTypebot.findUnique({\n      where: {\n        typebotId: id,\n      },\n      select: {\n        version: true,\n        groups: true,\n        typebot: {\n          select: {\n            workspace: {\n              select: {\n                plan: true,\n              },\n            },\n          },\n        },\n      },\n    });\n\n    if (!publicTypebot) continue;\n    if (publicTypebot.typebot.workspace.plan !== Plan.FREE) continue;\n\n    const groups = parseGroups(publicTypebot.groups, {\n      typebotVersion: publicTypebot.version,\n    });\n\n    let wasUpdated = false;\n    const updatedGroups = groups.map((group) => {\n      return {\n        ...group,\n        blocks: group.blocks.map((block) => {\n          if (\n            block.type === IntegrationBlockType.EMAIL &&\n            block.options?.credentialsId === \"default\"\n          ) {\n            wasUpdated = true;\n            return {\n              ...block,\n              options: {\n                ...block.options,\n                credentialsId: undefined,\n              },\n            };\n          }\n          return block;\n        }),\n      };\n    });\n\n    updatedGroupsList.push(updatedGroups);\n\n    if (wasUpdated) {\n      console.log(\"Updating...\", id);\n      await prisma.publicTypebot.update({\n        where: {\n          typebotId: id,\n        },\n        data: {\n          groups: updatedGroups,\n          updatedAt: new Date(),\n        },\n      });\n    }\n  }",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 68.5,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: , topTypebots.data",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 2 async call(s) inside loop over topTypebots.data\n// Problem: N+1 queries where N = topTypebots.data.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = topTypebots.data.map( => .id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const { id } of topTypebots.data) {\n    bar.update(i++);\n\n    const publicTypebot = await prisma.publicTypebot.findUnique({\n      where: {\n        typebotId: id,\n      },\n      select: {\n        version: true,\n        groups: true,\n        typebot: {\n          select: {\n            workspace: {\n              select: {\n                plan: true,\n              },\n            },\n          },\n        },\n      },\n    });\n\n    if (!publicTypebot) continue;\n    if (publicTypebot.typebot.workspace.plan !== Plan.FREE) continue;\n\n    const groups = parseGroups(publicTypebot.groups, {\n      typebotVersion: publicTypebot.version,\n    });\n\n    let wasUpdated = false;\n    const updatedGroups = groups.map((group) => {\n      return {\n        ...group,\n        blocks: group.blocks.map((block) => {\n          if (\n            block.type === IntegrationBlockType.EMAIL &&\n            block.options?.credentialsId === \"default\"\n          ) {\n            wasUpdated = true;\n            return {\n              ...block,\n              options: {\n                ...block.options,\n                credentialsId: undefined,\n              },\n            };\n          }\n          return block;\n        }),\n      };\n    });\n\n    updatedGroupsList.push(updatedGroups);\n\n    if (wasUpdated) {\n      console.log(\"Updating...\", id);\n      await prisma.publicTypebot.update({\n        where: {\n          typebotId: id,\n        },\n        data: {\n          groups: updatedGroups,\n          updatedAt: new Date(),\n        },\n      });\n    }\n  }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 63.849999999999994,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: publicTypebot",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for publicTypebot and related data\n// Optimized: Single query with include\n\nconst publicTypebotWithRelations = await prisma.publicTypebot.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const { id } of topTypebots.data) {\n    bar.update(i++);\n\n    const publicTypebot = await prisma.publicTypebot.findUnique({\n      where: {\n        typebotId: id,\n      },\n      select: {\n        version: true,\n        groups: true,\n        typebot: {\n          select: {\n            workspace: {\n              select: {\n                plan: true,\n              },\n            },\n          },\n        },\n      },\n    });\n\n    if (!publicTypebot) continue;\n    if (publicTypebot.typebot.workspace.plan !== Plan.FREE) continue;\n\n    const groups = parseGroups(publicTypebot.groups, {\n      typebotVersion: publicTypebot.version,\n    });\n\n    let wasUpdated = false;\n    const updatedGroups = groups.map((group) => {\n      return {\n        ...group,\n        blocks: group.blocks.map((block) => {\n          if (\n            block.type === IntegrationBlockType.EMAIL &&\n            block.options?.credentialsId === \"default\"\n          ) {\n            wasUpdated = true;\n            return {\n              ...block,\n              options: {\n                ...block.options,\n                credentialsId: undefined,\n              },\n            };\n          }\n          return block;\n        }),\n      };\n    });\n\n    updatedGroupsList.push(updatedGroups);\n\n    if (wasUpdated) {\n      console.log(\"Updating...\", id);\n      await prisma.publicTypebot.update({\n        where: {\n          typebotId: id,\n        },\n        data: {\n          groups: updatedGroups,\n          updatedAt: new Date(),\n        },\n      });\n    }\n  }\n*/\n\n// Access related data directly: publicTypebotWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "packages/scripts/src/migrateSubscriptionsToUsageBased.ts",
   "line": 84,
   "severity": "critical",
   "description": "Found 3 database queries (Prisma.count(), Prisma.update(), Prisma.update()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const workspace of workspacesWithPaidPlan) {\n    i += 1;\n    console.log(\n      `(${i} / ${workspacesWithPaidPlan.length})`,\n      \"Migrating workspace:\",\n      workspace.id,\n      workspace.name,\n      workspace.stripeId,\n      JSON.stringify(workspace.members.map((member) => member.user.email)),\n    );\n    if (!workspace.stripeId) {\n      console.log(\"No stripe ID, skipping...\");\n      workspacesWithoutStripeId.push(workspace);\n      writeFileSync(\n        \"./workspacesWithoutStripeId.json\",\n        JSON.stringify(workspacesWithoutStripeId, null, 2),\n      );\n      continue;\n    }\n\n    const subscriptions = await stripe.subscriptions.list({\n      customer: workspace.stripeId,\n    });\n\n    const currentSubscription = subscriptions.data\n      .filter((sub) => [\"past_due\", \"active\"].includes(sub.status))\n      .sort((a, b) => a.created - b.created)\n      .shift();\n\n    if (!currentSubscription) {\n      console.log(\"No current subscription in workspace:\", workspace.id);\n      workspacesWithoutSubscription.push(workspace);\n      writeFileSync(\n        \"./workspacesWithoutSubscription.json\",\n        JSON.stringify(workspacesWithoutSubscription),\n      );\n      continue;\n    }\n\n    if (\n      currentSubscription.items.data.find(\n        (item) =>\n          item.price.id === starterChatsPriceId ||\n          item.price.id === proChatsPriceId,\n      )\n    ) {\n      console.log(\n        \"Already migrated to usage based billing for workspace. Skipping...\",\n      );\n      continue;\n    }\n    if (\n      !currentSubscription.items.data.find(\n        (item) =>\n          item.price.id === starterPriceId ||\n          item.price.id === proPriceId ||\n          item.price.id === starterYearlyPriceId ||\n          item.price.id === proYearlyPriceId,\n      )\n    ) {\n      console.log(\n        \"Could not find STARTER or PRO plan in items for workspace:\",\n        workspace.id,\n      );\n      failedWorkspaces.push(workspace);\n      writeFileSync(\n        \"./failedWorkspaces.json\",\n        JSON.stringify(failedWorkspaces, null, 2),\n      );\n      continue;\n    }\n\n    const newSubscription = await stripe.subscriptions.update(\n      currentSubscription.id,\n      {\n        items: [\n          ...currentSubscription.items.data.flatMap<Stripe.SubscriptionUpdateParams.Item>(\n            (item) => {\n              if (\n                item.price.id === starterPriceId ||\n                item.price.id === proPriceId\n              )\n                return {\n                  id: item.id,\n                  price: item.price.id,\n                  quantity: item.quantity,\n                };\n              if (\n                item.price.id === starterYearlyPriceId ||\n                item.price.id === proYearlyPriceId\n              )\n                return [\n                  {\n                    id: item.id,\n                    price: item.price.id,\n                    quantity: item.quantity,\n                    deleted: true,\n                  },\n                  {\n                    price:\n                      workspace.plan === \"STARTER\"\n                        ? starterPriceId\n                        : proPriceId,\n                    quantity: 1,\n                  },\n                ];\n              return {\n                id: item.id,\n                price: item.price.id,\n                quantity: item.quantity,\n                deleted: true,\n              };\n            },\n          ),\n          {\n            price:\n              workspace.plan === \"STARTER\"\n                ? starterChatsPriceId\n                : proChatsPriceId,\n          },\n        ],\n      },\n    );\n\n    const totalResults = await prisma.result.count({\n      where: {\n        typebot: { workspaceId: workspace.id },\n        hasStarted: true,\n        createdAt: {\n          gte: new Date(newSubscription.current_period_start * 1000),\n          lt: todayMidnight,\n        },\n      },\n    });\n\n    if (workspace.plan === \"STARTER\" && totalResults >= 4000) {\n      console.log(\n        \"Workspace has more than 4000 chats, automatically upgrading to PRO plan\",\n      );\n      const currentPlanItemId = newSubscription?.items.data.find((item) =>\n        [starterPriceId, proPriceId].includes(item.price.id),\n      )?.id;\n\n      if (!currentPlanItemId)\n        throw new Error(\"Could not find current plan item ID for workspace\");\n\n      await stripe.subscriptions.update(newSubscription.id, {\n        items: [\n          {\n            id: currentPlanItemId,\n            price: proPriceId,\n            quantity: 1,\n          },\n          {\n            id: newSubscription.items.data.find(\n              (item) =>\n                item.price.id === starterChatsPriceId ||\n                item.price.id === proChatsPriceId,\n            )?.id,\n            price: proChatsPriceId,\n          },\n        ],\n      });\n\n      await prisma.workspace.update({\n        where: { id: workspace.id },\n        data: {\n          plan: \"PRO\",\n        },\n      });\n    }\n\n    const subscriptionItem = newSubscription.items.data.find(\n      (item) =>\n        item.price.id === starterChatsPriceId ||\n        item.price.id === proChatsPriceId,\n    );\n\n    if (!subscriptionItem)\n      throw new Error(\n        `Could not find subscription item for workspace ${workspace.id}`,\n      );\n\n    const idempotencyKey = createId();\n\n    console.log(\"Reporting total results:\", totalResults);\n    await stripe.subscriptionItems.createUsageRecord(\n      subscriptionItem.id,\n      {\n        quantity: totalResults,\n        timestamp: \"now\",\n      },\n      {\n        idempotencyKey,\n      },\n    );\n\n    if (workspace.isQuarantined) {\n      await prisma.workspace.update({\n        where: { id: workspace.id },\n        data: {\n          isQuarantined: false,\n        },\n      });\n    }\n  }",
   "estimatedImpact": {
    "severityScore": 9,
    "description": "301 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 301,
     "queriesOptimal": 1,
     "performanceGain": "301x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 47.8,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: workspace, workspacesWithPaidPlan",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 7 async call(s) inside loop over workspacesWithPaidPlan\n// Problem: N+1 queries where N = workspacesWithPaidPlan.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = workspacesWithPaidPlan.map(workspace => workspace.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const workspace of workspacesWithPaidPlan) {\n    i += 1;\n    console.log(\n      `(${i} / ${workspacesWithPaidPlan.length})`,\n      \"Migrating workspace:\",\n      workspace.id,\n      workspace.name,\n      workspace.stripeId,\n      JSON.stringify(workspace.members.map((member) => member.user.email)),\n    );\n    if (!workspace.stripeId) {\n      console.log(\"No stripe ID, skipping...\");\n      workspacesWithoutStripeId.push(workspace);\n      writeFileSync(\n        \"./workspacesWithoutStripeId.json\",\n        JSON.stringify(workspacesWithoutStripeId, null, 2),\n      );\n      continue;\n    }\n\n    const subscriptions = await stripe.subscriptions.list({\n      customer: workspace.stripeId,\n    });\n\n    const currentSubscription = subscriptions.data\n      .filter((sub) => [\"past_due\", \"active\"].includes(sub.status))\n      .sort((a, b) => a.created - b.created)\n      .shift();\n\n    if (!currentSubscription) {\n      console.log(\"No current subscription in workspace:\", workspace.id);\n      workspacesWithoutSubscription.push(workspace);\n      writeFileSync(\n        \"./workspacesWithoutSubscription.json\",\n        JSON.stringify(workspacesWithoutSubscription),\n      );\n      continue;\n    }\n\n    if (\n      currentSubscription.items.data.find(\n        (item) =>\n          item.price.id === starterChatsPriceId ||\n          item.price.id === proChatsPriceId,\n      )\n    ) {\n      console.log(\n        \"Already migrated to usage based billing for workspace. Skipping...\",\n      );\n      continue;\n    }\n    if (\n      !currentSubscription.items.data.find(\n        (item) =>\n          item.price.id === starterPriceId ||\n          item.price.id === proPriceId ||\n          item.price.id === starterYearlyPriceId ||\n          item.price.id === proYearlyPriceId,\n      )\n    ) {\n      console.log(\n        \"Could not find STARTER or PRO plan in items for workspace:\",\n        workspace.id,\n      );\n      failedWorkspaces.push(workspace);\n      writeFileSync(\n        \"./failedWorkspaces.json\",\n        JSON.stringify(failedWorkspaces, null, 2),\n      );\n      continue;\n    }\n\n    const newSubscription = await stripe.subscriptions.update(\n      currentSubscription.id,\n      {\n        items: [\n          ...currentSubscription.items.data.flatMap<Stripe.SubscriptionUpdateParams.Item>(\n            (item) => {\n              if (\n                item.price.id === starterPriceId ||\n                item.price.id === proPriceId\n              )\n                return {\n                  id: item.id,\n                  price: item.price.id,\n                  quantity: item.quantity,\n                };\n              if (\n                item.price.id === starterYearlyPriceId ||\n                item.price.id === proYearlyPriceId\n              )\n                return [\n                  {\n                    id: item.id,\n                    price: item.price.id,\n                    quantity: item.quantity,\n                    deleted: true,\n                  },\n                  {\n                    price:\n                      workspace.plan === \"STARTER\"\n                        ? starterPriceId\n                        : proPriceId,\n                    quantity: 1,\n                  },\n                ];\n              return {\n                id: item.id,\n                price: item.price.id,\n                quantity: item.quantity,\n                deleted: true,\n              };\n            },\n          ),\n          {\n            price:\n              workspace.plan === \"STARTER\"\n                ? starterChatsPriceId\n                : proChatsPriceId,\n          },\n        ],\n      },\n    );\n\n    const totalResults = await prisma.result.count({\n      where: {\n        typebot: { workspaceId: workspace.id },\n        hasStarted: true,\n        createdAt: {\n          gte: new Date(newSubscription.current_period_start * 1000),\n          lt: todayMidnight,\n        },\n      },\n    });\n\n    if (workspace.plan === \"STARTER\" && totalResults >= 4000) {\n      console.log(\n        \"Workspace has more than 4000 chats, automatically upgrading to PRO plan\",\n      );\n      const currentPlanItemId = newSubscription?.items.data.find((item) =>\n        [starterPriceId, proPriceId].includes(item.price.id),\n      )?.id;\n\n      if (!currentPlanItemId)\n        throw new Error(\"Could not find current plan item ID for workspace\");\n\n      await stripe.subscriptions.update(newSubscription.id, {\n        items: [\n          {\n            id: currentPlanItemId,\n            price: proPriceId,\n            quantity: 1,\n          },\n          {\n            id: newSubscription.items.data.find(\n              (item) =>\n                item.price.id === starterChatsPriceId ||\n                item.price.id === proChatsPriceId,\n            )?.id,\n            price: proChatsPriceId,\n          },\n        ],\n      });\n\n      await prisma.workspace.update({\n        where: { id: workspace.id },\n        data: {\n          plan: \"PRO\",\n        },\n      });\n    }\n\n    const subscriptionItem = newSubscription.items.data.find(\n      (item) =>\n        item.price.id === starterChatsPriceId ||\n        item.price.id === proChatsPriceId,\n    );\n\n    if (!subscriptionItem)\n      throw new Error(\n        `Could not find subscription item for workspace ${workspace.id}`,\n      );\n\n    const idempotencyKey = createId();\n\n    console.log(\"Reporting total results:\", totalResults);\n    await stripe.subscriptionItems.createUsageRecord(\n      subscriptionItem.id,\n      {\n        quantity: totalResults,\n        timestamp: \"now\",\n      },\n      {\n        idempotencyKey,\n      },\n    );\n\n    if (workspace.isQuarantined) {\n      await prisma.workspace.update({\n        where: { id: workspace.id },\n        data: {\n          isQuarantined: false,\n        },\n      });\n    }\n  }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "memoize",
     "rank": 2,
     "fitnessScore": 47.4,
     "reasoning": "Added memoization for 2 repeated calls",
     "code": "const updateCache = new Map(); // Memoize stripe.subscriptions.update\nconst updateCache = new Map(); // Memoize prisma.workspace.update\n\nfor (const workspace of workspacesWithPaidPlan) {\n    i += 1;\n    console.log(\n      `(${i} / ${workspacesWithPaidPlan.length})`,\n      \"Migrating workspace:\",\n      workspace.id,\n      workspace.name,\n      workspace.stripeId,\n      JSON.stringify(workspace.members.map((member) => member.user.email)),\n    );\n    if (!workspace.stripeId) {\n      console.log(\"No stripe ID, skipping...\");\n      workspacesWithoutStripeId.push(workspace);\n      writeFileSync(\n        \"./workspacesWithoutStripeId.json\",\n        JSON.stringify(workspacesWithoutStripeId, null, 2),\n      );\n      continue;\n    }\n\n    const subscriptions = await stripe.subscriptions.list({\n      customer: workspace.stripeId,\n    });\n\n    const currentSubscription = subscriptions.data\n      .filter((sub) => [\"past_due\", \"active\"].includes(sub.status))\n      .sort((a, b) => a.created - b.created)\n      .shift();\n\n    if (!currentSubscription) {\n      console.log(\"No current subscription in workspace:\", workspace.id);\n      workspacesWithoutSubscription.push(workspace);\n      writeFileSync(\n        \"./workspacesWithoutSubscription.json\",\n        JSON.stringify(workspacesWithoutSubscription),\n      );\n      continue;\n    }\n\n    if (\n      currentSubscription.items.data.find(\n        (item) =>\n          item.price.id === starterChatsPriceId ||\n          item.price.id === proChatsPriceId,\n      )\n    ) {\n      console.log(\n        \"Already migrated to usage based billing for workspace. Skipping...\",\n      );\n      continue;\n    }\n    if (\n      !currentSubscription.items.data.find(\n        (item) =>\n          item.price.id === starterPriceId ||\n          item.price.id === proPriceId ||\n          item.price.id === starterYearlyPriceId ||\n          item.price.id === proYearlyPriceId,\n      )\n    ) {\n      console.log(\n        \"Could not find STARTER or PRO plan in items for workspace:\",\n        workspace.id,\n      );\n      failedWorkspaces.push(workspace);\n      writeFileSync(\n        \"./failedWorkspaces.json\",\n        JSON.stringify(failedWorkspaces, null, 2),\n      );\n      continue;\n    }\n\n    const newSubscription = await stripe.subscriptions.update(\n      currentSubscription.id,\n      {\n        items: [\n          ...currentSubscription.items.data.flatMap<Stripe.SubscriptionUpdateParams.Item>(\n            (item) => {\n              if (\n                item.price.id === starterPriceId ||\n                item.price.id === proPriceId\n              )\n                return {\n                  id: item.id,\n                  price: item.price.id,\n                  quantity: item.quantity,\n                };\n              if (\n                item.price.id === starterYearlyPriceId ||\n                item.price.id === proYearlyPriceId\n              )\n                return [\n                  {\n                    id: item.id,\n                    price: item.price.id,\n                    quantity: item.quantity,\n                    deleted: true,\n                  },\n                  {\n                    price:\n                      workspace.plan === \"STARTER\"\n                        ? starterPriceId\n                        : proPriceId,\n                    quantity: 1,\n                  },\n                ];\n              return {\n                id: item.id,\n                price: item.price.id,\n                quantity: item.quantity,\n                deleted: true,\n              };\n            },\n          ),\n          {\n            price:\n              workspace.plan === \"STARTER\"\n                ? starterChatsPriceId\n                : proChatsPriceId,\n          },\n        ],\n      },\n    );\n\n    const totalResults = await prisma.result.count({\n      where: {\n        typebot: { workspaceId: workspace.id },\n        hasStarted: true,\n        createdAt: {\n          gte: new Date(newSubscription.current_period_start * 1000),\n          lt: todayMidnight,\n        },\n      },\n    });\n\n    if (workspace.plan === \"STARTER\" && totalResults >= 4000) {\n      console.log(\n        \"Workspace has more than 4000 chats, automatically upgrading to PRO plan\",\n      );\n      const currentPlanItemId = newSubscription?.items.data.find((item) =>\n        [starterPriceId, proPriceId].includes(item.price.id),\n      )?.id;\n\n      if (!currentPlanItemId)\n        throw new Error(\"Could not find current plan item ID for workspace\");\n\n      await stripe.subscriptions.update(newSubscription.id, {\n        items: [\n          {\n            id: currentPlanItemId,\n            price: proPriceId,\n            quantity: 1,\n          },\n          {\n            id: newSubscription.items.data.find(\n              (item) =>\n                item.price.id === starterChatsPriceId ||\n                item.price.id === proChatsPriceId,\n            )?.id,\n            price: proChatsPriceId,\n          },\n        ],\n      });\n\n      await prisma.workspace.update({\n        where: { id: workspace.id },\n        data: {\n          plan: \"PRO\",\n        },\n      });\n    }\n\n    const subscriptionItem = newSubscription.items.data.find(\n      (item) =>\n        item.price.id === starterChatsPriceId ||\n        item.price.id === proChatsPriceId,\n    );\n\n    if (!subscriptionItem)\n      throw new Error(\n        `Could not find subscription item for workspace ${workspace.id}`,\n      );\n\n    const idempotencyKey = createId();\n\n    console.log(\"Reporting total results:\", totalResults);\n    await stripe.subscriptionItems.createUsageRecord(\n      subscriptionItem.id,\n      {\n        quantity: totalResults,\n        timestamp: \"now\",\n      },\n      {\n        idempotencyKey,\n      },\n    );\n\n    if (workspace.isQuarantined) {\n      await prisma.workspace.update({\n        where: { id: workspace.id },\n        data: {\n          isQuarantined: false,\n        },\n      });\n    }\n  }"
    },
    {
     "type": "memoization",
     "rank": 3,
     "fitnessScore": 45.9,
     "reasoning": "Add memoization cache for repeated expensive calls\nPreserved: stripe.subscriptions.update, prisma.workspace.update",
     "code": "// OPTIMIZED: Memoization for repeated expensive calls\n// Found 2 repeated async call(s)\n\n// Memoize stripe.subscriptions.update (called 2x)\nconst updateCache = new Map();\nasync function memoizedUpdate(key) {\n  if (updateCache.has(key)) return updateCache.get(key);\n  const result = await stripe.subscriptions.update(key);\n  updateCache.set(key, result);\n  return result;\n}\n\n// Memoize prisma.workspace.update (called 2x)\nconst updateCache = new Map();\nasync function memoizedUpdate(key) {\n  if (updateCache.has(key)) return updateCache.get(key);\n  const result = await prisma.workspace.update(key);\n  updateCache.set(key, result);\n  return result;\n}\n\n// Original code (replace calls with memoized versions):\nfor (const workspace of workspacesWithPaidPlan) {\n    i += 1;\n    console.log(\n      `(${i} / ${workspacesWithPaidPlan.length})`,\n      \"Migrating workspace:\",\n      workspace.id,\n      workspace.name,\n      workspace.stripeId,\n      JSON.stringify(workspace.members.map((member) => member.user.email)),\n    );\n    if (!workspace.stripeId) {\n      console.log(\"No stripe ID, skipping...\");\n      workspacesWithoutStripeId.push(workspace);\n      writeFileSync(\n        \"./workspacesWithoutStripeId.json\",\n        JSON.stringify(workspacesWithoutStripeId, null, 2),\n      );\n      continue;\n    }\n\n    const subscriptions = await stripe.subscriptions.list({\n      customer: workspace.stripeId,\n    });\n\n    const currentSubscription = subscriptions.data\n      .filter((sub) => [\"past_due\", \"active\"].includes(sub.status))\n      .sort((a, b) => a.created - b.created)\n      .shift();\n\n    if (!currentSubscription) {\n      console.log(\"No current subscription in workspace:\", workspace.id);\n      workspacesWithoutSubscription.push(workspace);\n      writeFileSync(\n        \"./workspacesWithoutSubscription.json\",\n        JSON.stringify(workspacesWithoutSubscription),\n      );\n      continue;\n    }\n\n    if (\n      currentSubscription.items.data.find(\n        (item) =>\n          item.price.id === starterChatsPriceId ||\n          item.price.id === proChatsPriceId,\n      )\n    ) {\n      console.log(\n        \"Already migrated to usage based billing for workspace. Skipping...\",\n      );\n      continue;\n    }\n    if (\n      !currentSubscription.items.data.find(\n        (item) =>\n          item.price.id === starterPriceId ||\n          item.price.id === proPriceId ||\n          item.price.id === starterYearlyPriceId ||\n          item.price.id === proYearlyPriceId,\n      )\n    ) {\n      console.log(\n        \"Could not find STARTER or PRO plan in items for workspace:\",\n        workspace.id,\n      );\n      failedWorkspaces.push(workspace);\n      writeFileSync(\n        \"./failedWorkspaces.json\",\n        JSON.stringify(failedWorkspaces, null, 2),\n      );\n      continue;\n    }\n\n    const newSubscription = await stripe.subscriptions.update(\n      currentSubscription.id,\n      {\n        items: [\n          ...currentSubscription.items.data.flatMap<Stripe.SubscriptionUpdateParams.Item>(\n            (item) => {\n              if (\n                item.price.id === starterPriceId ||\n                item.price.id === proPriceId\n              )\n                return {\n                  id: item.id,\n                  price: item.price.id,\n                  quantity: item.quantity,\n                };\n              if (\n                item.price.id === starterYearlyPriceId ||\n                item.price.id === proYearlyPriceId\n              )\n                return [\n                  {\n                    id: item.id,\n                    price: item.price.id,\n                    quantity: item.quantity,\n                    deleted: true,\n                  },\n                  {\n                    price:\n                      workspace.plan === \"STARTER\"\n                        ? starterPriceId\n                        : proPriceId,\n                    quantity: 1,\n                  },\n                ];\n              return {\n                id: item.id,\n                price: item.price.id,\n                quantity: item.quantity,\n                deleted: true,\n              };\n            },\n          ),\n          {\n            price:\n              workspace.plan === \"STARTER\"\n                ? starterChatsPriceId\n                : proChatsPriceId,\n          },\n        ],\n      },\n    );\n\n    const totalResults = await prisma.result.count({\n      where: {\n        typebot: { workspaceId: workspace.id },\n        hasStarted: true,\n        createdAt: {\n          gte: new Date(newSubscription.current_period_start * 1000),\n          lt: todayMidnight,\n        },\n      },\n    });\n\n    if (workspace.plan === \"STARTER\" && totalResults >= 4000) {\n      console.log(\n        \"Workspace has more than 4000 chats, automatically upgrading to PRO plan\",\n      );\n      const currentPlanItemId = newSubscription?.items.data.find((item) =>\n        [starterPriceId, proPriceId].includes(item.price.id),\n      )?.id;\n\n      if (!currentPlanItemId)\n        throw new Error(\"Could not find current plan item ID for workspace\");\n\n      await stripe.subscriptions.update(newSubscription.id, {\n        items: [\n          {\n            id: currentPlanItemId,\n            price: proPriceId,\n            quantity: 1,\n          },\n          {\n            id: newSubscription.items.data.find(\n              (item) =>\n                item.price.id === starterChatsPriceId ||\n                item.price.id === proChatsPriceId,\n            )?.id,\n            price: proChatsPriceId,\n          },\n        ],\n      });\n\n      await prisma.workspace.update({\n        where: { id: workspace.id },\n        data: {\n          plan: \"PRO\",\n        },\n      });\n    }\n\n    const subscriptionItem = newSubscription.items.data.find(\n      (item) =>\n        item.price.id === starterChatsPriceId ||\n        item.price.id === proChatsPriceId,\n    );\n\n    if (!subscriptionItem)\n      throw new Error(\n        `Could not find subscription item for workspace ${workspace.id}`,\n      );\n\n    const idempotencyKey = createId();\n\n    console.log(\"Reporting total results:\", totalResults);\n    await stripe.subscriptionItems.createUsageRecord(\n      subscriptionItem.id,\n      {\n        quantity: totalResults,\n        timestamp: \"now\",\n      },\n      {\n        idempotencyKey,\n      },\n    );\n\n    if (workspace.isQuarantined) {\n      await prisma.workspace.update({\n        where: { id: workspace.id },\n        data: {\n          isQuarantined: false,\n        },\n      });\n    }\n  }"
    },
    {
     "type": "prisma-include",
     "rank": 4,
     "fitnessScore": 42.65,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: result",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for result and related data\n// Optimized: Single query with include\n\nconst resultWithRelations = await prisma.result.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const workspace of workspacesWithPaidPlan) {\n    i += 1;\n    console.log(\n      `(${i} / ${workspacesWithPaidPlan.length})`,\n      \"Migrating workspace:\",\n      workspace.id,\n      workspace.name,\n      workspace.stripeId,\n      JSON.stringify(workspace.members.map((member) => member.user.email)),\n    );\n    if (!workspace.stripeId) {\n      console.log(\"No stripe ID, skipping...\");\n      workspacesWithoutStripeId.push(workspace);\n      writeFileSync(\n        \"./workspacesWithoutStripeId.json\",\n        JSON.stringify(workspacesWithoutStripeId, null, 2),\n      );\n      continue;\n    }\n\n    const subscriptions = await stripe.subscriptions.list({\n      customer: workspace.stripeId,\n    });\n\n    const currentSubscription = subscriptions.data\n      .filter((sub) => [\"past_due\", \"active\"].includes(sub.status))\n      .sort((a, b) => a.created - b.created)\n      .shift();\n\n    if (!currentSubscription) {\n      console.log(\"No current subscription in workspace:\", workspace.id);\n      workspacesWithoutSubscription.push(workspace);\n      writeFileSync(\n        \"./workspacesWithoutSubscription.json\",\n        JSON.stringify(workspacesWithoutSubscription),\n      );\n      continue;\n    }\n\n    if (\n      currentSubscription.items.data.find(\n        (item) =>\n          item.price.id === starterChatsPriceId ||\n          item.price.id === proChatsPriceId,\n      )\n    ) {\n      console.log(\n        \"Already migrated to usage based billing for workspace. Skipping...\",\n      );\n      continue;\n    }\n    if (\n      !currentSubscription.items.data.find(\n        (item) =>\n          item.price.id === starterPriceId ||\n          item.price.id === proPriceId ||\n          item.price.id === starterYearlyPriceId ||\n          item.price.id === proYearlyPriceId,\n      )\n    ) {\n      console.log(\n        \"Could not find STARTER or PRO plan in items for workspace:\",\n        workspace.id,\n      );\n      failedWorkspaces.push(workspace);\n      writeFileSync(\n        \"./failedWorkspaces.json\",\n        JSON.stringify(failedWorkspaces, null, 2),\n      );\n      continue;\n    }\n\n    const newSubscription = await stripe.subscriptions.update(\n      currentSubscription.id,\n      {\n        items: [\n          ...currentSubscription.items.data.flatMap<Stripe.SubscriptionUpdateParams.Item>(\n            (item) => {\n              if (\n                item.price.id === starterPriceId ||\n                item.price.id === proPriceId\n              )\n                return {\n                  id: item.id,\n                  price: item.price.id,\n                  quantity: item.quantity,\n                };\n              if (\n                item.price.id === starterYearlyPriceId ||\n                item.price.id === proYearlyPriceId\n              )\n                return [\n                  {\n                    id: item.id,\n                    price: item.price.id,\n                    quantity: item.quantity,\n                    deleted: true,\n                  },\n                  {\n                    price:\n                      workspace.plan === \"STARTER\"\n                        ? starterPriceId\n                        : proPriceId,\n                    quantity: 1,\n                  },\n                ];\n              return {\n                id: item.id,\n                price: item.price.id,\n                quantity: item.quantity,\n                deleted: true,\n              };\n            },\n          ),\n          {\n            price:\n              workspace.plan === \"STARTER\"\n                ? starterChatsPriceId\n                : proChatsPriceId,\n          },\n        ],\n      },\n    );\n\n    const totalResults = await prisma.result.count({\n      where: {\n        typebot: { workspaceId: workspace.id },\n        hasStarted: true,\n        createdAt: {\n          gte: new Date(newSubscription.current_period_start * 1000),\n          lt: todayMidnight,\n        },\n      },\n    });\n\n    if (workspace.plan === \"STARTER\" && totalResults >= 4000) {\n      console.log(\n        \"Workspace has more than 4000 chats, automatically upgrading to PRO plan\",\n      );\n      const currentPlanItemId = newSubscription?.items.data.find((item) =>\n        [starterPriceId, proPriceId].includes(item.price.id),\n      )?.id;\n\n      if (!currentPlanItemId)\n        throw new Error(\"Could not find current plan item ID for workspace\");\n\n      await stripe.subscriptions.update(newSubscription.id, {\n        items: [\n          {\n            id: currentPlanItemId,\n            price: proPriceId,\n            quantity: 1,\n          },\n          {\n            id: newSubscription.items.data.find(\n              (item) =>\n                item.price.id === starterChatsPriceId ||\n                item.price.id === proChatsPriceId,\n            )?.id,\n            price: proChatsPriceId,\n          },\n        ],\n      });\n\n      await prisma.workspace.update({\n        where: { id: workspace.id },\n        data: {\n          plan: \"PRO\",\n        },\n      });\n    }\n\n    const subscriptionItem = newSubscription.items.data.find(\n      (item) =>\n        item.price.id === starterChatsPriceId ||\n        item.price.id === proChatsPriceId,\n    );\n\n    if (!subscriptionItem)\n      throw new Error(\n        `Could not find subscription item for workspace ${workspace.id}`,\n      );\n\n    const idempotencyKey = createId();\n\n    console.log(\"Reporting total results:\", totalResults);\n    await stripe.subscriptionItems.createUsageRecord(\n      subscriptionItem.id,\n      {\n        quantity: totalResults,\n        timestamp: \"now\",\n      },\n      {\n        idempotencyKey,\n      },\n    );\n\n    if (workspace.isQuarantined) {\n      await prisma.workspace.update({\n        where: { id: workspace.id },\n        data: {\n          isQuarantined: false,\n        },\n      });\n    }\n  }\n*/\n\n// Access related data directly: resultWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "packages/scripts/src/inspectUser.ts",
   "line": 94,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.count()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const typebot of workspace.workspace.typebots) {\n      const resultsCount = await prisma.result.count({\n        where: {\n          typebotId: typebot.id,\n          isArchived: false,\n          hasStarted: true,\n        },\n      });\n\n      if (resultsCount === 0) continue;\n\n      console.log(\n        `Typebot \"${typebot.name}\" (${typebot.id}) has ${resultsCount} collected results`,\n      );\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 84.6,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: typebot, workspace.workspace.typebots",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over workspace.workspace.typebots\n// Problem: N+1 queries where N = workspace.workspace.typebots.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = workspace.workspace.typebots.map(typebot => typebot.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const typebot of workspace.workspace.typebots) {\n      const resultsCount = await prisma.result.count({\n        where: {\n          typebotId: typebot.id,\n          isArchived: false,\n          hasStarted: true,\n        },\n      });\n\n      if (resultsCount === 0) continue;\n\n      console.log(\n        `Typebot \"${typebot.name}\" (${typebot.id}) has ${resultsCount} collected results`,\n      );\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 84.5,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: result",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for result and related data\n// Optimized: Single query with include\n\nconst resultWithRelations = await prisma.result.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const typebot of workspace.workspace.typebots) {\n      const resultsCount = await prisma.result.count({\n        where: {\n          typebotId: typebot.id,\n          isArchived: false,\n          hasStarted: true,\n        },\n      });\n\n      if (resultsCount === 0) continue;\n\n      console.log(\n        `Typebot \"${typebot.name}\" (${typebot.id}) has ${resultsCount} collected results`,\n      );\n    }\n*/\n\n// Access related data directly: resultWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "packages/scripts/src/bulkUpdate.ts",
   "line": 21,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.updateMany()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "results\n    .map((result) => {\n      if (\n        result.variables.some((variable) => typeof variable.value !== \"string\")\n      ) {\n        return prisma.result.updateMany({\n          where: { id: result.id },\n          data: {\n            variables: result.variables\n              .map((variable) => ({\n                ...variable,\n                value:\n                  typeof variable.value !== \"string\"\n                    ? safeStringify(variable.value)\n                    : variable.value,\n              }))\n              .filter(isDefined),\n          },\n        });\n      }\n    })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "prisma-include",
     "rank": 1,
     "fitnessScore": 84.15,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: result",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for result and related data\n// Optimized: Single query with include\n\nconst resultWithRelations = await prisma.result.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nresults\n    .map((result) => {\n      if (\n        result.variables.some((variable) => typeof variable.value !== \"string\")\n      ) {\n        return prisma.result.updateMany({\n          where: { id: result.id },\n          data: {\n            variables: result.variables\n              .map((variable) => ({\n                ...variable,\n                value:\n                  typeof variable.value !== \"string\"\n                    ? safeStringify(variable.value)\n                    : variable.value,\n              }))\n              .filter(isDefined),\n          },\n        });\n      }\n    })\n*/\n\n// Access related data directly: resultWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "packages/scripts/src/helpers/trackAndReportYesterdaysResults.ts",
   "line": 46,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.groupBy()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const workspace of recentWorkspaces) {\n    try {\n      const results = await prisma.result.groupBy({\n        by: [\"typebotId\"],\n        _count: {\n          _all: true,\n        },\n        where: {\n          typebotId: { in: workspace.typebots.map((typebot) => typebot.id) },\n          hasStarted: true,\n          createdAt: {\n            gte: yesterdayMidnight,\n            lt: todayMidnight,\n          },\n        },\n      });\n      const olderAdmin = workspace.members\n        .sort((a, b) => a.createdAt.getTime() - b.createdAt.getTime())\n        .at(0);\n      if (!olderAdmin) continue;\n      for (const result of results) {\n        if (result._count._all === 0) continue;\n        newResultsCollectedEvents.push({\n          name: \"New results collected\",\n          typebotId: result.typebotId,\n          workspaceId: workspace.id,\n          userId: olderAdmin.user.id,\n          data: {\n            total: result._count._all,\n          },\n        });\n        resultsSum += result._count._all;\n      }\n    } catch (error) {\n      console.error(\"❌ Error processing workspace\", workspace.id);\n      throw error;\n    }\n  }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "prisma-include",
     "rank": 1,
     "fitnessScore": 81.85,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: result",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for result and related data\n// Optimized: Single query with include\n\nconst resultWithRelations = await prisma.result.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const workspace of recentWorkspaces) {\n    try {\n      const results = await prisma.result.groupBy({\n        by: [\"typebotId\"],\n        _count: {\n          _all: true,\n        },\n        where: {\n          typebotId: { in: workspace.typebots.map((typebot) => typebot.id) },\n          hasStarted: true,\n          createdAt: {\n            gte: yesterdayMidnight,\n            lt: todayMidnight,\n          },\n        },\n      });\n      const olderAdmin = workspace.members\n        .sort((a, b) => a.createdAt.getTime() - b.createdAt.getTime())\n        .at(0);\n      if (!olderAdmin) continue;\n      for (const result of results) {\n        if (result._count._all === 0) continue;\n        newResultsCollectedEvents.push({\n          name: \"New results collected\",\n          typebotId: result.typebotId,\n          workspaceId: workspace.id,\n          userId: olderAdmin.user.id,\n          data: {\n            total: result._count._all,\n          },\n        });\n        resultsSum += result._count._all;\n      }\n    } catch (error) {\n      console.error(\"❌ Error processing workspace\", workspace.id);\n      throw error;\n    }\n  }\n*/\n\n// Access related data directly: resultWithRelations[0].relation"
    },
    {
     "type": "batch-query-before-loop",
     "rank": 2,
     "fitnessScore": 78.75,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: result, results",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over results\n// Problem: N+1 queries where N = results.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = results.map(result => result.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const workspace of recentWorkspaces) {\n    try {\n      const results = await prisma.result.groupBy({\n        by: [\"typebotId\"],\n        _count: {\n          _all: true,\n        },\n        where: {\n          typebotId: { in: workspace.typebots.map((typebot) => typebot.id) },\n          hasStarted: true,\n          createdAt: {\n            gte: yesterdayMidnight,\n            lt: todayMidnight,\n          },\n        },\n      });\n      const olderAdmin = workspace.members\n        .sort((a, b) => a.createdAt.getTime() - b.createdAt.getTime())\n        .at(0);\n      if (!olderAdmin) continue;\n      for (const result of results) {\n        if (result._count._all === 0) continue;\n        newResultsCollectedEvents.push({\n          name: \"New results collected\",\n          typebotId: result.typebotId,\n          workspaceId: workspace.id,\n          userId: olderAdmin.user.id,\n          data: {\n            total: result._count._all,\n          },\n        });\n        resultsSum += result._count._all;\n      }\n    } catch (error) {\n      console.error(\"❌ Error processing workspace\", workspace.id);\n      throw error;\n    }\n  }\n\n// Performance: 1 query instead of N queries"
    }
   ]
  },
  {
   "file": "packages/scripts/src/helpers/checkAndReportLastHourResults.ts",
   "line": 68,
   "severity": "critical",
   "description": "Found 3 database queries (Prisma.updateMany(), Prisma.updateMany(), Prisma.updateMany()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const workspace of workspaces) {\n    if (workspace.isQuarantined) continue;\n    const subscription = await getSubscription(workspace, { stripe });\n    const { totalChatsUsed, usagePeriodStart } = await getUsage({\n      workspaceId: workspace.id,\n      subscription,\n    });\n    const isEnterpriseUsageBasedSubscription =\n      workspace.plan === Plan.ENTERPRISE &&\n      isDefined(subscription && findMeteredSubscriptionItem(subscription));\n    const chatsLimit = isEnterpriseUsageBasedSubscription\n      ? \"inf\"\n      : getChatsLimit(workspace);\n    const enforcedChatsLimit =\n      workspace.chatsHardLimit ??\n      (chatsLimit === \"inf\" ? undefined : chatsLimit);\n\n    if (isDefined(enforcedChatsLimit))\n      limitWarningEmailEvents.push(\n        ...(await sendLimitWarningEmails({\n          chatsLimit: enforcedChatsLimit,\n          totalChatsUsed,\n          usagePeriodStart,\n          workspace,\n        })),\n      );\n\n    const isUsageBasedSubscription =\n      isEnterpriseUsageBasedSubscription ||\n      isDefined(\n        subscription?.items.data.find(\n          (item) =>\n            item.price.id === env.STRIPE_STARTER_PRICE_ID ||\n            item.price.id === env.STRIPE_PRO_PRICE_ID,\n        ),\n      );\n\n    if (\n      isUsageBasedSubscription &&\n      subscription &&\n      (workspace.plan === \"STARTER\" ||\n        workspace.plan === \"PRO\" ||\n        workspace.plan === \"ENTERPRISE\")\n    ) {\n      if (workspace.plan === \"STARTER\" && totalChatsUsed >= 4000) {\n        console.log(\n          \"Workspace has more than 4000 chats, automatically upgrading to PRO plan\",\n        );\n        const autoUpgradeResult = await autoUpgradeToPro(subscription, {\n          stripe,\n          workspaceId: workspace.id,\n        });\n        if (autoUpgradeResult.status === \"success\") {\n          autoUpgradeEvents.push(\n            ...workspace.members\n              .filter((member) => member.role === WorkspaceRole.ADMIN)\n              .map((member) => ({\n                name: \"Subscription automatically updated\" as const,\n                userId: member.user.id,\n                workspaceId: workspace.id,\n                data: {\n                  plan: Plan.PRO,\n                },\n              })),\n          );\n          await reportUsageToStripe(totalChatsUsed, {\n            stripe,\n            subscription: autoUpgradeResult.newSubscription,\n          });\n        } else if (\n          autoUpgradeResult.status === \"error\" &&\n          autoUpgradeResult.reason === \"payment_required\"\n        ) {\n          console.log(\n            `Workspace ${workspace.id} has more than 4000 chats, but payment is required to upgrade to PRO plan, automatically quarantining workspace...`,\n          );\n          await prisma.workspace.updateMany({\n            where: { id: workspace.id },\n            data: { isQuarantined: true },\n          });\n          quarantineEvents.push(\n            ...workspace.members\n              .filter((member) => member.role === WorkspaceRole.ADMIN)\n              .map((member) => ({\n                name: \"Workspace automatically quarantined\" as const,\n                userId: member.user.id,\n                workspaceId: workspace.id,\n                data: {\n                  reason: \"auto upgrade payment failed\" as const,\n                },\n              })),\n          );\n        }\n      } else {\n        await reportUsageToStripe(totalChatsUsed, { stripe, subscription });\n\n        if (workspace.plan === \"PRO\") {\n          const isSuspicious = await isSuspiciousWorkspace(\n            subscription,\n            totalChatsUsed,\n            workspace.plan,\n            { stripe },\n          );\n\n          if (isSuspicious) {\n            console.log(\n              `Suspicious Pro workspace ${workspace.id} collected ${totalChatsUsed} chats, resetting billing cycle...`,\n            );\n            const adminMembers = workspace.members.filter(\n              (member) => member.role === WorkspaceRole.ADMIN,\n            );\n            const adminEmails = adminMembers\n              .map((member) => member.user.email)\n              .filter(isDefined);\n\n            try {\n              await chargeAndResetBillingCycle(subscription, { stripe });\n              console.log(\n                `Successfully reset billing cycle for workspace ${workspace.id}`,\n              );\n              await sendBillingCycleResetEmail({\n                to: adminEmails,\n                workspaceName: workspace.name,\n                totalChatsUsed,\n                url: `${process.env.NEXTAUTH_URL}/typebots?workspaceId=${workspace.id}`,\n              });\n              billingCycleResetEvents.push(\n                ...adminMembers.map((member) => ({\n                  name: \"Billing cycle reset\" as const,\n                  userId: member.user.id,\n                  workspaceId: workspace.id,\n                })),\n              );\n              continue;\n            } catch (error) {\n              console.error(\n                `Failed to reset billing cycle for workspace ${workspace.id}, quarantining...`,\n                error,\n              );\n              await sendBillingCycleResetFailedEmail({\n                to: adminEmails,\n                workspaceName: workspace.name,\n                totalChatsUsed,\n              });\n              await prisma.workspace.updateMany({\n                where: { id: workspace.id },\n                data: { isQuarantined: true },\n              });\n              quarantineEvents.push(\n                ...adminMembers.map((member) => ({\n                  name: \"Workspace automatically quarantined\" as const,\n                  userId: member.user.id,\n                  workspaceId: workspace.id,\n                  data: {\n                    reason:\n                      \"suspicious billing cycle reset payment failed\" as const,\n                  },\n                })),\n              );\n            }\n          }\n        }\n      }\n    }\n\n    if (\n      isDefined(enforcedChatsLimit) &&\n      ((workspace.plan === Plan.FREE &&\n        totalChatsUsed > enforcedChatsLimit * 1.5) ||\n        (isDefined(workspace.chatsHardLimit) &&\n          totalChatsUsed >= workspace.chatsHardLimit))\n    ) {\n      console.log(`Automatically quarantine workspace ${workspace.id}...`);\n      await prisma.workspace.updateMany({\n        where: { id: workspace.id },\n        data: { isQuarantined: true },\n      });\n      quarantineEvents.push(\n        ...workspace.members\n          .filter((member) => member.role === WorkspaceRole.ADMIN)\n          .map((member) => ({\n            name: \"Workspace automatically quarantined\" as const,\n            userId: member.user.id,\n            workspaceId: workspace.id,\n            data: {\n              totalChatsUsed,\n              chatsLimit: enforcedChatsLimit,\n              reason: \"free limit reached\" as const,\n            },\n          })),\n      );\n    }\n  }",
   "estimatedImpact": {
    "severityScore": 9,
    "description": "301 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 301,
     "queriesOptimal": 1,
     "performanceGain": "301x slower"
    }
   },
   "solutions": [
    {
     "type": "single-pass",
     "rank": 1,
     "fitnessScore": 58.55,
     "reasoning": "Converted filter().map() chain to single reduce() pass",
     "code": "for (const workspace of workspaces) {\n  if (workspace.isQuarantined) continue;\n  const subscription = await getSubscription(workspace, {\n    stripe\n  });\n  const {\n    totalChatsUsed,\n    usagePeriodStart\n  } = await getUsage({\n    workspaceId: workspace.id,\n    subscription\n  });\n  const isEnterpriseUsageBasedSubscription = workspace.plan === Plan.ENTERPRISE && isDefined(subscription && findMeteredSubscriptionItem(subscription));\n  const chatsLimit = isEnterpriseUsageBasedSubscription ? \"inf\" : getChatsLimit(workspace);\n  const enforcedChatsLimit = workspace.chatsHardLimit ?? (chatsLimit === \"inf\" ? undefined : chatsLimit);\n  if (isDefined(enforcedChatsLimit)) limitWarningEmailEvents.push(...(await sendLimitWarningEmails({\n    chatsLimit: enforcedChatsLimit,\n    totalChatsUsed,\n    usagePeriodStart,\n    workspace\n  })));\n  const isUsageBasedSubscription = isEnterpriseUsageBasedSubscription || isDefined(subscription?.items.data.find(item => item.price.id === env.STRIPE_STARTER_PRICE_ID || item.price.id === env.STRIPE_PRO_PRICE_ID));\n  if (isUsageBasedSubscription && subscription && (workspace.plan === \"STARTER\" || workspace.plan === \"PRO\" || workspace.plan === \"ENTERPRISE\")) {\n    if (workspace.plan === \"STARTER\" && totalChatsUsed >= 4000) {\n      console.log(\"Workspace has more than 4000 chats, automatically upgrading to PRO plan\");\n      const autoUpgradeResult = await autoUpgradeToPro(subscription, {\n        stripe,\n        workspaceId: workspace.id\n      });\n      if (autoUpgradeResult.status === \"success\") {\n        autoUpgradeEvents.push(...workspace.members.reduce((acc, member) => {\n          if (member.role === WorkspaceRole.ADMIN) acc.push({\n            name: \"Subscription automatically updated\" as const,\n            userId: member.user.id,\n            workspaceId: workspace.id,\n            data: {\n              plan: Plan.PRO\n            }\n          });\n          return acc;\n        }, []));\n        await reportUsageToStripe(totalChatsUsed, {\n          stripe,\n          subscription: autoUpgradeResult.newSubscription\n        });\n      } else if (autoUpgradeResult.status === \"error\" && autoUpgradeResult.reason === \"payment_required\") {\n        console.log(`Workspace ${workspace.id} has more than 4000 chats, but payment is required to upgrade to PRO plan, automatically quarantining workspace...`);\n        await prisma.workspace.updateMany({\n          where: {\n            id: workspace.id\n          },\n          data: {\n            isQuarantined: true\n          }\n        });\n        quarantineEvents.push(...workspace.members.reduce((acc, member) => {\n          if (member.role === WorkspaceRole.ADMIN) acc.push({\n            name: \"Workspace automatically quarantined\" as const,\n            userId: member.user.id,\n            workspaceId: workspace.id,\n            data: {\n              reason: \"auto upgrade payment failed\" as const\n            }\n          });\n          return acc;\n        }, []));\n      }\n    } else {\n      await reportUsageToStripe(totalChatsUsed, {\n        stripe,\n        subscription\n      });\n      if (workspace.plan === \"PRO\") {\n        const isSuspicious = await isSuspiciousWorkspace(subscription, totalChatsUsed, workspace.plan, {\n          stripe\n        });\n        if (isSuspicious) {\n          console.log(`Suspicious Pro workspace ${workspace.id} collected ${totalChatsUsed} chats, resetting billing cycle...`);\n          const adminMembers = workspace.members.filter(member => member.role === WorkspaceRole.ADMIN);\n          const adminEmails = adminMembers.map(member => member.user.email).filter(isDefined);\n          try {\n            await chargeAndResetBillingCycle(subscription, {\n              stripe\n            });\n            console.log(`Successfully reset billing cycle for workspace ${workspace.id}`);\n            await sendBillingCycleResetEmail({\n              to: adminEmails,\n              workspaceName: workspace.name,\n              totalChatsUsed,\n              url: `${process.env.NEXTAUTH_URL}/typebots?workspaceId=${workspace.id}`\n            });\n            billingCycleResetEvents.push(...adminMembers.map(member => ({\n              name: \"Billing cycle reset\" as const,\n              userId: member.user.id,\n              workspaceId: workspace.id\n            })));\n            continue;\n          } catch (error) {\n            console.error(`Failed to reset billing cycle for workspace ${workspace.id}, quarantining...`, error);\n            await sendBillingCycleResetFailedEmail({\n              to: adminEmails,\n              workspaceName: workspace.name,\n              totalChatsUsed\n            });\n            await prisma.workspace.updateMany({\n              where: {\n                id: workspace.id\n              },\n              data: {\n                isQuarantined: true\n              }\n            });\n            quarantineEvents.push(...adminMembers.map(member => ({\n              name: \"Workspace automatically quarantined\" as const,\n              userId: member.user.id,\n              workspaceId: workspace.id,\n              data: {\n                reason: \"suspicious billing cycle reset payment failed\" as const\n              }\n            })));\n          }\n        }\n      }\n    }\n  }\n  if (isDefined(enforcedChatsLimit) && (workspace.plan === Plan.FREE && totalChatsUsed > enforcedChatsLimit * 1.5 || isDefined(workspace.chatsHardLimit) && totalChatsUsed >= workspace.chatsHardLimit)) {\n    console.log(`Automatically quarantine workspace ${workspace.id}...`);\n    await prisma.workspace.updateMany({\n      where: {\n        id: workspace.id\n      },\n      data: {\n        isQuarantined: true\n      }\n    });\n    quarantineEvents.push(...workspace.members.reduce((acc, member) => {\n      if (member.role === WorkspaceRole.ADMIN) acc.push({\n        name: \"Workspace automatically quarantined\" as const,\n        userId: member.user.id,\n        workspaceId: workspace.id,\n        data: {\n          totalChatsUsed,\n          chatsLimit: enforcedChatsLimit,\n          reason: \"free limit reached\" as const\n        }\n      });\n      return acc;\n    }, []));\n  }\n}"
    },
    {
     "type": "batch-query-before-loop",
     "rank": 2,
     "fitnessScore": 52.05,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: workspace, workspaces",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 13 async call(s) inside loop over workspaces\n// Problem: N+1 queries where N = workspaces.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = workspaces.map(workspace => workspace.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const workspace of workspaces) {\n    if (workspace.isQuarantined) continue;\n    const subscription = await getSubscription(workspace, { stripe });\n    const { totalChatsUsed, usagePeriodStart } = await getUsage({\n      workspaceId: workspace.id,\n      subscription,\n    });\n    const isEnterpriseUsageBasedSubscription =\n      workspace.plan === Plan.ENTERPRISE &&\n      isDefined(subscription && findMeteredSubscriptionItem(subscription));\n    const chatsLimit = isEnterpriseUsageBasedSubscription\n      ? \"inf\"\n      : getChatsLimit(workspace);\n    const enforcedChatsLimit =\n      workspace.chatsHardLimit ??\n      (chatsLimit === \"inf\" ? undefined : chatsLimit);\n\n    if (isDefined(enforcedChatsLimit))\n      limitWarningEmailEvents.push(\n        ...(await sendLimitWarningEmails({\n          chatsLimit: enforcedChatsLimit,\n          totalChatsUsed,\n          usagePeriodStart,\n          workspace,\n        })),\n      );\n\n    const isUsageBasedSubscription =\n      isEnterpriseUsageBasedSubscription ||\n      isDefined(\n        subscription?.items.data.find(\n          (item) =>\n            item.price.id === env.STRIPE_STARTER_PRICE_ID ||\n            item.price.id === env.STRIPE_PRO_PRICE_ID,\n        ),\n      );\n\n    if (\n      isUsageBasedSubscription &&\n      subscription &&\n      (workspace.plan === \"STARTER\" ||\n        workspace.plan === \"PRO\" ||\n        workspace.plan === \"ENTERPRISE\")\n    ) {\n      if (workspace.plan === \"STARTER\" && totalChatsUsed >= 4000) {\n        console.log(\n          \"Workspace has more than 4000 chats, automatically upgrading to PRO plan\",\n        );\n        const autoUpgradeResult = await autoUpgradeToPro(subscription, {\n          stripe,\n          workspaceId: workspace.id,\n        });\n        if (autoUpgradeResult.status === \"success\") {\n          autoUpgradeEvents.push(\n            ...workspace.members\n              .filter((member) => member.role === WorkspaceRole.ADMIN)\n              .map((member) => ({\n                name: \"Subscription automatically updated\" as const,\n                userId: member.user.id,\n                workspaceId: workspace.id,\n                data: {\n                  plan: Plan.PRO,\n                },\n              })),\n          );\n          await reportUsageToStripe(totalChatsUsed, {\n            stripe,\n            subscription: autoUpgradeResult.newSubscription,\n          });\n        } else if (\n          autoUpgradeResult.status === \"error\" &&\n          autoUpgradeResult.reason === \"payment_required\"\n        ) {\n          console.log(\n            `Workspace ${workspace.id} has more than 4000 chats, but payment is required to upgrade to PRO plan, automatically quarantining workspace...`,\n          );\n          await prisma.workspace.updateMany({\n            where: { id: workspace.id },\n            data: { isQuarantined: true },\n          });\n          quarantineEvents.push(\n            ...workspace.members\n              .filter((member) => member.role === WorkspaceRole.ADMIN)\n              .map((member) => ({\n                name: \"Workspace automatically quarantined\" as const,\n                userId: member.user.id,\n                workspaceId: workspace.id,\n                data: {\n                  reason: \"auto upgrade payment failed\" as const,\n                },\n              })),\n          );\n        }\n      } else {\n        await reportUsageToStripe(totalChatsUsed, { stripe, subscription });\n\n        if (workspace.plan === \"PRO\") {\n          const isSuspicious = await isSuspiciousWorkspace(\n            subscription,\n            totalChatsUsed,\n            workspace.plan,\n            { stripe },\n          );\n\n          if (isSuspicious) {\n            console.log(\n              `Suspicious Pro workspace ${workspace.id} collected ${totalChatsUsed} chats, resetting billing cycle...`,\n            );\n            const adminMembers = workspace.members.filter(\n              (member) => member.role === WorkspaceRole.ADMIN,\n            );\n            const adminEmails = adminMembers\n              .map((member) => member.user.email)\n              .filter(isDefined);\n\n            try {\n              await chargeAndResetBillingCycle(subscription, { stripe });\n              console.log(\n                `Successfully reset billing cycle for workspace ${workspace.id}`,\n              );\n              await sendBillingCycleResetEmail({\n                to: adminEmails,\n                workspaceName: workspace.name,\n                totalChatsUsed,\n                url: `${process.env.NEXTAUTH_URL}/typebots?workspaceId=${workspace.id}`,\n              });\n              billingCycleResetEvents.push(\n                ...adminMembers.map((member) => ({\n                  name: \"Billing cycle reset\" as const,\n                  userId: member.user.id,\n                  workspaceId: workspace.id,\n                })),\n              );\n              continue;\n            } catch (error) {\n              console.error(\n                `Failed to reset billing cycle for workspace ${workspace.id}, quarantining...`,\n                error,\n              );\n              await sendBillingCycleResetFailedEmail({\n                to: adminEmails,\n                workspaceName: workspace.name,\n                totalChatsUsed,\n              });\n              await prisma.workspace.updateMany({\n                where: { id: workspace.id },\n                data: { isQuarantined: true },\n              });\n              quarantineEvents.push(\n                ...adminMembers.map((member) => ({\n                  name: \"Workspace automatically quarantined\" as const,\n                  userId: member.user.id,\n                  workspaceId: workspace.id,\n                  data: {\n                    reason:\n                      \"suspicious billing cycle reset payment failed\" as const,\n                  },\n                })),\n              );\n            }\n          }\n        }\n      }\n    }\n\n    if (\n      isDefined(enforcedChatsLimit) &&\n      ((workspace.plan === Plan.FREE &&\n        totalChatsUsed > enforcedChatsLimit * 1.5) ||\n        (isDefined(workspace.chatsHardLimit) &&\n          totalChatsUsed >= workspace.chatsHardLimit))\n    ) {\n      console.log(`Automatically quarantine workspace ${workspace.id}...`);\n      await prisma.workspace.updateMany({\n        where: { id: workspace.id },\n        data: { isQuarantined: true },\n      });\n      quarantineEvents.push(\n        ...workspace.members\n          .filter((member) => member.role === WorkspaceRole.ADMIN)\n          .map((member) => ({\n            name: \"Workspace automatically quarantined\" as const,\n            userId: member.user.id,\n            workspaceId: workspace.id,\n            data: {\n              totalChatsUsed,\n              chatsLimit: enforcedChatsLimit,\n              reason: \"free limit reached\" as const,\n            },\n          })),\n      );\n    }\n  }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "memoization",
     "rank": 3,
     "fitnessScore": 49.15,
     "reasoning": "Add memoization cache for repeated expensive calls\nPreserved: prisma.workspace.updateMany",
     "code": "// OPTIMIZED: Memoization for repeated expensive calls\n// Found 1 repeated async call(s)\n\n// Memoize prisma.workspace.updateMany (called 3x)\nconst updateManyCache = new Map();\nasync function memoizedUpdateMany(key) {\n  if (updateManyCache.has(key)) return updateManyCache.get(key);\n  const result = await prisma.workspace.updateMany(key);\n  updateManyCache.set(key, result);\n  return result;\n}\n\n// Original code (replace calls with memoized versions):\nfor (const workspace of workspaces) {\n    if (workspace.isQuarantined) continue;\n    const subscription = await getSubscription(workspace, { stripe });\n    const { totalChatsUsed, usagePeriodStart } = await getUsage({\n      workspaceId: workspace.id,\n      subscription,\n    });\n    const isEnterpriseUsageBasedSubscription =\n      workspace.plan === Plan.ENTERPRISE &&\n      isDefined(subscription && findMeteredSubscriptionItem(subscription));\n    const chatsLimit = isEnterpriseUsageBasedSubscription\n      ? \"inf\"\n      : getChatsLimit(workspace);\n    const enforcedChatsLimit =\n      workspace.chatsHardLimit ??\n      (chatsLimit === \"inf\" ? undefined : chatsLimit);\n\n    if (isDefined(enforcedChatsLimit))\n      limitWarningEmailEvents.push(\n        ...(await sendLimitWarningEmails({\n          chatsLimit: enforcedChatsLimit,\n          totalChatsUsed,\n          usagePeriodStart,\n          workspace,\n        })),\n      );\n\n    const isUsageBasedSubscription =\n      isEnterpriseUsageBasedSubscription ||\n      isDefined(\n        subscription?.items.data.find(\n          (item) =>\n            item.price.id === env.STRIPE_STARTER_PRICE_ID ||\n            item.price.id === env.STRIPE_PRO_PRICE_ID,\n        ),\n      );\n\n    if (\n      isUsageBasedSubscription &&\n      subscription &&\n      (workspace.plan === \"STARTER\" ||\n        workspace.plan === \"PRO\" ||\n        workspace.plan === \"ENTERPRISE\")\n    ) {\n      if (workspace.plan === \"STARTER\" && totalChatsUsed >= 4000) {\n        console.log(\n          \"Workspace has more than 4000 chats, automatically upgrading to PRO plan\",\n        );\n        const autoUpgradeResult = await autoUpgradeToPro(subscription, {\n          stripe,\n          workspaceId: workspace.id,\n        });\n        if (autoUpgradeResult.status === \"success\") {\n          autoUpgradeEvents.push(\n            ...workspace.members\n              .filter((member) => member.role === WorkspaceRole.ADMIN)\n              .map((member) => ({\n                name: \"Subscription automatically updated\" as const,\n                userId: member.user.id,\n                workspaceId: workspace.id,\n                data: {\n                  plan: Plan.PRO,\n                },\n              })),\n          );\n          await reportUsageToStripe(totalChatsUsed, {\n            stripe,\n            subscription: autoUpgradeResult.newSubscription,\n          });\n        } else if (\n          autoUpgradeResult.status === \"error\" &&\n          autoUpgradeResult.reason === \"payment_required\"\n        ) {\n          console.log(\n            `Workspace ${workspace.id} has more than 4000 chats, but payment is required to upgrade to PRO plan, automatically quarantining workspace...`,\n          );\n          await prisma.workspace.updateMany({\n            where: { id: workspace.id },\n            data: { isQuarantined: true },\n          });\n          quarantineEvents.push(\n            ...workspace.members\n              .filter((member) => member.role === WorkspaceRole.ADMIN)\n              .map((member) => ({\n                name: \"Workspace automatically quarantined\" as const,\n                userId: member.user.id,\n                workspaceId: workspace.id,\n                data: {\n                  reason: \"auto upgrade payment failed\" as const,\n                },\n              })),\n          );\n        }\n      } else {\n        await reportUsageToStripe(totalChatsUsed, { stripe, subscription });\n\n        if (workspace.plan === \"PRO\") {\n          const isSuspicious = await isSuspiciousWorkspace(\n            subscription,\n            totalChatsUsed,\n            workspace.plan,\n            { stripe },\n          );\n\n          if (isSuspicious) {\n            console.log(\n              `Suspicious Pro workspace ${workspace.id} collected ${totalChatsUsed} chats, resetting billing cycle...`,\n            );\n            const adminMembers = workspace.members.filter(\n              (member) => member.role === WorkspaceRole.ADMIN,\n            );\n            const adminEmails = adminMembers\n              .map((member) => member.user.email)\n              .filter(isDefined);\n\n            try {\n              await chargeAndResetBillingCycle(subscription, { stripe });\n              console.log(\n                `Successfully reset billing cycle for workspace ${workspace.id}`,\n              );\n              await sendBillingCycleResetEmail({\n                to: adminEmails,\n                workspaceName: workspace.name,\n                totalChatsUsed,\n                url: `${process.env.NEXTAUTH_URL}/typebots?workspaceId=${workspace.id}`,\n              });\n              billingCycleResetEvents.push(\n                ...adminMembers.map((member) => ({\n                  name: \"Billing cycle reset\" as const,\n                  userId: member.user.id,\n                  workspaceId: workspace.id,\n                })),\n              );\n              continue;\n            } catch (error) {\n              console.error(\n                `Failed to reset billing cycle for workspace ${workspace.id}, quarantining...`,\n                error,\n              );\n              await sendBillingCycleResetFailedEmail({\n                to: adminEmails,\n                workspaceName: workspace.name,\n                totalChatsUsed,\n              });\n              await prisma.workspace.updateMany({\n                where: { id: workspace.id },\n                data: { isQuarantined: true },\n              });\n              quarantineEvents.push(\n                ...adminMembers.map((member) => ({\n                  name: \"Workspace automatically quarantined\" as const,\n                  userId: member.user.id,\n                  workspaceId: workspace.id,\n                  data: {\n                    reason:\n                      \"suspicious billing cycle reset payment failed\" as const,\n                  },\n                })),\n              );\n            }\n          }\n        }\n      }\n    }\n\n    if (\n      isDefined(enforcedChatsLimit) &&\n      ((workspace.plan === Plan.FREE &&\n        totalChatsUsed > enforcedChatsLimit * 1.5) ||\n        (isDefined(workspace.chatsHardLimit) &&\n          totalChatsUsed >= workspace.chatsHardLimit))\n    ) {\n      console.log(`Automatically quarantine workspace ${workspace.id}...`);\n      await prisma.workspace.updateMany({\n        where: { id: workspace.id },\n        data: { isQuarantined: true },\n      });\n      quarantineEvents.push(\n        ...workspace.members\n          .filter((member) => member.role === WorkspaceRole.ADMIN)\n          .map((member) => ({\n            name: \"Workspace automatically quarantined\" as const,\n            userId: member.user.id,\n            workspaceId: workspace.id,\n            data: {\n              totalChatsUsed,\n              chatsLimit: enforcedChatsLimit,\n              reason: \"free limit reached\" as const,\n            },\n          })),\n      );\n    }\n  }"
    },
    {
     "type": "prisma-include",
     "rank": 4,
     "fitnessScore": 46.9,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: workspace",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for workspace and related data\n// Optimized: Single query with include\n\nconst workspaceWithRelations = await prisma.workspace.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const workspace of workspaces) {\n    if (workspace.isQuarantined) continue;\n    const subscription = await getSubscription(workspace, { stripe });\n    const { totalChatsUsed, usagePeriodStart } = await getUsage({\n      workspaceId: workspace.id,\n      subscription,\n    });\n    const isEnterpriseUsageBasedSubscription =\n      workspace.plan === Plan.ENTERPRISE &&\n      isDefined(subscription && findMeteredSubscriptionItem(subscription));\n    const chatsLimit = isEnterpriseUsageBasedSubscription\n      ? \"inf\"\n      : getChatsLimit(workspace);\n    const enforcedChatsLimit =\n      workspace.chatsHardLimit ??\n      (chatsLimit === \"inf\" ? undefined : chatsLimit);\n\n    if (isDefined(enforcedChatsLimit))\n      limitWarningEmailEvents.push(\n        ...(await sendLimitWarningEmails({\n          chatsLimit: enforcedChatsLimit,\n          totalChatsUsed,\n          usagePeriodStart,\n          workspace,\n        })),\n      );\n\n    const isUsageBasedSubscription =\n      isEnterpriseUsageBasedSubscription ||\n      isDefined(\n        subscription?.items.data.find(\n          (item) =>\n            item.price.id === env.STRIPE_STARTER_PRICE_ID ||\n            item.price.id === env.STRIPE_PRO_PRICE_ID,\n        ),\n      );\n\n    if (\n      isUsageBasedSubscription &&\n      subscription &&\n      (workspace.plan === \"STARTER\" ||\n        workspace.plan === \"PRO\" ||\n        workspace.plan === \"ENTERPRISE\")\n    ) {\n      if (workspace.plan === \"STARTER\" && totalChatsUsed >= 4000) {\n        console.log(\n          \"Workspace has more than 4000 chats, automatically upgrading to PRO plan\",\n        );\n        const autoUpgradeResult = await autoUpgradeToPro(subscription, {\n          stripe,\n          workspaceId: workspace.id,\n        });\n        if (autoUpgradeResult.status === \"success\") {\n          autoUpgradeEvents.push(\n            ...workspace.members\n              .filter((member) => member.role === WorkspaceRole.ADMIN)\n              .map((member) => ({\n                name: \"Subscription automatically updated\" as const,\n                userId: member.user.id,\n                workspaceId: workspace.id,\n                data: {\n                  plan: Plan.PRO,\n                },\n              })),\n          );\n          await reportUsageToStripe(totalChatsUsed, {\n            stripe,\n            subscription: autoUpgradeResult.newSubscription,\n          });\n        } else if (\n          autoUpgradeResult.status === \"error\" &&\n          autoUpgradeResult.reason === \"payment_required\"\n        ) {\n          console.log(\n            `Workspace ${workspace.id} has more than 4000 chats, but payment is required to upgrade to PRO plan, automatically quarantining workspace...`,\n          );\n          await prisma.workspace.updateMany({\n            where: { id: workspace.id },\n            data: { isQuarantined: true },\n          });\n          quarantineEvents.push(\n            ...workspace.members\n              .filter((member) => member.role === WorkspaceRole.ADMIN)\n              .map((member) => ({\n                name: \"Workspace automatically quarantined\" as const,\n                userId: member.user.id,\n                workspaceId: workspace.id,\n                data: {\n                  reason: \"auto upgrade payment failed\" as const,\n                },\n              })),\n          );\n        }\n      } else {\n        await reportUsageToStripe(totalChatsUsed, { stripe, subscription });\n\n        if (workspace.plan === \"PRO\") {\n          const isSuspicious = await isSuspiciousWorkspace(\n            subscription,\n            totalChatsUsed,\n            workspace.plan,\n            { stripe },\n          );\n\n          if (isSuspicious) {\n            console.log(\n              `Suspicious Pro workspace ${workspace.id} collected ${totalChatsUsed} chats, resetting billing cycle...`,\n            );\n            const adminMembers = workspace.members.filter(\n              (member) => member.role === WorkspaceRole.ADMIN,\n            );\n            const adminEmails = adminMembers\n              .map((member) => member.user.email)\n              .filter(isDefined);\n\n            try {\n              await chargeAndResetBillingCycle(subscription, { stripe });\n              console.log(\n                `Successfully reset billing cycle for workspace ${workspace.id}`,\n              );\n              await sendBillingCycleResetEmail({\n                to: adminEmails,\n                workspaceName: workspace.name,\n                totalChatsUsed,\n                url: `${process.env.NEXTAUTH_URL}/typebots?workspaceId=${workspace.id}`,\n              });\n              billingCycleResetEvents.push(\n                ...adminMembers.map((member) => ({\n                  name: \"Billing cycle reset\" as const,\n                  userId: member.user.id,\n                  workspaceId: workspace.id,\n                })),\n              );\n              continue;\n            } catch (error) {\n              console.error(\n                `Failed to reset billing cycle for workspace ${workspace.id}, quarantining...`,\n                error,\n              );\n              await sendBillingCycleResetFailedEmail({\n                to: adminEmails,\n                workspaceName: workspace.name,\n                totalChatsUsed,\n              });\n              await prisma.workspace.updateMany({\n                where: { id: workspace.id },\n                data: { isQuarantined: true },\n              });\n              quarantineEvents.push(\n                ...adminMembers.map((member) => ({\n                  name: \"Workspace automatically quarantined\" as const,\n                  userId: member.user.id,\n                  workspaceId: workspace.id,\n                  data: {\n                    reason:\n                      \"suspicious billing cycle reset payment failed\" as const,\n                  },\n                })),\n              );\n            }\n          }\n        }\n      }\n    }\n\n    if (\n      isDefined(enforcedChatsLimit) &&\n      ((workspace.plan === Plan.FREE &&\n        totalChatsUsed > enforcedChatsLimit * 1.5) ||\n        (isDefined(workspace.chatsHardLimit) &&\n          totalChatsUsed >= workspace.chatsHardLimit))\n    ) {\n      console.log(`Automatically quarantine workspace ${workspace.id}...`);\n      await prisma.workspace.updateMany({\n        where: { id: workspace.id },\n        data: { isQuarantined: true },\n      });\n      quarantineEvents.push(\n        ...workspace.members\n          .filter((member) => member.role === WorkspaceRole.ADMIN)\n          .map((member) => ({\n            name: \"Workspace automatically quarantined\" as const,\n            userId: member.user.id,\n            workspaceId: workspace.id,\n            data: {\n              totalChatsUsed,\n              chatsLimit: enforcedChatsLimit,\n              reason: \"free limit reached\" as const,\n            },\n          })),\n      );\n    }\n  }\n*/\n\n// Access related data directly: workspaceWithRelations[0].relation"
    },
    {
     "type": "memoize",
     "rank": 5,
     "fitnessScore": 46.65,
     "reasoning": "Added memoization for 1 repeated calls",
     "code": "const updateManyCache = new Map(); // Memoize prisma.workspace.updateMany\n\nfor (const workspace of workspaces) {\n    if (workspace.isQuarantined) continue;\n    const subscription = await getSubscription(workspace, { stripe });\n    const { totalChatsUsed, usagePeriodStart } = await getUsage({\n      workspaceId: workspace.id,\n      subscription,\n    });\n    const isEnterpriseUsageBasedSubscription =\n      workspace.plan === Plan.ENTERPRISE &&\n      isDefined(subscription && findMeteredSubscriptionItem(subscription));\n    const chatsLimit = isEnterpriseUsageBasedSubscription\n      ? \"inf\"\n      : getChatsLimit(workspace);\n    const enforcedChatsLimit =\n      workspace.chatsHardLimit ??\n      (chatsLimit === \"inf\" ? undefined : chatsLimit);\n\n    if (isDefined(enforcedChatsLimit))\n      limitWarningEmailEvents.push(\n        ...(await sendLimitWarningEmails({\n          chatsLimit: enforcedChatsLimit,\n          totalChatsUsed,\n          usagePeriodStart,\n          workspace,\n        })),\n      );\n\n    const isUsageBasedSubscription =\n      isEnterpriseUsageBasedSubscription ||\n      isDefined(\n        subscription?.items.data.find(\n          (item) =>\n            item.price.id === env.STRIPE_STARTER_PRICE_ID ||\n            item.price.id === env.STRIPE_PRO_PRICE_ID,\n        ),\n      );\n\n    if (\n      isUsageBasedSubscription &&\n      subscription &&\n      (workspace.plan === \"STARTER\" ||\n        workspace.plan === \"PRO\" ||\n        workspace.plan === \"ENTERPRISE\")\n    ) {\n      if (workspace.plan === \"STARTER\" && totalChatsUsed >= 4000) {\n        console.log(\n          \"Workspace has more than 4000 chats, automatically upgrading to PRO plan\",\n        );\n        const autoUpgradeResult = await autoUpgradeToPro(subscription, {\n          stripe,\n          workspaceId: workspace.id,\n        });\n        if (autoUpgradeResult.status === \"success\") {\n          autoUpgradeEvents.push(\n            ...workspace.members\n              .filter((member) => member.role === WorkspaceRole.ADMIN)\n              .map((member) => ({\n                name: \"Subscription automatically updated\" as const,\n                userId: member.user.id,\n                workspaceId: workspace.id,\n                data: {\n                  plan: Plan.PRO,\n                },\n              })),\n          );\n          await reportUsageToStripe(totalChatsUsed, {\n            stripe,\n            subscription: autoUpgradeResult.newSubscription,\n          });\n        } else if (\n          autoUpgradeResult.status === \"error\" &&\n          autoUpgradeResult.reason === \"payment_required\"\n        ) {\n          console.log(\n            `Workspace ${workspace.id} has more than 4000 chats, but payment is required to upgrade to PRO plan, automatically quarantining workspace...`,\n          );\n          await prisma.workspace.updateMany({\n            where: { id: workspace.id },\n            data: { isQuarantined: true },\n          });\n          quarantineEvents.push(\n            ...workspace.members\n              .filter((member) => member.role === WorkspaceRole.ADMIN)\n              .map((member) => ({\n                name: \"Workspace automatically quarantined\" as const,\n                userId: member.user.id,\n                workspaceId: workspace.id,\n                data: {\n                  reason: \"auto upgrade payment failed\" as const,\n                },\n              })),\n          );\n        }\n      } else {\n        await reportUsageToStripe(totalChatsUsed, { stripe, subscription });\n\n        if (workspace.plan === \"PRO\") {\n          const isSuspicious = await isSuspiciousWorkspace(\n            subscription,\n            totalChatsUsed,\n            workspace.plan,\n            { stripe },\n          );\n\n          if (isSuspicious) {\n            console.log(\n              `Suspicious Pro workspace ${workspace.id} collected ${totalChatsUsed} chats, resetting billing cycle...`,\n            );\n            const adminMembers = workspace.members.filter(\n              (member) => member.role === WorkspaceRole.ADMIN,\n            );\n            const adminEmails = adminMembers\n              .map((member) => member.user.email)\n              .filter(isDefined);\n\n            try {\n              await chargeAndResetBillingCycle(subscription, { stripe });\n              console.log(\n                `Successfully reset billing cycle for workspace ${workspace.id}`,\n              );\n              await sendBillingCycleResetEmail({\n                to: adminEmails,\n                workspaceName: workspace.name,\n                totalChatsUsed,\n                url: `${process.env.NEXTAUTH_URL}/typebots?workspaceId=${workspace.id}`,\n              });\n              billingCycleResetEvents.push(\n                ...adminMembers.map((member) => ({\n                  name: \"Billing cycle reset\" as const,\n                  userId: member.user.id,\n                  workspaceId: workspace.id,\n                })),\n              );\n              continue;\n            } catch (error) {\n              console.error(\n                `Failed to reset billing cycle for workspace ${workspace.id}, quarantining...`,\n                error,\n              );\n              await sendBillingCycleResetFailedEmail({\n                to: adminEmails,\n                workspaceName: workspace.name,\n                totalChatsUsed,\n              });\n              await prisma.workspace.updateMany({\n                where: { id: workspace.id },\n                data: { isQuarantined: true },\n              });\n              quarantineEvents.push(\n                ...adminMembers.map((member) => ({\n                  name: \"Workspace automatically quarantined\" as const,\n                  userId: member.user.id,\n                  workspaceId: workspace.id,\n                  data: {\n                    reason:\n                      \"suspicious billing cycle reset payment failed\" as const,\n                  },\n                })),\n              );\n            }\n          }\n        }\n      }\n    }\n\n    if (\n      isDefined(enforcedChatsLimit) &&\n      ((workspace.plan === Plan.FREE &&\n        totalChatsUsed > enforcedChatsLimit * 1.5) ||\n        (isDefined(workspace.chatsHardLimit) &&\n          totalChatsUsed >= workspace.chatsHardLimit))\n    ) {\n      console.log(`Automatically quarantine workspace ${workspace.id}...`);\n      await prisma.workspace.updateMany({\n        where: { id: workspace.id },\n        data: { isQuarantined: true },\n      });\n      quarantineEvents.push(\n        ...workspace.members\n          .filter((member) => member.role === WorkspaceRole.ADMIN)\n          .map((member) => ({\n            name: \"Workspace automatically quarantined\" as const,\n            userId: member.user.id,\n            workspaceId: workspace.id,\n            data: {\n              totalChatsUsed,\n              chatsLimit: enforcedChatsLimit,\n              reason: \"free limit reached\" as const,\n            },\n          })),\n      );\n    }\n  }"
    }
   ]
  },
  {
   "file": "packages/scripts/src/churnAgent/getYesterdayChurnSummary.ts",
   "line": 128,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.findUnique()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const [index, row] of churnedWorkspacesResponse.results.entries()) {\n    const workspaceId = row[0] as string;\n    console.log(\n      `\\n🏢 Processing workspace ${index + 1}/${totalChurnedWorkspaces}: ${workspaceId}`,\n    );\n\n    const workspace = await prisma.workspace.findUnique({\n      where: {\n        id: workspaceId,\n      },\n      select: {\n        id: true,\n        name: true,\n        stripeId: true,\n        createdAt: true,\n        plan: true,\n        members: {\n          select: {\n            role: true,\n            user: {\n              select: {\n                id: true,\n                email: true,\n              },\n            },\n          },\n        },\n        typebots: {\n          select: {\n            version: true,\n            id: true,\n            name: true,\n            createdAt: true,\n            updatedAt: true,\n            groups: true,\n          },\n        },\n      },\n    });\n\n    if (!workspace) {\n      console.error(`❌ Workspace not found: ${workspaceId}`);\n      continue;\n    }\n\n    console.log(`   📝 Workspace: ${workspace.name}`);\n    console.log(`   🤖 Typebots: ${workspace.typebots.length}`);\n\n    const cancellationReason = workspace.stripeId\n      ? await getSubscriptionCancellationDetails(workspace.stripeId)\n      : null;\n\n    if (workspace.stripeId && !cancellationReason) {\n      console.log(\"   ❌ Cancellation was most likely unscheduled\");\n      continue;\n    }\n\n    const subscriptionDetails = {\n      plan: workspace.plan,\n      total_paid: cancellationReason?.totalPaid,\n      scheduled_cancellation_date: yesterday,\n      cancel_at: cancellationReason?.cancelAt?.toISOString().split(\"T\")[0],\n      cancellation_reason: cancellationReason?.feedback\n        ? {\n            stripeQuickPickReason: cancellationReason.feedback,\n            comment: cancellationReason.comment,\n          }\n        : undefined,\n    };\n\n    const workspaceData = await getWorkspaceJsonRepresentation(workspace);\n\n    const userMessage = JSON.stringify(\n      {\n        subscription: subscriptionDetails,\n        ...workspaceData,\n      },\n      null,\n      2,\n    );\n\n    const workspacePromptPath = path.join(\n      __dirname,\n      `../../logs/workspaces/${workspace.id}/prompt.txt`,\n    );\n    createFolderIfNotExists(workspacePromptPath);\n    writeFileSync(workspacePromptPath, userMessage);\n\n    console.log(\"   🧠 Generating churn report...\");\n    const { object: churnSummary } = await generateObject({\n      model: openai(\"gpt-5\"),\n      schema: zodToSchema(churnSummarySchema),\n      system: mainAgentSystemPrompt,\n      messages: [\n        {\n          role: \"user\",\n          content: userMessage,\n        },\n      ],\n    });\n\n    console.log(\"   ✅ Success\");\n    const workspaceSummaryPath = path.join(\n      __dirname,\n      `../../logs/workspaces/${workspace.id}/summary.txt`,\n    );\n    createFolderIfNotExists(workspaceSummaryPath);\n    writeFileSync(workspaceSummaryPath, JSON.stringify(churnSummary, null, 2));\n    const fullSummary = {\n      ...churnSummary,\n      workspace: {\n        id: workspace.id,\n        name: workspace.name,\n        plan: workspace.plan,\n        totalSpent: subscriptionDetails.total_paid,\n        createdAt: workspace.createdAt.toLocaleDateString(),\n        stripeId: workspace.stripeId,\n        countryEmoji: cancellationReason?.countryEmoji,\n      },\n    };\n    await onSummaryGenerated(fullSummary);\n    summaries.push(fullSummary);\n  }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 77,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: , churnedWorkspacesResponse.results.entries()",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 5 async call(s) inside loop over churnedWorkspacesResponse.results.entries()\n// Problem: N+1 queries where N = churnedWorkspacesResponse.results.entries().length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = churnedWorkspacesResponse.results.entries().map( => .id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const [index, row] of churnedWorkspacesResponse.results.entries()) {\n    const workspaceId = row[0] as string;\n    console.log(\n      `\\n🏢 Processing workspace ${index + 1}/${totalChurnedWorkspaces}: ${workspaceId}`,\n    );\n\n    const workspace = await prisma.workspace.findUnique({\n      where: {\n        id: workspaceId,\n      },\n      select: {\n        id: true,\n        name: true,\n        stripeId: true,\n        createdAt: true,\n        plan: true,\n        members: {\n          select: {\n            role: true,\n            user: {\n              select: {\n                id: true,\n                email: true,\n              },\n            },\n          },\n        },\n        typebots: {\n          select: {\n            version: true,\n            id: true,\n            name: true,\n            createdAt: true,\n            updatedAt: true,\n            groups: true,\n          },\n        },\n      },\n    });\n\n    if (!workspace) {\n      console.error(`❌ Workspace not found: ${workspaceId}`);\n      continue;\n    }\n\n    console.log(`   📝 Workspace: ${workspace.name}`);\n    console.log(`   🤖 Typebots: ${workspace.typebots.length}`);\n\n    const cancellationReason = workspace.stripeId\n      ? await getSubscriptionCancellationDetails(workspace.stripeId)\n      : null;\n\n    if (workspace.stripeId && !cancellationReason) {\n      console.log(\"   ❌ Cancellation was most likely unscheduled\");\n      continue;\n    }\n\n    const subscriptionDetails = {\n      plan: workspace.plan,\n      total_paid: cancellationReason?.totalPaid,\n      scheduled_cancellation_date: yesterday,\n      cancel_at: cancellationReason?.cancelAt?.toISOString().split(\"T\")[0],\n      cancellation_reason: cancellationReason?.feedback\n        ? {\n            stripeQuickPickReason: cancellationReason.feedback,\n            comment: cancellationReason.comment,\n          }\n        : undefined,\n    };\n\n    const workspaceData = await getWorkspaceJsonRepresentation(workspace);\n\n    const userMessage = JSON.stringify(\n      {\n        subscription: subscriptionDetails,\n        ...workspaceData,\n      },\n      null,\n      2,\n    );\n\n    const workspacePromptPath = path.join(\n      __dirname,\n      `../../logs/workspaces/${workspace.id}/prompt.txt`,\n    );\n    createFolderIfNotExists(workspacePromptPath);\n    writeFileSync(workspacePromptPath, userMessage);\n\n    console.log(\"   🧠 Generating churn report...\");\n    const { object: churnSummary } = await generateObject({\n      model: openai(\"gpt-5\"),\n      schema: zodToSchema(churnSummarySchema),\n      system: mainAgentSystemPrompt,\n      messages: [\n        {\n          role: \"user\",\n          content: userMessage,\n        },\n      ],\n    });\n\n    console.log(\"   ✅ Success\");\n    const workspaceSummaryPath = path.join(\n      __dirname,\n      `../../logs/workspaces/${workspace.id}/summary.txt`,\n    );\n    createFolderIfNotExists(workspaceSummaryPath);\n    writeFileSync(workspaceSummaryPath, JSON.stringify(churnSummary, null, 2));\n    const fullSummary = {\n      ...churnSummary,\n      workspace: {\n        id: workspace.id,\n        name: workspace.name,\n        plan: workspace.plan,\n        totalSpent: subscriptionDetails.total_paid,\n        createdAt: workspace.createdAt.toLocaleDateString(),\n        stripeId: workspace.stripeId,\n        countryEmoji: cancellationReason?.countryEmoji,\n      },\n    };\n    await onSummaryGenerated(fullSummary);\n    summaries.push(fullSummary);\n  }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 69.69999999999999,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: workspace",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for workspace and related data\n// Optimized: Single query with include\n\nconst workspaceWithRelations = await prisma.workspace.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const [index, row] of churnedWorkspacesResponse.results.entries()) {\n    const workspaceId = row[0] as string;\n    console.log(\n      `\\n🏢 Processing workspace ${index + 1}/${totalChurnedWorkspaces}: ${workspaceId}`,\n    );\n\n    const workspace = await prisma.workspace.findUnique({\n      where: {\n        id: workspaceId,\n      },\n      select: {\n        id: true,\n        name: true,\n        stripeId: true,\n        createdAt: true,\n        plan: true,\n        members: {\n          select: {\n            role: true,\n            user: {\n              select: {\n                id: true,\n                email: true,\n              },\n            },\n          },\n        },\n        typebots: {\n          select: {\n            version: true,\n            id: true,\n            name: true,\n            createdAt: true,\n            updatedAt: true,\n            groups: true,\n          },\n        },\n      },\n    });\n\n    if (!workspace) {\n      console.error(`❌ Workspace not found: ${workspaceId}`);\n      continue;\n    }\n\n    console.log(`   📝 Workspace: ${workspace.name}`);\n    console.log(`   🤖 Typebots: ${workspace.typebots.length}`);\n\n    const cancellationReason = workspace.stripeId\n      ? await getSubscriptionCancellationDetails(workspace.stripeId)\n      : null;\n\n    if (workspace.stripeId && !cancellationReason) {\n      console.log(\"   ❌ Cancellation was most likely unscheduled\");\n      continue;\n    }\n\n    const subscriptionDetails = {\n      plan: workspace.plan,\n      total_paid: cancellationReason?.totalPaid,\n      scheduled_cancellation_date: yesterday,\n      cancel_at: cancellationReason?.cancelAt?.toISOString().split(\"T\")[0],\n      cancellation_reason: cancellationReason?.feedback\n        ? {\n            stripeQuickPickReason: cancellationReason.feedback,\n            comment: cancellationReason.comment,\n          }\n        : undefined,\n    };\n\n    const workspaceData = await getWorkspaceJsonRepresentation(workspace);\n\n    const userMessage = JSON.stringify(\n      {\n        subscription: subscriptionDetails,\n        ...workspaceData,\n      },\n      null,\n      2,\n    );\n\n    const workspacePromptPath = path.join(\n      __dirname,\n      `../../logs/workspaces/${workspace.id}/prompt.txt`,\n    );\n    createFolderIfNotExists(workspacePromptPath);\n    writeFileSync(workspacePromptPath, userMessage);\n\n    console.log(\"   🧠 Generating churn report...\");\n    const { object: churnSummary } = await generateObject({\n      model: openai(\"gpt-5\"),\n      schema: zodToSchema(churnSummarySchema),\n      system: mainAgentSystemPrompt,\n      messages: [\n        {\n          role: \"user\",\n          content: userMessage,\n        },\n      ],\n    });\n\n    console.log(\"   ✅ Success\");\n    const workspaceSummaryPath = path.join(\n      __dirname,\n      `../../logs/workspaces/${workspace.id}/summary.txt`,\n    );\n    createFolderIfNotExists(workspaceSummaryPath);\n    writeFileSync(workspaceSummaryPath, JSON.stringify(churnSummary, null, 2));\n    const fullSummary = {\n      ...churnSummary,\n      workspace: {\n        id: workspace.id,\n        name: workspace.name,\n        plan: workspace.plan,\n        totalSpent: subscriptionDetails.total_paid,\n        createdAt: workspace.createdAt.toLocaleDateString(),\n        stripeId: workspace.stripeId,\n        countryEmoji: cancellationReason?.countryEmoji,\n      },\n    };\n    await onSummaryGenerated(fullSummary);\n    summaries.push(fullSummary);\n  }\n*/\n\n// Access related data directly: workspaceWithRelations[0].relation"
    }
   ]
  }
 ]
}