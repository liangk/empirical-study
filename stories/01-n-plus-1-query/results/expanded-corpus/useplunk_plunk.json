{
 "repository": "useplunk/plunk",
 "filesScanned": 212,
 "findings": [
  {
   "file": "apps/api/src/services/WorkflowService.ts",
   "line": 460,
   "severity": "medium",
   "description": "Found 1 database query (Database.create()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const step of source.steps) {\n        const created = await tx.workflowStep.create({\n          data: {\n            workflowId: newWorkflow.id,\n            type: step.type,\n            name: step.name,\n            position: step.position as Prisma.InputJsonValue,\n            config: step.config as Prisma.InputJsonValue,\n            templateId: step.templateId,\n          },\n        });\n        stepIdMap.set(step.id, created.id);\n      }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 88.1,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: step, source.steps",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over source.steps\n// Problem: N+1 queries where N = source.steps.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = source.steps.map(step => step.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const step of source.steps) {\n        const created = await tx.workflowStep.create({\n          data: {\n            workflowId: newWorkflow.id,\n            type: step.type,\n            name: step.name,\n            position: step.position as Prisma.InputJsonValue,\n            config: step.config as Prisma.InputJsonValue,\n            templateId: step.templateId,\n          },\n        });\n        stepIdMap.set(step.id, created.id);\n      }\n\n// Performance: 1 query instead of N queries"
    }
   ]
  },
  {
   "file": "apps/api/src/services/WorkflowService.ts",
   "line": 474,
   "severity": "medium",
   "description": "Found 1 database query (Database.create()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const transition of transitions) {\n        const fromStepId = stepIdMap.get(transition.fromStepId);\n        const toStepId = stepIdMap.get(transition.toStepId);\n        if (!fromStepId || !toStepId) continue;\n\n        await tx.workflowTransition.create({\n          data: {\n            fromStepId,\n            toStepId,\n            condition:\n              transition.condition === null\n                ? Prisma.JsonNull\n                : (transition.condition as Prisma.InputJsonValue),\n            priority: transition.priority,\n          },\n        });\n      }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 85.6,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: transition, transitions",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over transitions\n// Problem: N+1 queries where N = transitions.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = transitions.map(transition => transition.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const transition of transitions) {\n        const fromStepId = stepIdMap.get(transition.fromStepId);\n        const toStepId = stepIdMap.get(transition.toStepId);\n        if (!fromStepId || !toStepId) continue;\n\n        await tx.workflowTransition.create({\n          data: {\n            fromStepId,\n            toStepId,\n            condition:\n              transition.condition === null\n                ? Prisma.JsonNull\n                : (transition.condition as Prisma.InputJsonValue),\n            priority: transition.priority,\n          },\n        });\n      }\n\n// Performance: 1 query instead of N queries"
    }
   ]
  },
  {
   "file": "apps/api/src/services/WorkflowService.ts",
   "line": 814,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.update()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const incoming of step.incomingTransitions) {\n        await prisma.workflowTransition.update({\n          where: {id: incoming.id},\n          data: {toStepId: child.toStepId},\n        });\n      }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 88.1,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: incoming, step.incomingTransitions",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over step.incomingTransitions\n// Problem: N+1 queries where N = step.incomingTransitions.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = step.incomingTransitions.map(incoming => incoming.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const incoming of step.incomingTransitions) {\n        await prisma.workflowTransition.update({\n          where: {id: incoming.id},\n          data: {toStepId: child.toStepId},\n        });\n      }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 79.95,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: workflowTransition",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for workflowTransition and related data\n// Optimized: Single query with include\n\nconst workflowTransitionWithRelations = await prisma.workflowTransition.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const incoming of step.incomingTransitions) {\n        await prisma.workflowTransition.update({\n          where: {id: incoming.id},\n          data: {toStepId: child.toStepId},\n        });\n      }\n*/\n\n// Access related data directly: workflowTransitionWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/services/WorkflowExecutionService.ts",
   "line": 437,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.update()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const stepExecution of waitingExecutions) {\n      const config = stepExecution.step.config;\n\n      if (config && typeof config === 'object' && 'eventName' in config && config.eventName === eventName) {\n        // Event matches, resume execution\n        await prisma.workflowStepExecution.update({\n          where: {id: stepExecution.id},\n          data: {\n            status: StepExecutionStatus.COMPLETED,\n            completedAt: new Date(),\n            output: toPrismaJson({\n              eventName,\n              eventData: data ? toPrismaJson(data) : undefined,\n              receivedAt: new Date().toISOString(),\n            }),\n          },\n        });\n\n        // Cancel any pending timeout job\n        await QueueService.cancelWorkflowTimeout(stepExecution.id);\n\n        // Continue workflow\n        await this.processNextSteps(stepExecution.execution, stepExecution.step, {eventReceived: true});\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 84.35,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: stepExecution, waitingExecutions",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over waitingExecutions\n// Problem: N+1 queries where N = waitingExecutions.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = waitingExecutions.map(stepExecution => stepExecution.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const stepExecution of waitingExecutions) {\n      const config = stepExecution.step.config;\n\n      if (config && typeof config === 'object' && 'eventName' in config && config.eventName === eventName) {\n        // Event matches, resume execution\n        await prisma.workflowStepExecution.update({\n          where: {id: stepExecution.id},\n          data: {\n            status: StepExecutionStatus.COMPLETED,\n            completedAt: new Date(),\n            output: toPrismaJson({\n              eventName,\n              eventData: data ? toPrismaJson(data) : undefined,\n              receivedAt: new Date().toISOString(),\n            }),\n          },\n        });\n\n        // Cancel any pending timeout job\n        dataMap.get(stepExecution.id);\n\n        // Continue workflow\n        dataMap.get(stepExecution.id);\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 78.7,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: workflowStepExecution",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for workflowStepExecution and related data\n// Optimized: Single query with include\n\nconst workflowStepExecutionWithRelations = await prisma.workflowStepExecution.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const stepExecution of waitingExecutions) {\n      const config = stepExecution.step.config;\n\n      if (config && typeof config === 'object' && 'eventName' in config && config.eventName === eventName) {\n        // Event matches, resume execution\n        await prisma.workflowStepExecution.update({\n          where: {id: stepExecution.id},\n          data: {\n            status: StepExecutionStatus.COMPLETED,\n            completedAt: new Date(),\n            output: toPrismaJson({\n              eventName,\n              eventData: data ? toPrismaJson(data) : undefined,\n              receivedAt: new Date().toISOString(),\n            }),\n          },\n        });\n\n        // Cancel any pending timeout job\n        await QueueService.cancelWorkflowTimeout(stepExecution.id);\n\n        // Continue workflow\n        await this.processNextSteps(stepExecution.execution, stepExecution.step, {eventReceived: true});\n      }\n    }\n*/\n\n// Access related data directly: workflowStepExecutionWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/services/SegmentService.ts",
   "line": 303,
   "severity": "critical",
   "description": "Found 3 database queries (Prisma.count(), Prisma.count(), Prisma.update()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "batch.map(async segment => {\n          try {\n            let memberCount: number;\n\n            if (segment.type === 'STATIC') {\n              memberCount = await prisma.segmentMembership.count({\n                where: {segmentId: segment.id, exitedAt: null},\n              });\n            } else {\n              const condition = fromPrismaJson<FilterCondition>(segment.condition);\n              const where = await this.buildWhereClause(projectId, condition);\n              memberCount = await prisma.contact.count({where});\n            }\n\n            await prisma.segment.update({\n              where: {id: segment.id},\n              data: {memberCount},\n            });\n          } catch (error) {\n            signale.error(`Failed to update count for segment ${segment.id}:`, error);\n          }\n        })",
   "estimatedImpact": {
    "severityScore": 9,
    "description": "301 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 301,
     "queriesOptimal": 1,
     "performanceGain": "301x slower"
    }
   },
   "solutions": [
    {
     "type": "prisma-include",
     "rank": 1,
     "fitnessScore": 79.95,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: segmentMembership",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for segmentMembership and related data\n// Optimized: Single query with include\n\nconst segmentMembershipWithRelations = await prisma.segmentMembership.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nbatch.map(async segment => {\n          try {\n            let memberCount: number;\n\n            if (segment.type === 'STATIC') {\n              memberCount = await prisma.segmentMembership.count({\n                where: {segmentId: segment.id, exitedAt: null},\n              });\n            } else {\n              const condition = fromPrismaJson<FilterCondition>(segment.condition);\n              const where = await this.buildWhereClause(projectId, condition);\n              memberCount = await prisma.contact.count({where});\n            }\n\n            await prisma.segment.update({\n              where: {id: segment.id},\n              data: {memberCount},\n            });\n          } catch (error) {\n            signale.error(`Failed to update count for segment ${segment.id}:`, error);\n          }\n        })\n*/\n\n// Access related data directly: segmentMembershipWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/services/QueueService.ts",
   "line": 698,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.findUnique()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const job of scheduledCampaigns) {\n      // We need to check if the campaign belongs to this project\n      // by looking up the campaign in the database\n      const campaign = await prisma.campaign.findUnique({\n        where: {id: job.data.campaignId},\n        select: {projectId: true},\n      });\n\n      if (campaign?.projectId === projectId) {\n        await job.remove();\n        signale.info(`[QUEUE] Removed scheduled campaign job ${job.id}`);\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 89.5,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: job, scheduledCampaigns",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 2 async call(s) inside loop over scheduledCampaigns\n// Problem: N+1 queries where N = scheduledCampaigns.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = scheduledCampaigns.map(job => job.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const job of scheduledCampaigns) {\n      // We need to check if the campaign belongs to this project\n      // by looking up the campaign in the database\n      const campaign = await prisma.campaign.findUnique({\n        where: {id: job.data.campaignId},\n        select: {projectId: true},\n      });\n\n      if (campaign?.projectId === projectId) {\n        dataMap.get(job.id);\n        signale.info(`[QUEUE] Removed scheduled campaign job ${job.id}`);\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 81.69999999999999,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: campaign",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for campaign and related data\n// Optimized: Single query with include\n\nconst campaignWithRelations = await prisma.campaign.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const job of scheduledCampaigns) {\n      // We need to check if the campaign belongs to this project\n      // by looking up the campaign in the database\n      const campaign = await prisma.campaign.findUnique({\n        where: {id: job.data.campaignId},\n        select: {projectId: true},\n      });\n\n      if (campaign?.projectId === projectId) {\n        await job.remove();\n        signale.info(`[QUEUE] Removed scheduled campaign job ${job.id}`);\n      }\n    }\n*/\n\n// Access related data directly: campaignWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/services/QueueService.ts",
   "line": 714,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.findUnique()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const job of pendingEmails) {\n      const email = await prisma.email.findUnique({\n        where: {id: job.data.emailId},\n        select: {projectId: true},\n      });\n\n      if (email?.projectId === projectId) {\n        await job.remove();\n        signale.info(`[QUEUE] Removed email job ${job.id}`);\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 89.5,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: job, pendingEmails",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 2 async call(s) inside loop over pendingEmails\n// Problem: N+1 queries where N = pendingEmails.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = pendingEmails.map(job => job.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const job of pendingEmails) {\n      const email = await prisma.email.findUnique({\n        where: {id: job.data.emailId},\n        select: {projectId: true},\n      });\n\n      if (email?.projectId === projectId) {\n        dataMap.get(job.id);\n        signale.info(`[QUEUE] Removed email job ${job.id}`);\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 81.69999999999999,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: email",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for email and related data\n// Optimized: Single query with include\n\nconst emailWithRelations = await prisma.email.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const job of pendingEmails) {\n      const email = await prisma.email.findUnique({\n        where: {id: job.data.emailId},\n        select: {projectId: true},\n      });\n\n      if (email?.projectId === projectId) {\n        await job.remove();\n        signale.info(`[QUEUE] Removed email job ${job.id}`);\n      }\n    }\n*/\n\n// Access related data directly: emailWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/services/QueueService.ts",
   "line": 728,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.findUnique()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const job of campaignBatches) {\n      const campaign = await prisma.campaign.findUnique({\n        where: {id: job.data.campaignId},\n        select: {projectId: true},\n      });\n\n      if (campaign?.projectId === projectId) {\n        await job.remove();\n        signale.info(`[QUEUE] Removed campaign batch job ${job.id}`);\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 89.5,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: job, campaignBatches",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 2 async call(s) inside loop over campaignBatches\n// Problem: N+1 queries where N = campaignBatches.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = campaignBatches.map(job => job.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const job of campaignBatches) {\n      const campaign = await prisma.campaign.findUnique({\n        where: {id: job.data.campaignId},\n        select: {projectId: true},\n      });\n\n      if (campaign?.projectId === projectId) {\n        dataMap.get(job.id);\n        signale.info(`[QUEUE] Removed campaign batch job ${job.id}`);\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 81.69999999999999,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: campaign",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for campaign and related data\n// Optimized: Single query with include\n\nconst campaignWithRelations = await prisma.campaign.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const job of campaignBatches) {\n      const campaign = await prisma.campaign.findUnique({\n        where: {id: job.data.campaignId},\n        select: {projectId: true},\n      });\n\n      if (campaign?.projectId === projectId) {\n        await job.remove();\n        signale.info(`[QUEUE] Removed campaign batch job ${job.id}`);\n      }\n    }\n*/\n\n// Access related data directly: campaignWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/services/QueueService.ts",
   "line": 742,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.findUnique()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const job of workflowSteps) {\n      const execution = await prisma.workflowExecution.findUnique({\n        where: {id: job.data.executionId},\n        select: {workflow: {select: {projectId: true}}},\n      });\n\n      if (execution?.workflow.projectId === projectId) {\n        await job.remove();\n        signale.info(`[QUEUE] Removed workflow step job ${job.id}`);\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 88.25,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: job, workflowSteps",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 2 async call(s) inside loop over workflowSteps\n// Problem: N+1 queries where N = workflowSteps.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = workflowSteps.map(job => job.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const job of workflowSteps) {\n      const execution = await prisma.workflowExecution.findUnique({\n        where: {id: job.data.executionId},\n        select: {workflow: {select: {projectId: true}}},\n      });\n\n      if (execution?.workflow.projectId === projectId) {\n        dataMap.get(job.id);\n        signale.info(`[QUEUE] Removed workflow step job ${job.id}`);\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 81.69999999999999,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: workflowExecution",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for workflowExecution and related data\n// Optimized: Single query with include\n\nconst workflowExecutionWithRelations = await prisma.workflowExecution.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const job of workflowSteps) {\n      const execution = await prisma.workflowExecution.findUnique({\n        where: {id: job.data.executionId},\n        select: {workflow: {select: {projectId: true}}},\n      });\n\n      if (execution?.workflow.projectId === projectId) {\n        await job.remove();\n        signale.info(`[QUEUE] Removed workflow step job ${job.id}`);\n      }\n    }\n*/\n\n// Access related data directly: workflowExecutionWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/services/QueueService.ts",
   "line": 777,
   "severity": "high",
   "description": "Found 2 database queries (Prisma.count(), Prisma.update()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const campaign of sendingCampaigns) {\n        const actualEmailCount = await prisma.email.count({where: {campaignId: campaign.id}});\n        await prisma.campaign.update({\n          where: {id: campaign.id},\n          data: {totalRecipients: actualEmailCount},\n        });\n        await CampaignService.finalizeIfDone(campaign.id);\n      }",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 88.1,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: campaign, sendingCampaigns",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over sendingCampaigns\n// Problem: N+1 queries where N = sendingCampaigns.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = sendingCampaigns.map(campaign => campaign.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const campaign of sendingCampaigns) {\n        const actualEmailCount = await prisma.email.count({where: {campaignId: campaign.id}});\n        await prisma.campaign.update({\n          where: {id: campaign.id},\n          data: {totalRecipients: actualEmailCount},\n        });\n        dataMap.get(campaign.id);\n      }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 79.95,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: email",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for email and related data\n// Optimized: Single query with include\n\nconst emailWithRelations = await prisma.email.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const campaign of sendingCampaigns) {\n        const actualEmailCount = await prisma.email.count({where: {campaignId: campaign.id}});\n        await prisma.campaign.update({\n          where: {id: campaign.id},\n          data: {totalRecipients: actualEmailCount},\n        });\n        await CampaignService.finalizeIfDone(campaign.id);\n      }\n*/\n\n// Access related data directly: emailWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/jobs/import-processor.ts",
   "line": 87,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.upsert()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const [batchIndex, record] of batch.entries()) {\n            const rowNumber = i + batchIndex + 2; // +2 for header row and 1-based index\n\n            try {\n              // Validate email\n              const email = record.email?.trim();\n              if (!email) {\n                result.failureCount++;\n                result.errors.push({\n                  row: rowNumber,\n                  email: '',\n                  error: 'Email is required',\n                });\n                continue;\n              }\n\n              // Basic email validation\n              if (!isValidEmail(email)) {\n                result.failureCount++;\n                result.errors.push({\n                  row: rowNumber,\n                  email,\n                  error: 'Invalid email format',\n                });\n                continue;\n              }\n\n              // Extract subscribed field if present (case-insensitive)\n              const subscribedValue = record.subscribed;\n              let subscribed: boolean | undefined;\n\n              if (subscribedValue !== undefined && subscribedValue !== '') {\n                // Handle various truthy/falsy values\n                const lowerValue = subscribedValue.toLowerCase().trim();\n                subscribed = lowerValue === 'true' || lowerValue === '1' || lowerValue === 'yes';\n              }\n\n              // Extract custom data (all fields except email and subscribed)\n              const {email: _, subscribed: __, ...customData} = record;\n              const customEntries = Object.entries(customData);\n              const data =\n                customEntries.length > 0\n                  ? Object.fromEntries(customEntries.map(([k, v]) => [k, coerceCustomValue(v)]))\n                  : undefined;\n\n              // Check if contact exists before upserting\n              const existingContact = await ContactService.findByEmail(projectId, email);\n              const isUpdate = !!existingContact;\n\n              // Upsert contact with subscribed value from CSV if provided\n              // For new contacts, ContactService.upsert defaults to true\n              // For existing contacts, only update if explicitly provided in CSV\n              await ContactService.upsert(projectId, email, data, subscribed);\n\n              result.successCount++;\n              if (isUpdate) {\n                result.updatedCount++;\n              } else {\n                result.createdCount++;\n              }\n            } catch (error) {\n              result.failureCount++;\n              result.errors.push({\n                row: rowNumber,\n                email: record.email || '',\n                error: error instanceof Error ? error.message : 'Unknown error',\n              });\n            }\n          }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 68.3,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: , batch.entries()",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 2 async call(s) inside loop over batch.entries()\n// Problem: N+1 queries where N = batch.entries().length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = batch.entries().map( => .id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const [batchIndex, record] of batch.entries()) {\n            const rowNumber = i + batchIndex + 2; // +2 for header row and 1-based index\n\n            try {\n              // Validate email\n              const email = record.email?.trim();\n              if (!email) {\n                result.failureCount++;\n                result.errors.push({\n                  row: rowNumber,\n                  email: '',\n                  error: 'Email is required',\n                });\n                continue;\n              }\n\n              // Basic email validation\n              if (!isValidEmail(email)) {\n                result.failureCount++;\n                result.errors.push({\n                  row: rowNumber,\n                  email,\n                  error: 'Invalid email format',\n                });\n                continue;\n              }\n\n              // Extract subscribed field if present (case-insensitive)\n              const subscribedValue = record.subscribed;\n              let subscribed: boolean | undefined;\n\n              if (subscribedValue !== undefined && subscribedValue !== '') {\n                // Handle various truthy/falsy values\n                const lowerValue = subscribedValue.toLowerCase().trim();\n                subscribed = lowerValue === 'true' || lowerValue === '1' || lowerValue === 'yes';\n              }\n\n              // Extract custom data (all fields except email and subscribed)\n              const {email: _, subscribed: __, ...customData} = record;\n              const customEntries = Object.entries(customData);\n              const data =\n                customEntries.length > 0\n                  ? Object.fromEntries(customEntries.map(([k, v]) => [k, coerceCustomValue(v)]))\n                  : undefined;\n\n              // Check if contact exists before upserting\n              const existingContact = dataMap.get(.id);\n              const isUpdate = !!existingContact;\n\n              // Upsert contact with subscribed value from CSV if provided\n              // For new contacts, ContactService.upsert defaults to true\n              // For existing contacts, only update if explicitly provided in CSV\n              dataMap.get(.id);\n\n              result.successCount++;\n              if (isUpdate) {\n                result.updatedCount++;\n              } else {\n                result.createdCount++;\n              }\n            } catch (error) {\n              result.failureCount++;\n              result.errors.push({\n                row: rowNumber,\n                email: record.email || '',\n                error: error instanceof Error ? error.message : 'Unknown error',\n              });\n            }\n          }\n\n// Performance: 1 query instead of N queries"
    }
   ]
  },
  {
   "file": "apps/api/src/jobs/domain-verification.ts",
   "line": 54,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.update()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const sesIdentity of sesIdentities) {\n        const dbDomain = domains.find(d => d.domain === sesIdentity.domain);\n\n        if (!dbDomain) {\n          continue;\n        }\n\n        const isVerified = sesIdentity.status === 'Success';\n\n        // If domain failed verification, retry\n        if (sesIdentity.status === 'Failed') {\n          signale.warn(`[DOMAIN-VERIFICATION] Restarting verification for ${sesIdentity.domain}`);\n\n          let attempt = 0;\n          const maxAttempts = 5;\n          let success = false;\n          let delay = 5000;\n\n          while (attempt < maxAttempts && !success) {\n            try {\n              await verifyDomain(sesIdentity.domain);\n              success = true;\n              signale.success(`[DOMAIN-VERIFICATION] Restarted verification for ${sesIdentity.domain}`);\n            } catch (e: unknown) {\n              const error = e as {Code?: string; name?: string; message?: string};\n              if (\n                error?.Code === 'Throttling' ||\n                error?.name === 'Throttling' ||\n                error?.message?.includes('Throttling')\n              ) {\n                signale.warn(\n                  `[DOMAIN-VERIFICATION] Throttling detected, waiting ${delay / 1000} seconds (attempt ${attempt + 1})`,\n                );\n                await new Promise(r => setTimeout(r, delay));\n                delay *= 2; // Exponential backoff\n                attempt++;\n              } else {\n                signale.error(\n                  `[DOMAIN-VERIFICATION] Error restarting verification: ${error?.message || 'Unknown error'}`,\n                );\n                throw e;\n              }\n            }\n          }\n\n          if (!success) {\n            signale.error(\n              `[DOMAIN-VERIFICATION] Failed to verify ${sesIdentity.domain} after ${maxAttempts} attempts due to throttling`,\n            );\n          }\n        }\n\n        // Update verification status in database\n        await prisma.domain.update({\n          where: {id: dbDomain.id},\n          data: {verified: isVerified},\n        });\n\n        // If domain was just verified, disable feedback forwarding\n        if (!dbDomain.verified && isVerified) {\n          signale.success(`[DOMAIN-VERIFICATION] Domain ${sesIdentity.domain} is now verified!`);\n\n          try {\n            await disableFeedbackForwarding(sesIdentity.domain);\n            signale.info(`[DOMAIN-VERIFICATION] Disabled feedback forwarding for ${sesIdentity.domain}`);\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error disabling feedback forwarding: ${error}`);\n          }\n\n          // Send email notification about domain verified\n          try {\n            // Use SETNX to atomically check and set the flag (prevents race conditions)\n            const cacheKey = Keys.Domain.verifiedEmail(dbDomain.id);\n            const ttl = 604800; // 7 days\n\n            // SETNX returns 1 if key was set (didn't exist), 0 if key already existed\n            const wasSet = await redis.set(cacheKey, '1', 'EX', ttl, 'NX');\n            if (wasSet) {\n              const members = await MembershipService.getMembers(dbDomain.projectId);\n              const emails = members.map(m => m.email);\n              if (emails.length > 0) {\n                const template = React.createElement(DomainVerifiedEmail, {\n                  projectName: dbDomain.project.name,\n                  projectId: dbDomain.projectId,\n                  domain: sesIdentity.domain,\n                  dashboardUrl: DASHBOARD_URI,\n                  landingUrl: LANDING_URI,\n                });\n                await Promise.all(\n                  emails.map(email => sendPlatformEmail(email, 'Domain Verified Successfully', template)),\n                );\n              }\n            }\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error sending verified email: ${error}`);\n          }\n\n          // Invalidate cache\n          await redis.del(Keys.Domain.id(dbDomain.id));\n          await redis.del(Keys.Domain.project(dbDomain.projectId));\n        }\n\n        // If domain was unverified, invalidate cache\n        if (dbDomain.verified && !isVerified) {\n          signale.warn(`[DOMAIN-VERIFICATION] Domain ${sesIdentity.domain} is no longer verified`);\n\n          // Send email notification about domain verification failed\n          try {\n            // Use SETNX to atomically check and set the flag (prevents race conditions)\n            const now = new Date();\n            const year = now.getFullYear();\n            const month = String(now.getMonth() + 1).padStart(2, '0');\n            const cacheKey = Keys.Domain.unverifiedEmail(dbDomain.id, year, month);\n            const endOfMonth = new Date(now.getFullYear(), now.getMonth() + 1, 1);\n            const ttl = Math.floor((endOfMonth.getTime() - now.getTime()) / 1000);\n\n            // SETNX returns 1 if key was set (didn't exist), 0 if key already existed\n            const wasSet = await redis.set(cacheKey, '1', 'EX', ttl, 'NX');\n            if (wasSet) {\n              const members = await MembershipService.getMembers(dbDomain.projectId);\n              const emails = members.map(m => m.email);\n              if (emails.length > 0) {\n                const template = React.createElement(DomainUnverifiedEmail, {\n                  projectName: dbDomain.project.name,\n                  projectId: dbDomain.projectId,\n                  domain: sesIdentity.domain,\n                  dashboardUrl: DASHBOARD_URI,\n                  landingUrl: LANDING_URI,\n                });\n                await Promise.all(\n                  emails.map(email => sendPlatformEmail(email, 'Domain Verification Failed', template)),\n                );\n              }\n            }\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error sending unverified email: ${error}`);\n          }\n\n          await redis.del(Keys.Domain.id(dbDomain.id));\n          await redis.del(Keys.Domain.project(dbDomain.projectId));\n        }\n      }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "memoize",
     "rank": 1,
     "fitnessScore": 56,
     "reasoning": "Added memoization for 4 repeated calls",
     "code": "const setCache = new Map(); // Memoize redis.set\nconst getMembersCache = new Map(); // Memoize MembershipService.getMembers\nconst allCache = new Map(); // Memoize Promise.all\nconst delCache = new Map(); // Memoize redis.del\n\nfor (const sesIdentity of sesIdentities) {\n        const dbDomain = domains.find(d => d.domain === sesIdentity.domain);\n\n        if (!dbDomain) {\n          continue;\n        }\n\n        const isVerified = sesIdentity.status === 'Success';\n\n        // If domain failed verification, retry\n        if (sesIdentity.status === 'Failed') {\n          signale.warn(`[DOMAIN-VERIFICATION] Restarting verification for ${sesIdentity.domain}`);\n\n          let attempt = 0;\n          const maxAttempts = 5;\n          let success = false;\n          let delay = 5000;\n\n          while (attempt < maxAttempts && !success) {\n            try {\n              await verifyDomain(sesIdentity.domain);\n              success = true;\n              signale.success(`[DOMAIN-VERIFICATION] Restarted verification for ${sesIdentity.domain}`);\n            } catch (e: unknown) {\n              const error = e as {Code?: string; name?: string; message?: string};\n              if (\n                error?.Code === 'Throttling' ||\n                error?.name === 'Throttling' ||\n                error?.message?.includes('Throttling')\n              ) {\n                signale.warn(\n                  `[DOMAIN-VERIFICATION] Throttling detected, waiting ${delay / 1000} seconds (attempt ${attempt + 1})`,\n                );\n                await new Promise(r => setTimeout(r, delay));\n                delay *= 2; // Exponential backoff\n                attempt++;\n              } else {\n                signale.error(\n                  `[DOMAIN-VERIFICATION] Error restarting verification: ${error?.message || 'Unknown error'}`,\n                );\n                throw e;\n              }\n            }\n          }\n\n          if (!success) {\n            signale.error(\n              `[DOMAIN-VERIFICATION] Failed to verify ${sesIdentity.domain} after ${maxAttempts} attempts due to throttling`,\n            );\n          }\n        }\n\n        // Update verification status in database\n        await prisma.domain.update({\n          where: {id: dbDomain.id},\n          data: {verified: isVerified},\n        });\n\n        // If domain was just verified, disable feedback forwarding\n        if (!dbDomain.verified && isVerified) {\n          signale.success(`[DOMAIN-VERIFICATION] Domain ${sesIdentity.domain} is now verified!`);\n\n          try {\n            await disableFeedbackForwarding(sesIdentity.domain);\n            signale.info(`[DOMAIN-VERIFICATION] Disabled feedback forwarding for ${sesIdentity.domain}`);\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error disabling feedback forwarding: ${error}`);\n          }\n\n          // Send email notification about domain verified\n          try {\n            // Use SETNX to atomically check and set the flag (prevents race conditions)\n            const cacheKey = Keys.Domain.verifiedEmail(dbDomain.id);\n            const ttl = 604800; // 7 days\n\n            // SETNX returns 1 if key was set (didn't exist), 0 if key already existed\n            const wasSet = await redis.set(cacheKey, '1', 'EX', ttl, 'NX');\n            if (wasSet) {\n              const members = await MembershipService.getMembers(dbDomain.projectId);\n              const emails = members.map(m => m.email);\n              if (emails.length > 0) {\n                const template = React.createElement(DomainVerifiedEmail, {\n                  projectName: dbDomain.project.name,\n                  projectId: dbDomain.projectId,\n                  domain: sesIdentity.domain,\n                  dashboardUrl: DASHBOARD_URI,\n                  landingUrl: LANDING_URI,\n                });\n                await Promise.all(\n                  emails.map(email => sendPlatformEmail(email, 'Domain Verified Successfully', template)),\n                );\n              }\n            }\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error sending verified email: ${error}`);\n          }\n\n          // Invalidate cache\n          await redis.del(Keys.Domain.id(dbDomain.id));\n          await redis.del(Keys.Domain.project(dbDomain.projectId));\n        }\n\n        // If domain was unverified, invalidate cache\n        if (dbDomain.verified && !isVerified) {\n          signale.warn(`[DOMAIN-VERIFICATION] Domain ${sesIdentity.domain} is no longer verified`);\n\n          // Send email notification about domain verification failed\n          try {\n            // Use SETNX to atomically check and set the flag (prevents race conditions)\n            const now = new Date();\n            const year = now.getFullYear();\n            const month = String(now.getMonth() + 1).padStart(2, '0');\n            const cacheKey = Keys.Domain.unverifiedEmail(dbDomain.id, year, month);\n            const endOfMonth = new Date(now.getFullYear(), now.getMonth() + 1, 1);\n            const ttl = Math.floor((endOfMonth.getTime() - now.getTime()) / 1000);\n\n            // SETNX returns 1 if key was set (didn't exist), 0 if key already existed\n            const wasSet = await redis.set(cacheKey, '1', 'EX', ttl, 'NX');\n            if (wasSet) {\n              const members = await MembershipService.getMembers(dbDomain.projectId);\n              const emails = members.map(m => m.email);\n              if (emails.length > 0) {\n                const template = React.createElement(DomainUnverifiedEmail, {\n                  projectName: dbDomain.project.name,\n                  projectId: dbDomain.projectId,\n                  domain: sesIdentity.domain,\n                  dashboardUrl: DASHBOARD_URI,\n                  landingUrl: LANDING_URI,\n                });\n                await Promise.all(\n                  emails.map(email => sendPlatformEmail(email, 'Domain Verification Failed', template)),\n                );\n              }\n            }\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error sending unverified email: ${error}`);\n          }\n\n          await redis.del(Keys.Domain.id(dbDomain.id));\n          await redis.del(Keys.Domain.project(dbDomain.projectId));\n        }\n      }"
    },
    {
     "type": "memoization",
     "rank": 2,
     "fitnessScore": 56,
     "reasoning": "Add memoization cache for repeated expensive calls\nPreserved: redis.set, MembershipService.getMembers, Promise.all, redis.del",
     "code": "// OPTIMIZED: Memoization for repeated expensive calls\n// Found 4 repeated async call(s)\n\n// Memoize redis.set (called 2x)\nconst setCache = new Map();\nasync function memoizedSet(key) {\n  if (setCache.has(key)) return setCache.get(key);\n  const result = await redis.set(key);\n  setCache.set(key, result);\n  return result;\n}\n\n// Memoize MembershipService.getMembers (called 2x)\nconst getMembersCache = new Map();\nasync function memoizedGetMembers(key) {\n  if (getMembersCache.has(key)) return getMembersCache.get(key);\n  const result = await MembershipService.getMembers(key);\n  getMembersCache.set(key, result);\n  return result;\n}\n\n// Memoize Promise.all (called 2x)\nconst allCache = new Map();\nasync function memoizedAll(key) {\n  if (allCache.has(key)) return allCache.get(key);\n  const result = await Promise.all(key);\n  allCache.set(key, result);\n  return result;\n}\n\n// Memoize redis.del (called 4x)\nconst delCache = new Map();\nasync function memoizedDel(key) {\n  if (delCache.has(key)) return delCache.get(key);\n  const result = await redis.del(key);\n  delCache.set(key, result);\n  return result;\n}\n\n// Original code (replace calls with memoized versions):\nfor (const sesIdentity of sesIdentities) {\n        const dbDomain = domains.find(d => d.domain === sesIdentity.domain);\n\n        if (!dbDomain) {\n          continue;\n        }\n\n        const isVerified = sesIdentity.status === 'Success';\n\n        // If domain failed verification, retry\n        if (sesIdentity.status === 'Failed') {\n          signale.warn(`[DOMAIN-VERIFICATION] Restarting verification for ${sesIdentity.domain}`);\n\n          let attempt = 0;\n          const maxAttempts = 5;\n          let success = false;\n          let delay = 5000;\n\n          while (attempt < maxAttempts && !success) {\n            try {\n              await verifyDomain(sesIdentity.domain);\n              success = true;\n              signale.success(`[DOMAIN-VERIFICATION] Restarted verification for ${sesIdentity.domain}`);\n            } catch (e: unknown) {\n              const error = e as {Code?: string; name?: string; message?: string};\n              if (\n                error?.Code === 'Throttling' ||\n                error?.name === 'Throttling' ||\n                error?.message?.includes('Throttling')\n              ) {\n                signale.warn(\n                  `[DOMAIN-VERIFICATION] Throttling detected, waiting ${delay / 1000} seconds (attempt ${attempt + 1})`,\n                );\n                await new Promise(r => setTimeout(r, delay));\n                delay *= 2; // Exponential backoff\n                attempt++;\n              } else {\n                signale.error(\n                  `[DOMAIN-VERIFICATION] Error restarting verification: ${error?.message || 'Unknown error'}`,\n                );\n                throw e;\n              }\n            }\n          }\n\n          if (!success) {\n            signale.error(\n              `[DOMAIN-VERIFICATION] Failed to verify ${sesIdentity.domain} after ${maxAttempts} attempts due to throttling`,\n            );\n          }\n        }\n\n        // Update verification status in database\n        await prisma.domain.update({\n          where: {id: dbDomain.id},\n          data: {verified: isVerified},\n        });\n\n        // If domain was just verified, disable feedback forwarding\n        if (!dbDomain.verified && isVerified) {\n          signale.success(`[DOMAIN-VERIFICATION] Domain ${sesIdentity.domain} is now verified!`);\n\n          try {\n            await disableFeedbackForwarding(sesIdentity.domain);\n            signale.info(`[DOMAIN-VERIFICATION] Disabled feedback forwarding for ${sesIdentity.domain}`);\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error disabling feedback forwarding: ${error}`);\n          }\n\n          // Send email notification about domain verified\n          try {\n            // Use SETNX to atomically check and set the flag (prevents race conditions)\n            const cacheKey = Keys.Domain.verifiedEmail(dbDomain.id);\n            const ttl = 604800; // 7 days\n\n            // SETNX returns 1 if key was set (didn't exist), 0 if key already existed\n            const wasSet = await redis.set(cacheKey, '1', 'EX', ttl, 'NX');\n            if (wasSet) {\n              const members = await MembershipService.getMembers(dbDomain.projectId);\n              const emails = members.map(m => m.email);\n              if (emails.length > 0) {\n                const template = React.createElement(DomainVerifiedEmail, {\n                  projectName: dbDomain.project.name,\n                  projectId: dbDomain.projectId,\n                  domain: sesIdentity.domain,\n                  dashboardUrl: DASHBOARD_URI,\n                  landingUrl: LANDING_URI,\n                });\n                await Promise.all(\n                  emails.map(email => sendPlatformEmail(email, 'Domain Verified Successfully', template)),\n                );\n              }\n            }\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error sending verified email: ${error}`);\n          }\n\n          // Invalidate cache\n          await redis.del(Keys.Domain.id(dbDomain.id));\n          await redis.del(Keys.Domain.project(dbDomain.projectId));\n        }\n\n        // If domain was unverified, invalidate cache\n        if (dbDomain.verified && !isVerified) {\n          signale.warn(`[DOMAIN-VERIFICATION] Domain ${sesIdentity.domain} is no longer verified`);\n\n          // Send email notification about domain verification failed\n          try {\n            // Use SETNX to atomically check and set the flag (prevents race conditions)\n            const now = new Date();\n            const year = now.getFullYear();\n            const month = String(now.getMonth() + 1).padStart(2, '0');\n            const cacheKey = Keys.Domain.unverifiedEmail(dbDomain.id, year, month);\n            const endOfMonth = new Date(now.getFullYear(), now.getMonth() + 1, 1);\n            const ttl = Math.floor((endOfMonth.getTime() - now.getTime()) / 1000);\n\n            // SETNX returns 1 if key was set (didn't exist), 0 if key already existed\n            const wasSet = await redis.set(cacheKey, '1', 'EX', ttl, 'NX');\n            if (wasSet) {\n              const members = await MembershipService.getMembers(dbDomain.projectId);\n              const emails = members.map(m => m.email);\n              if (emails.length > 0) {\n                const template = React.createElement(DomainUnverifiedEmail, {\n                  projectName: dbDomain.project.name,\n                  projectId: dbDomain.projectId,\n                  domain: sesIdentity.domain,\n                  dashboardUrl: DASHBOARD_URI,\n                  landingUrl: LANDING_URI,\n                });\n                await Promise.all(\n                  emails.map(email => sendPlatformEmail(email, 'Domain Verification Failed', template)),\n                );\n              }\n            }\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error sending unverified email: ${error}`);\n          }\n\n          await redis.del(Keys.Domain.id(dbDomain.id));\n          await redis.del(Keys.Domain.project(dbDomain.projectId));\n        }\n      }"
    },
    {
     "type": "batch-query-before-loop",
     "rank": 3,
     "fitnessScore": 53.2,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: sesIdentity, sesIdentities",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 14 async call(s) inside loop over sesIdentities\n// Problem: N+1 queries where N = sesIdentities.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = sesIdentities.map(sesIdentity => sesIdentity.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const sesIdentity of sesIdentities) {\n        const dbDomain = domains.find(d => d.domain === sesIdentity.domain);\n\n        if (!dbDomain) {\n          continue;\n        }\n\n        const isVerified = sesIdentity.status === 'Success';\n\n        // If domain failed verification, retry\n        if (sesIdentity.status === 'Failed') {\n          signale.warn(`[DOMAIN-VERIFICATION] Restarting verification for ${sesIdentity.domain}`);\n\n          let attempt = 0;\n          const maxAttempts = 5;\n          let success = false;\n          let delay = 5000;\n\n          while (attempt < maxAttempts && !success) {\n            try {\n              await verifyDomain(sesIdentity.domain);\n              success = true;\n              signale.success(`[DOMAIN-VERIFICATION] Restarted verification for ${sesIdentity.domain}`);\n            } catch (e: unknown) {\n              const error = e as {Code?: string; name?: string; message?: string};\n              if (\n                error?.Code === 'Throttling' ||\n                error?.name === 'Throttling' ||\n                error?.message?.includes('Throttling')\n              ) {\n                signale.warn(\n                  `[DOMAIN-VERIFICATION] Throttling detected, waiting ${delay / 1000} seconds (attempt ${attempt + 1})`,\n                );\n                await new Promise(r => setTimeout(r, delay));\n                delay *= 2; // Exponential backoff\n                attempt++;\n              } else {\n                signale.error(\n                  `[DOMAIN-VERIFICATION] Error restarting verification: ${error?.message || 'Unknown error'}`,\n                );\n                throw e;\n              }\n            }\n          }\n\n          if (!success) {\n            signale.error(\n              `[DOMAIN-VERIFICATION] Failed to verify ${sesIdentity.domain} after ${maxAttempts} attempts due to throttling`,\n            );\n          }\n        }\n\n        // Update verification status in database\n        await prisma.domain.update({\n          where: {id: dbDomain.id},\n          data: {verified: isVerified},\n        });\n\n        // If domain was just verified, disable feedback forwarding\n        if (!dbDomain.verified && isVerified) {\n          signale.success(`[DOMAIN-VERIFICATION] Domain ${sesIdentity.domain} is now verified!`);\n\n          try {\n            await disableFeedbackForwarding(sesIdentity.domain);\n            signale.info(`[DOMAIN-VERIFICATION] Disabled feedback forwarding for ${sesIdentity.domain}`);\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error disabling feedback forwarding: ${error}`);\n          }\n\n          // Send email notification about domain verified\n          try {\n            // Use SETNX to atomically check and set the flag (prevents race conditions)\n            const cacheKey = Keys.Domain.verifiedEmail(dbDomain.id);\n            const ttl = 604800; // 7 days\n\n            // SETNX returns 1 if key was set (didn't exist), 0 if key already existed\n            const wasSet = dataMap.get(sesIdentity.id);\n            if (wasSet) {\n              const members = dataMap.get(sesIdentity.id);\n              const emails = members.map(m => m.email);\n              if (emails.length > 0) {\n                const template = React.createElement(DomainVerifiedEmail, {\n                  projectName: dbDomain.project.name,\n                  projectId: dbDomain.projectId,\n                  domain: sesIdentity.domain,\n                  dashboardUrl: DASHBOARD_URI,\n                  landingUrl: LANDING_URI,\n                });\n                dataMap.get(sesIdentity.id)),\n                );\n              }\n            }\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error sending verified email: ${error}`);\n          }\n\n          // Invalidate cache\n          dataMap.get(sesIdentity.id));\n          dataMap.get(sesIdentity.id));\n        }\n\n        // If domain was unverified, invalidate cache\n        if (dbDomain.verified && !isVerified) {\n          signale.warn(`[DOMAIN-VERIFICATION] Domain ${sesIdentity.domain} is no longer verified`);\n\n          // Send email notification about domain verification failed\n          try {\n            // Use SETNX to atomically check and set the flag (prevents race conditions)\n            const now = new Date();\n            const year = now.getFullYear();\n            const month = String(now.getMonth() + 1).padStart(2, '0');\n            const cacheKey = Keys.Domain.unverifiedEmail(dbDomain.id, year, month);\n            const endOfMonth = new Date(now.getFullYear(), now.getMonth() + 1, 1);\n            const ttl = Math.floor((endOfMonth.getTime() - now.getTime()) / 1000);\n\n            // SETNX returns 1 if key was set (didn't exist), 0 if key already existed\n            const wasSet = dataMap.get(sesIdentity.id);\n            if (wasSet) {\n              const members = dataMap.get(sesIdentity.id);\n              const emails = members.map(m => m.email);\n              if (emails.length > 0) {\n                const template = React.createElement(DomainUnverifiedEmail, {\n                  projectName: dbDomain.project.name,\n                  projectId: dbDomain.projectId,\n                  domain: sesIdentity.domain,\n                  dashboardUrl: DASHBOARD_URI,\n                  landingUrl: LANDING_URI,\n                });\n                dataMap.get(sesIdentity.id)),\n                );\n              }\n            }\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error sending unverified email: ${error}`);\n          }\n\n          dataMap.get(sesIdentity.id));\n          dataMap.get(sesIdentity.id));\n        }\n      }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 4,
     "fitnessScore": 45.75,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: domain",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for domain and related data\n// Optimized: Single query with include\n\nconst domainWithRelations = await prisma.domain.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const sesIdentity of sesIdentities) {\n        const dbDomain = domains.find(d => d.domain === sesIdentity.domain);\n\n        if (!dbDomain) {\n          continue;\n        }\n\n        const isVerified = sesIdentity.status === 'Success';\n\n        // If domain failed verification, retry\n        if (sesIdentity.status === 'Failed') {\n          signale.warn(`[DOMAIN-VERIFICATION] Restarting verification for ${sesIdentity.domain}`);\n\n          let attempt = 0;\n          const maxAttempts = 5;\n          let success = false;\n          let delay = 5000;\n\n          while (attempt < maxAttempts && !success) {\n            try {\n              await verifyDomain(sesIdentity.domain);\n              success = true;\n              signale.success(`[DOMAIN-VERIFICATION] Restarted verification for ${sesIdentity.domain}`);\n            } catch (e: unknown) {\n              const error = e as {Code?: string; name?: string; message?: string};\n              if (\n                error?.Code === 'Throttling' ||\n                error?.name === 'Throttling' ||\n                error?.message?.includes('Throttling')\n              ) {\n                signale.warn(\n                  `[DOMAIN-VERIFICATION] Throttling detected, waiting ${delay / 1000} seconds (attempt ${attempt + 1})`,\n                );\n                await new Promise(r => setTimeout(r, delay));\n                delay *= 2; // Exponential backoff\n                attempt++;\n              } else {\n                signale.error(\n                  `[DOMAIN-VERIFICATION] Error restarting verification: ${error?.message || 'Unknown error'}`,\n                );\n                throw e;\n              }\n            }\n          }\n\n          if (!success) {\n            signale.error(\n              `[DOMAIN-VERIFICATION] Failed to verify ${sesIdentity.domain} after ${maxAttempts} attempts due to throttling`,\n            );\n          }\n        }\n\n        // Update verification status in database\n        await prisma.domain.update({\n          where: {id: dbDomain.id},\n          data: {verified: isVerified},\n        });\n\n        // If domain was just verified, disable feedback forwarding\n        if (!dbDomain.verified && isVerified) {\n          signale.success(`[DOMAIN-VERIFICATION] Domain ${sesIdentity.domain} is now verified!`);\n\n          try {\n            await disableFeedbackForwarding(sesIdentity.domain);\n            signale.info(`[DOMAIN-VERIFICATION] Disabled feedback forwarding for ${sesIdentity.domain}`);\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error disabling feedback forwarding: ${error}`);\n          }\n\n          // Send email notification about domain verified\n          try {\n            // Use SETNX to atomically check and set the flag (prevents race conditions)\n            const cacheKey = Keys.Domain.verifiedEmail(dbDomain.id);\n            const ttl = 604800; // 7 days\n\n            // SETNX returns 1 if key was set (didn't exist), 0 if key already existed\n            const wasSet = await redis.set(cacheKey, '1', 'EX', ttl, 'NX');\n            if (wasSet) {\n              const members = await MembershipService.getMembers(dbDomain.projectId);\n              const emails = members.map(m => m.email);\n              if (emails.length > 0) {\n                const template = React.createElement(DomainVerifiedEmail, {\n                  projectName: dbDomain.project.name,\n                  projectId: dbDomain.projectId,\n                  domain: sesIdentity.domain,\n                  dashboardUrl: DASHBOARD_URI,\n                  landingUrl: LANDING_URI,\n                });\n                await Promise.all(\n                  emails.map(email => sendPlatformEmail(email, 'Domain Verified Successfully', template)),\n                );\n              }\n            }\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error sending verified email: ${error}`);\n          }\n\n          // Invalidate cache\n          await redis.del(Keys.Domain.id(dbDomain.id));\n          await redis.del(Keys.Domain.project(dbDomain.projectId));\n        }\n\n        // If domain was unverified, invalidate cache\n        if (dbDomain.verified && !isVerified) {\n          signale.warn(`[DOMAIN-VERIFICATION] Domain ${sesIdentity.domain} is no longer verified`);\n\n          // Send email notification about domain verification failed\n          try {\n            // Use SETNX to atomically check and set the flag (prevents race conditions)\n            const now = new Date();\n            const year = now.getFullYear();\n            const month = String(now.getMonth() + 1).padStart(2, '0');\n            const cacheKey = Keys.Domain.unverifiedEmail(dbDomain.id, year, month);\n            const endOfMonth = new Date(now.getFullYear(), now.getMonth() + 1, 1);\n            const ttl = Math.floor((endOfMonth.getTime() - now.getTime()) / 1000);\n\n            // SETNX returns 1 if key was set (didn't exist), 0 if key already existed\n            const wasSet = await redis.set(cacheKey, '1', 'EX', ttl, 'NX');\n            if (wasSet) {\n              const members = await MembershipService.getMembers(dbDomain.projectId);\n              const emails = members.map(m => m.email);\n              if (emails.length > 0) {\n                const template = React.createElement(DomainUnverifiedEmail, {\n                  projectName: dbDomain.project.name,\n                  projectId: dbDomain.projectId,\n                  domain: sesIdentity.domain,\n                  dashboardUrl: DASHBOARD_URI,\n                  landingUrl: LANDING_URI,\n                });\n                await Promise.all(\n                  emails.map(email => sendPlatformEmail(email, 'Domain Verification Failed', template)),\n                );\n              }\n            }\n          } catch (error) {\n            signale.error(`[DOMAIN-VERIFICATION] Error sending unverified email: ${error}`);\n          }\n\n          await redis.del(Keys.Domain.id(dbDomain.id));\n          await redis.del(Keys.Domain.project(dbDomain.projectId));\n        }\n      }\n*/\n\n// Access related data directly: domainWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/jobs/card-verification-sweep-processor.ts",
   "line": 87,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.update()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const project of stale) {\n    const stalledForMs = now - (project.cardVerificationAt?.getTime() ?? now);\n\n    // REQUIRES_ACTION already carries an issuer verdict — an SCA challenge, a soft decline, or\n    // a payment method we cannot test. Re-charging resolves none of those, so it only escalates.\n    const retryable =\n      project.cardVerification === 'PENDING' &&\n      project.customer &&\n      project.cardVerificationSession &&\n      project.cardVerificationAttempts < MAX_RETRIES;\n\n    if (retryable) {\n      const requeued = await requeueVerification(\n        project.id,\n        project.customer as string,\n        project.cardVerificationSession as string,\n        project.cardVerificationAttempts,\n      );\n\n      if (requeued) {\n        retried += 1;\n        continue;\n      }\n    }\n\n    await NtfyService.notifyCardVerificationStalled(\n      project.name,\n      project.id,\n      project.cardVerification ?? 'UNKNOWN',\n      Math.round(stalledForMs / 60000),\n      project.cardVerificationAttempts >= MAX_RETRIES,\n    );\n\n    // Park it past the retry budget so this alert fires once rather than every sweep. The row\n    // keeps the state, so `cardVerification = 'REQUIRES_ACTION'` remains the support query.\n    await prisma.project.update({\n      where: {id: project.id},\n      data: {\n        cardVerification: 'REQUIRES_ACTION',\n        cardVerificationAttempts: MAX_RETRIES + 1,\n        cardVerificationAt: new Date(),\n      },\n    });\n\n    escalated += 1;\n  }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 77.85,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: project, stale",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over stale\n// Problem: N+1 queries where N = stale.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = stale.map(project => project.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const project of stale) {\n    const stalledForMs = now - (project.cardVerificationAt?.getTime() ?? now);\n\n    // REQUIRES_ACTION already carries an issuer verdict — an SCA challenge, a soft decline, or\n    // a payment method we cannot test. Re-charging resolves none of those, so it only escalates.\n    const retryable =\n      project.cardVerification === 'PENDING' &&\n      project.customer &&\n      project.cardVerificationSession &&\n      project.cardVerificationAttempts < MAX_RETRIES;\n\n    if (retryable) {\n      const requeued = await requeueVerification(\n        project.id,\n        project.customer as string,\n        project.cardVerificationSession as string,\n        project.cardVerificationAttempts,\n      );\n\n      if (requeued) {\n        retried += 1;\n        continue;\n      }\n    }\n\n    dataMap.get(project.id),\n      project.cardVerificationAttempts >= MAX_RETRIES,\n    );\n\n    // Park it past the retry budget so this alert fires once rather than every sweep. The row\n    // keeps the state, so `cardVerification = 'REQUIRES_ACTION'` remains the support query.\n    await prisma.project.update({\n      where: {id: project.id},\n      data: {\n        cardVerification: 'REQUIRES_ACTION',\n        cardVerificationAttempts: MAX_RETRIES + 1,\n        cardVerificationAt: new Date(),\n      },\n    });\n\n    escalated += 1;\n  }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 65.2,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: project",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for project and related data\n// Optimized: Single query with include\n\nconst projectWithRelations = await prisma.project.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const project of stale) {\n    const stalledForMs = now - (project.cardVerificationAt?.getTime() ?? now);\n\n    // REQUIRES_ACTION already carries an issuer verdict — an SCA challenge, a soft decline, or\n    // a payment method we cannot test. Re-charging resolves none of those, so it only escalates.\n    const retryable =\n      project.cardVerification === 'PENDING' &&\n      project.customer &&\n      project.cardVerificationSession &&\n      project.cardVerificationAttempts < MAX_RETRIES;\n\n    if (retryable) {\n      const requeued = await requeueVerification(\n        project.id,\n        project.customer as string,\n        project.cardVerificationSession as string,\n        project.cardVerificationAttempts,\n      );\n\n      if (requeued) {\n        retried += 1;\n        continue;\n      }\n    }\n\n    await NtfyService.notifyCardVerificationStalled(\n      project.name,\n      project.id,\n      project.cardVerification ?? 'UNKNOWN',\n      Math.round(stalledForMs / 60000),\n      project.cardVerificationAttempts >= MAX_RETRIES,\n    );\n\n    // Park it past the retry budget so this alert fires once rather than every sweep. The row\n    // keeps the state, so `cardVerification = 'REQUIRES_ACTION'` remains the support query.\n    await prisma.project.update({\n      where: {id: project.id},\n      data: {\n        cardVerification: 'REQUIRES_ACTION',\n        cardVerificationAttempts: MAX_RETRIES + 1,\n        cardVerificationAt: new Date(),\n      },\n    });\n\n    escalated += 1;\n  }\n*/\n\n// Access related data directly: projectWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/controllers/Webhooks.ts",
   "line": 129,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.findMany()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const recipient of recipients) {\n            const recipientEmail = recipient as string;\n            const domain = recipientEmail.split('@')[1];\n\n            if (!domain) {\n              signale.warn('[WEBHOOK] Invalid recipient email format:', recipientEmail);\n              continue;\n            }\n\n            // Find ALL projects that have this domain verified\n            // A domain can be shared across multiple projects if users are members of both\n            const domainRecords = await prisma.domain.findMany({\n              where: {\n                domain,\n                verified: true, // Only process emails for verified domains\n              },\n              include: {\n                project: true,\n              },\n            });\n\n            if (domainRecords.length === 0) {\n              signale.info(`[WEBHOOK] No verified domain found for: ${domain}`);\n              continue;\n            }\n\n            signale.info(\n              `[WEBHOOK] Found ${domainRecords.length} project(s) with verified domain ${domain}. Processing inbound email for all.`,\n            );\n\n            // Extract sender information (same for all projects)\n            const senderEmail = body.mail?.source;\n            const senderFromHeader = body.mail?.commonHeaders?.from?.[0] || senderEmail;\n\n            // Parse email content if available\n            let htmlBody: string | undefined;\n\n            if (body.content && typeof body.content === 'string') {\n              try {\n                const isBase64 = body.receipt?.action?.encoding === 'BASE64';\n                const emailBuffer = isBase64\n                  ? Buffer.from(body.content, 'base64')\n                  : Buffer.from(body.content);\n\n                const parsed = await simpleParser(emailBuffer);\n                const raw =\n                  (parsed.html ? String(parsed.html) : undefined) ??\n                  parsed.textAsHtml ??\n                  parsed.text ??\n                  undefined;\n\n                if (raw) {\n                  htmlBody = sanitizeHtml(raw, {\n                    allowedTags: sanitizeHtml.defaults.allowedTags.concat(['img']),\n                    allowedAttributes: {\n                      ...sanitizeHtml.defaults.allowedAttributes,\n                      img: ['src', 'alt', 'width', 'height'],\n                      '*': ['style'],\n                    },\n                    allowedSchemes: ['http', 'https', 'mailto'],\n                  });\n                }\n              } catch (parseError) {\n                signale.error('[WEBHOOK] Failed to parse email content:', parseError);\n              }\n            }\n\n            // Process inbound email for each project that has this domain verified\n            for (const domainRecord of domainRecords) {\n              signale.info(`[WEBHOOK] Processing inbound email for project: ${domainRecord.project.name}`);\n\n              // Check billing limits before processing inbound email\n              const limitCheck = await BillingLimitService.checkLimit(domainRecord.projectId, EmailSourceType.INBOUND);\n\n              if (!limitCheck.allowed) {\n                signale.warn(\n                  `[WEBHOOK] Inbound email blocked for project ${domainRecord.project.name}: ${limitCheck.message}`,\n                );\n                continue; // Skip this project but continue processing for other projects\n              }\n\n              // Find or create a contact for the sender in this project\n              let contact;\n              if (senderEmail) {\n                contact = await ContactService.upsert(\n                  domainRecord.projectId,\n                  senderEmail,\n                  undefined, // No additional data\n                  true, // Subscribe by default for inbound email senders\n                );\n              }\n\n              // Create an Email record for tracking with parsed content\n              const inboundEmail = await prisma.email.create({\n                data: {\n                  projectId: domainRecord.projectId,\n                  contactId: contact!.id,\n                  subject: body.mail?.commonHeaders?.subject || '(No subject)',\n                  body: htmlBody || '', // Store HTML body in the body field\n                  from: recipientEmail, // The recipient address that received the email\n                  sourceType: EmailSourceType.INBOUND,\n                  status: EmailStatus.RECEIVED, // Inbound emails use RECEIVED status\n                  deliveredAt: new Date(body.mail?.timestamp || new Date()),\n                },\n              });\n\n              // Increment usage counter in cache\n              await BillingLimitService.incrementUsage(domainRecord.projectId, EmailSourceType.INBOUND);\n\n              // Record Stripe metering if project has customer\n              if (domainRecord.project.customer) {\n                await MeterService.recordEmailSent(\n                  domainRecord.project.customer,\n                  1, // Inbound emails count as 1 credit\n                  `email_${inboundEmail.id}`,\n                );\n              }\n\n              // Prepare event data with all inbound email details including body content\n              const eventData = {\n                messageId: body.mail?.messageId,\n                from: senderEmail,\n                fromHeader: senderFromHeader,\n                to: recipientEmail,\n                subject: body.mail?.commonHeaders?.subject,\n                timestamp: body.mail?.timestamp,\n                recipients: body.receipt?.recipients,\n                hasContent: !!body.content,\n                // Email body content\n                body: htmlBody,\n                // Security verdicts\n                spamVerdict: body.receipt?.spamVerdict?.status,\n                virusVerdict: body.receipt?.virusVerdict?.status,\n                spfVerdict: body.receipt?.spfVerdict?.status,\n                dkimVerdict: body.receipt?.dkimVerdict?.status,\n                dmarcVerdict: body.receipt?.dmarcVerdict?.status,\n                // Processing metadata\n                processingTimeMillis: body.receipt?.processingTimeMillis,\n              };\n\n              // Create the email.received event (this will trigger workflows)\n              await EventService.trackEvent(\n                domainRecord.projectId,\n                'email.received',\n                contact?.id,\n                inboundEmail.id, // Link the event to the inbound email record\n                eventData,\n              );\n\n              signale.success(\n                `[WEBHOOK] Created email.received event for ${senderEmail} → ${recipientEmail} (project: ${domainRecord.project.name})`,\n              );\n            }\n          }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 55.3,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: domainRecord, domainRecords",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 8 async call(s) inside loop over domainRecords\n// Problem: N+1 queries where N = domainRecords.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = domainRecords.map(domainRecord => domainRecord.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const recipient of recipients) {\n            const recipientEmail = recipient as string;\n            const domain = recipientEmail.split('@')[1];\n\n            if (!domain) {\n              signale.warn('[WEBHOOK] Invalid recipient email format:', recipientEmail);\n              continue;\n            }\n\n            // Find ALL projects that have this domain verified\n            // A domain can be shared across multiple projects if users are members of both\n            const domainRecords = await prisma.domain.findMany({\n              where: {\n                domain,\n                verified: true, // Only process emails for verified domains\n              },\n              include: {\n                project: true,\n              },\n            });\n\n            if (domainRecords.length === 0) {\n              signale.info(`[WEBHOOK] No verified domain found for: ${domain}`);\n              continue;\n            }\n\n            signale.info(\n              `[WEBHOOK] Found ${domainRecords.length} project(s) with verified domain ${domain}. Processing inbound email for all.`,\n            );\n\n            // Extract sender information (same for all projects)\n            const senderEmail = body.mail?.source;\n            const senderFromHeader = body.mail?.commonHeaders?.from?.[0] || senderEmail;\n\n            // Parse email content if available\n            let htmlBody: string | undefined;\n\n            if (body.content && typeof body.content === 'string') {\n              try {\n                const isBase64 = body.receipt?.action?.encoding === 'BASE64';\n                const emailBuffer = isBase64\n                  ? Buffer.from(body.content, 'base64')\n                  : Buffer.from(body.content);\n\n                const parsed = await simpleParser(emailBuffer);\n                const raw =\n                  (parsed.html ? String(parsed.html) : undefined) ??\n                  parsed.textAsHtml ??\n                  parsed.text ??\n                  undefined;\n\n                if (raw) {\n                  htmlBody = sanitizeHtml(raw, {\n                    allowedTags: sanitizeHtml.defaults.allowedTags.concat(['img']),\n                    allowedAttributes: {\n                      ...sanitizeHtml.defaults.allowedAttributes,\n                      img: ['src', 'alt', 'width', 'height'],\n                      '*': ['style'],\n                    },\n                    allowedSchemes: ['http', 'https', 'mailto'],\n                  });\n                }\n              } catch (parseError) {\n                signale.error('[WEBHOOK] Failed to parse email content:', parseError);\n              }\n            }\n\n            // Process inbound email for each project that has this domain verified\n            for (const domainRecord of domainRecords) {\n              signale.info(`[WEBHOOK] Processing inbound email for project: ${domainRecord.project.name}`);\n\n              // Check billing limits before processing inbound email\n              const limitCheck = dataMap.get(domainRecord.id);\n\n              if (!limitCheck.allowed) {\n                signale.warn(\n                  `[WEBHOOK] Inbound email blocked for project ${domainRecord.project.name}: ${limitCheck.message}`,\n                );\n                continue; // Skip this project but continue processing for other projects\n              }\n\n              // Find or create a contact for the sender in this project\n              let contact;\n              if (senderEmail) {\n                contact = dataMap.get(domainRecord.id);\n              }\n\n              // Create an Email record for tracking with parsed content\n              const inboundEmail = await prisma.email.create({\n                data: {\n                  projectId: domainRecord.projectId,\n                  contactId: contact!.id,\n                  subject: body.mail?.commonHeaders?.subject || '(No subject)',\n                  body: htmlBody || '', // Store HTML body in the body field\n                  from: recipientEmail, // The recipient address that received the email\n                  sourceType: EmailSourceType.INBOUND,\n                  status: EmailStatus.RECEIVED, // Inbound emails use RECEIVED status\n                  deliveredAt: new Date(body.mail?.timestamp || new Date()),\n                },\n              });\n\n              // Increment usage counter in cache\n              dataMap.get(domainRecord.id);\n\n              // Record Stripe metering if project has customer\n              if (domainRecord.project.customer) {\n                dataMap.get(domainRecord.id);\n              }\n\n              // Prepare event data with all inbound email details including body content\n              const eventData = {\n                messageId: body.mail?.messageId,\n                from: senderEmail,\n                fromHeader: senderFromHeader,\n                to: recipientEmail,\n                subject: body.mail?.commonHeaders?.subject,\n                timestamp: body.mail?.timestamp,\n                recipients: body.receipt?.recipients,\n                hasContent: !!body.content,\n                // Email body content\n                body: htmlBody,\n                // Security verdicts\n                spamVerdict: body.receipt?.spamVerdict?.status,\n                virusVerdict: body.receipt?.virusVerdict?.status,\n                spfVerdict: body.receipt?.spfVerdict?.status,\n                dkimVerdict: body.receipt?.dkimVerdict?.status,\n                dmarcVerdict: body.receipt?.dmarcVerdict?.status,\n                // Processing metadata\n                processingTimeMillis: body.receipt?.processingTimeMillis,\n              };\n\n              // Create the email.received event (this will trigger workflows)\n              dataMap.get(domainRecord.id);\n\n              signale.success(\n                `[WEBHOOK] Created email.received event for ${senderEmail} → ${recipientEmail} (project: ${domainRecord.project.name})`,\n              );\n            }\n          }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 47.15,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: domain",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for domain and related data\n// Optimized: Single query with include\n\nconst domainWithRelations = await prisma.domain.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const recipient of recipients) {\n            const recipientEmail = recipient as string;\n            const domain = recipientEmail.split('@')[1];\n\n            if (!domain) {\n              signale.warn('[WEBHOOK] Invalid recipient email format:', recipientEmail);\n              continue;\n            }\n\n            // Find ALL projects that have this domain verified\n            // A domain can be shared across multiple projects if users are members of both\n            const domainRecords = await prisma.domain.findMany({\n              where: {\n                domain,\n                verified: true, // Only process emails for verified domains\n              },\n              include: {\n                project: true,\n              },\n            });\n\n            if (domainRecords.length === 0) {\n              signale.info(`[WEBHOOK] No verified domain found for: ${domain}`);\n              continue;\n            }\n\n            signale.info(\n              `[WEBHOOK] Found ${domainRecords.length} project(s) with verified domain ${domain}. Processing inbound email for all.`,\n            );\n\n            // Extract sender information (same for all projects)\n            const senderEmail = body.mail?.source;\n            const senderFromHeader = body.mail?.commonHeaders?.from?.[0] || senderEmail;\n\n            // Parse email content if available\n            let htmlBody: string | undefined;\n\n            if (body.content && typeof body.content === 'string') {\n              try {\n                const isBase64 = body.receipt?.action?.encoding === 'BASE64';\n                const emailBuffer = isBase64\n                  ? Buffer.from(body.content, 'base64')\n                  : Buffer.from(body.content);\n\n                const parsed = await simpleParser(emailBuffer);\n                const raw =\n                  (parsed.html ? String(parsed.html) : undefined) ??\n                  parsed.textAsHtml ??\n                  parsed.text ??\n                  undefined;\n\n                if (raw) {\n                  htmlBody = sanitizeHtml(raw, {\n                    allowedTags: sanitizeHtml.defaults.allowedTags.concat(['img']),\n                    allowedAttributes: {\n                      ...sanitizeHtml.defaults.allowedAttributes,\n                      img: ['src', 'alt', 'width', 'height'],\n                      '*': ['style'],\n                    },\n                    allowedSchemes: ['http', 'https', 'mailto'],\n                  });\n                }\n              } catch (parseError) {\n                signale.error('[WEBHOOK] Failed to parse email content:', parseError);\n              }\n            }\n\n            // Process inbound email for each project that has this domain verified\n            for (const domainRecord of domainRecords) {\n              signale.info(`[WEBHOOK] Processing inbound email for project: ${domainRecord.project.name}`);\n\n              // Check billing limits before processing inbound email\n              const limitCheck = await BillingLimitService.checkLimit(domainRecord.projectId, EmailSourceType.INBOUND);\n\n              if (!limitCheck.allowed) {\n                signale.warn(\n                  `[WEBHOOK] Inbound email blocked for project ${domainRecord.project.name}: ${limitCheck.message}`,\n                );\n                continue; // Skip this project but continue processing for other projects\n              }\n\n              // Find or create a contact for the sender in this project\n              let contact;\n              if (senderEmail) {\n                contact = await ContactService.upsert(\n                  domainRecord.projectId,\n                  senderEmail,\n                  undefined, // No additional data\n                  true, // Subscribe by default for inbound email senders\n                );\n              }\n\n              // Create an Email record for tracking with parsed content\n              const inboundEmail = await prisma.email.create({\n                data: {\n                  projectId: domainRecord.projectId,\n                  contactId: contact!.id,\n                  subject: body.mail?.commonHeaders?.subject || '(No subject)',\n                  body: htmlBody || '', // Store HTML body in the body field\n                  from: recipientEmail, // The recipient address that received the email\n                  sourceType: EmailSourceType.INBOUND,\n                  status: EmailStatus.RECEIVED, // Inbound emails use RECEIVED status\n                  deliveredAt: new Date(body.mail?.timestamp || new Date()),\n                },\n              });\n\n              // Increment usage counter in cache\n              await BillingLimitService.incrementUsage(domainRecord.projectId, EmailSourceType.INBOUND);\n\n              // Record Stripe metering if project has customer\n              if (domainRecord.project.customer) {\n                await MeterService.recordEmailSent(\n                  domainRecord.project.customer,\n                  1, // Inbound emails count as 1 credit\n                  `email_${inboundEmail.id}`,\n                );\n              }\n\n              // Prepare event data with all inbound email details including body content\n              const eventData = {\n                messageId: body.mail?.messageId,\n                from: senderEmail,\n                fromHeader: senderFromHeader,\n                to: recipientEmail,\n                subject: body.mail?.commonHeaders?.subject,\n                timestamp: body.mail?.timestamp,\n                recipients: body.receipt?.recipients,\n                hasContent: !!body.content,\n                // Email body content\n                body: htmlBody,\n                // Security verdicts\n                spamVerdict: body.receipt?.spamVerdict?.status,\n                virusVerdict: body.receipt?.virusVerdict?.status,\n                spfVerdict: body.receipt?.spfVerdict?.status,\n                dkimVerdict: body.receipt?.dkimVerdict?.status,\n                dmarcVerdict: body.receipt?.dmarcVerdict?.status,\n                // Processing metadata\n                processingTimeMillis: body.receipt?.processingTimeMillis,\n              };\n\n              // Create the email.received event (this will trigger workflows)\n              await EventService.trackEvent(\n                domainRecord.projectId,\n                'email.received',\n                contact?.id,\n                inboundEmail.id, // Link the event to the inbound email record\n                eventData,\n              );\n\n              signale.success(\n                `[WEBHOOK] Created email.received event for ${senderEmail} → ${recipientEmail} (project: ${domainRecord.project.name})`,\n              );\n            }\n          }\n*/\n\n// Access related data directly: domainWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/controllers/Webhooks.ts",
   "line": 197,
   "severity": "high",
   "description": "Found 2 database queries (Prisma.upsert(), Prisma.create()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const domainRecord of domainRecords) {\n              signale.info(`[WEBHOOK] Processing inbound email for project: ${domainRecord.project.name}`);\n\n              // Check billing limits before processing inbound email\n              const limitCheck = await BillingLimitService.checkLimit(domainRecord.projectId, EmailSourceType.INBOUND);\n\n              if (!limitCheck.allowed) {\n                signale.warn(\n                  `[WEBHOOK] Inbound email blocked for project ${domainRecord.project.name}: ${limitCheck.message}`,\n                );\n                continue; // Skip this project but continue processing for other projects\n              }\n\n              // Find or create a contact for the sender in this project\n              let contact;\n              if (senderEmail) {\n                contact = await ContactService.upsert(\n                  domainRecord.projectId,\n                  senderEmail,\n                  undefined, // No additional data\n                  true, // Subscribe by default for inbound email senders\n                );\n              }\n\n              // Create an Email record for tracking with parsed content\n              const inboundEmail = await prisma.email.create({\n                data: {\n                  projectId: domainRecord.projectId,\n                  contactId: contact!.id,\n                  subject: body.mail?.commonHeaders?.subject || '(No subject)',\n                  body: htmlBody || '', // Store HTML body in the body field\n                  from: recipientEmail, // The recipient address that received the email\n                  sourceType: EmailSourceType.INBOUND,\n                  status: EmailStatus.RECEIVED, // Inbound emails use RECEIVED status\n                  deliveredAt: new Date(body.mail?.timestamp || new Date()),\n                },\n              });\n\n              // Increment usage counter in cache\n              await BillingLimitService.incrementUsage(domainRecord.projectId, EmailSourceType.INBOUND);\n\n              // Record Stripe metering if project has customer\n              if (domainRecord.project.customer) {\n                await MeterService.recordEmailSent(\n                  domainRecord.project.customer,\n                  1, // Inbound emails count as 1 credit\n                  `email_${inboundEmail.id}`,\n                );\n              }\n\n              // Prepare event data with all inbound email details including body content\n              const eventData = {\n                messageId: body.mail?.messageId,\n                from: senderEmail,\n                fromHeader: senderFromHeader,\n                to: recipientEmail,\n                subject: body.mail?.commonHeaders?.subject,\n                timestamp: body.mail?.timestamp,\n                recipients: body.receipt?.recipients,\n                hasContent: !!body.content,\n                // Email body content\n                body: htmlBody,\n                // Security verdicts\n                spamVerdict: body.receipt?.spamVerdict?.status,\n                virusVerdict: body.receipt?.virusVerdict?.status,\n                spfVerdict: body.receipt?.spfVerdict?.status,\n                dkimVerdict: body.receipt?.dkimVerdict?.status,\n                dmarcVerdict: body.receipt?.dmarcVerdict?.status,\n                // Processing metadata\n                processingTimeMillis: body.receipt?.processingTimeMillis,\n              };\n\n              // Create the email.received event (this will trigger workflows)\n              await EventService.trackEvent(\n                domainRecord.projectId,\n                'email.received',\n                contact?.id,\n                inboundEmail.id, // Link the event to the inbound email record\n                eventData,\n              );\n\n              signale.success(\n                `[WEBHOOK] Created email.received event for ${senderEmail} → ${recipientEmail} (project: ${domainRecord.project.name})`,\n              );\n            }",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 66.35,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: domainRecord, domainRecords",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 6 async call(s) inside loop over domainRecords\n// Problem: N+1 queries where N = domainRecords.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = domainRecords.map(domainRecord => domainRecord.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const domainRecord of domainRecords) {\n              signale.info(`[WEBHOOK] Processing inbound email for project: ${domainRecord.project.name}`);\n\n              // Check billing limits before processing inbound email\n              const limitCheck = dataMap.get(domainRecord.id);\n\n              if (!limitCheck.allowed) {\n                signale.warn(\n                  `[WEBHOOK] Inbound email blocked for project ${domainRecord.project.name}: ${limitCheck.message}`,\n                );\n                continue; // Skip this project but continue processing for other projects\n              }\n\n              // Find or create a contact for the sender in this project\n              let contact;\n              if (senderEmail) {\n                contact = dataMap.get(domainRecord.id);\n              }\n\n              // Create an Email record for tracking with parsed content\n              const inboundEmail = await prisma.email.create({\n                data: {\n                  projectId: domainRecord.projectId,\n                  contactId: contact!.id,\n                  subject: body.mail?.commonHeaders?.subject || '(No subject)',\n                  body: htmlBody || '', // Store HTML body in the body field\n                  from: recipientEmail, // The recipient address that received the email\n                  sourceType: EmailSourceType.INBOUND,\n                  status: EmailStatus.RECEIVED, // Inbound emails use RECEIVED status\n                  deliveredAt: new Date(body.mail?.timestamp || new Date()),\n                },\n              });\n\n              // Increment usage counter in cache\n              dataMap.get(domainRecord.id);\n\n              // Record Stripe metering if project has customer\n              if (domainRecord.project.customer) {\n                dataMap.get(domainRecord.id);\n              }\n\n              // Prepare event data with all inbound email details including body content\n              const eventData = {\n                messageId: body.mail?.messageId,\n                from: senderEmail,\n                fromHeader: senderFromHeader,\n                to: recipientEmail,\n                subject: body.mail?.commonHeaders?.subject,\n                timestamp: body.mail?.timestamp,\n                recipients: body.receipt?.recipients,\n                hasContent: !!body.content,\n                // Email body content\n                body: htmlBody,\n                // Security verdicts\n                spamVerdict: body.receipt?.spamVerdict?.status,\n                virusVerdict: body.receipt?.virusVerdict?.status,\n                spfVerdict: body.receipt?.spfVerdict?.status,\n                dkimVerdict: body.receipt?.dkimVerdict?.status,\n                dmarcVerdict: body.receipt?.dmarcVerdict?.status,\n                // Processing metadata\n                processingTimeMillis: body.receipt?.processingTimeMillis,\n              };\n\n              // Create the email.received event (this will trigger workflows)\n              dataMap.get(domainRecord.id);\n\n              signale.success(\n                `[WEBHOOK] Created email.received event for ${senderEmail} → ${recipientEmail} (project: ${domainRecord.project.name})`,\n              );\n            }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "prisma-include",
     "rank": 2,
     "fitnessScore": 63.699999999999996,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: email",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for email and related data\n// Optimized: Single query with include\n\nconst emailWithRelations = await prisma.email.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nfor (const domainRecord of domainRecords) {\n              signale.info(`[WEBHOOK] Processing inbound email for project: ${domainRecord.project.name}`);\n\n              // Check billing limits before processing inbound email\n              const limitCheck = await BillingLimitService.checkLimit(domainRecord.projectId, EmailSourceType.INBOUND);\n\n              if (!limitCheck.allowed) {\n                signale.warn(\n                  `[WEBHOOK] Inbound email blocked for project ${domainRecord.project.name}: ${limitCheck.message}`,\n                );\n                continue; // Skip this project but continue processing for other projects\n              }\n\n              // Find or create a contact for the sender in this project\n              let contact;\n              if (senderEmail) {\n                contact = await ContactService.upsert(\n                  domainRecord.projectId,\n                  senderEmail,\n                  undefined, // No additional data\n                  true, // Subscribe by default for inbound email senders\n                );\n              }\n\n              // Create an Email record for tracking with parsed content\n              const inboundEmail = await prisma.email.create({\n                data: {\n                  projectId: domainRecord.projectId,\n                  contactId: contact!.id,\n                  subject: body.mail?.commonHeaders?.subject || '(No subject)',\n                  body: htmlBody || '', // Store HTML body in the body field\n                  from: recipientEmail, // The recipient address that received the email\n                  sourceType: EmailSourceType.INBOUND,\n                  status: EmailStatus.RECEIVED, // Inbound emails use RECEIVED status\n                  deliveredAt: new Date(body.mail?.timestamp || new Date()),\n                },\n              });\n\n              // Increment usage counter in cache\n              await BillingLimitService.incrementUsage(domainRecord.projectId, EmailSourceType.INBOUND);\n\n              // Record Stripe metering if project has customer\n              if (domainRecord.project.customer) {\n                await MeterService.recordEmailSent(\n                  domainRecord.project.customer,\n                  1, // Inbound emails count as 1 credit\n                  `email_${inboundEmail.id}`,\n                );\n              }\n\n              // Prepare event data with all inbound email details including body content\n              const eventData = {\n                messageId: body.mail?.messageId,\n                from: senderEmail,\n                fromHeader: senderFromHeader,\n                to: recipientEmail,\n                subject: body.mail?.commonHeaders?.subject,\n                timestamp: body.mail?.timestamp,\n                recipients: body.receipt?.recipients,\n                hasContent: !!body.content,\n                // Email body content\n                body: htmlBody,\n                // Security verdicts\n                spamVerdict: body.receipt?.spamVerdict?.status,\n                virusVerdict: body.receipt?.virusVerdict?.status,\n                spfVerdict: body.receipt?.spfVerdict?.status,\n                dkimVerdict: body.receipt?.dkimVerdict?.status,\n                dmarcVerdict: body.receipt?.dmarcVerdict?.status,\n                // Processing metadata\n                processingTimeMillis: body.receipt?.processingTimeMillis,\n              };\n\n              // Create the email.received event (this will trigger workflows)\n              await EventService.trackEvent(\n                domainRecord.projectId,\n                'email.received',\n                contact?.id,\n                inboundEmail.id, // Link the event to the inbound email record\n                eventData,\n              );\n\n              signale.success(\n                `[WEBHOOK] Created email.received event for ${senderEmail} → ${recipientEmail} (project: ${domainRecord.project.name})`,\n              );\n            }\n*/\n\n// Access related data directly: emailWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "apps/api/src/controllers/Actions.ts",
   "line": 282,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.upsert()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const recipient of recipients) {\n      // Merge recipient name with data if provided\n      const recipientData = recipient.name\n        ? {...(data as Record<string, unknown> | undefined), name: recipient.name}\n        : (data as Record<string, unknown> | undefined);\n\n      // Create or update contact with metadata\n      // Transactional emails should not subscribe contacts by default\n      // New contacts default to unsubscribed unless explicitly opted in\n      // Existing contacts preserve their subscription state unless explicitly changed\n      const contact = await ContactService.upsert(auth.projectId, recipient.email, recipientData, subscribed, false);\n\n      // Get merged data including non-persistent fields for template rendering\n      const mergedData = ContactService.getMergedData(contact, data as Record<string, unknown> | undefined);\n\n      // Add system variables (email, unsubscribe URLs, etc.) to merged data\n      // These are always available for template rendering\n      const dataWithSystemVars = {\n        ...mergedData,\n        id: contact.id,\n        email: contact.email,\n        data: mergedData, // Also available as nested data for {{data.fieldName}} syntax\n        unsubscribeUrl: `${DASHBOARD_URI}/unsubscribe/${contact.id}`,\n        subscribeUrl: `${DASHBOARD_URI}/subscribe/${contact.id}`,\n        manageUrl: `${DASHBOARD_URI}/manage/${contact.id}`,\n      };\n\n      // Render template with contact data\n      // Simple template variable replacement: {{fieldname}}\n      let renderedSubject = emailSubject!;\n      let renderedBody = emailBody!;\n\n      for (const [key, value] of Object.entries(dataWithSystemVars)) {\n        const placeholder = new RegExp(`\\\\{\\\\{\\\\s*${key}\\\\s*\\\\}\\\\}`, 'g');\n        const fallbackPlaceholder = new RegExp(`\\\\{\\\\{\\\\s*${key}\\\\s*\\\\?\\\\?\\\\s*([^}]+)\\\\}\\\\}`, 'g');\n\n        // Replace with value\n        const stringValue = value !== null && value !== undefined ? String(value) : '';\n        renderedSubject = renderedSubject!.replace(placeholder, stringValue);\n        renderedBody = renderedBody!.replace(placeholder, stringValue);\n\n        // Handle fallback syntax: {{field ?? default}}\n        renderedSubject = renderedSubject!.replace(fallbackPlaceholder, stringValue || '$1');\n        renderedBody = renderedBody!.replace(fallbackPlaceholder, stringValue || '$1');\n      }\n\n      // Replace any remaining placeholders with empty string or fallback value\n      renderedSubject = renderedSubject!.replace(/\\{\\{\\s*(\\w+)\\s*\\}\\}/g, '');\n      renderedBody = renderedBody!.replace(/\\{\\{\\s*(\\w+)\\s*\\}\\}/g, '');\n\n      // Handle fallback placeholders that weren't matched\n      renderedSubject = renderedSubject!.replace(/\\{\\{\\s*\\w+\\s*\\?\\?\\s*([^}]+)\\}\\}/g, '$1');\n      renderedBody = renderedBody!.replace(/\\{\\{\\s*\\w+\\s*\\?\\?\\s*([^}]+)\\}\\}/g, '$1');\n\n      const email = await EmailService.sendTransactionalEmail({\n        projectId: auth.projectId,\n        contactId: contact.id,\n        subject: renderedSubject,\n        body: renderedBody,\n        from: emailFrom,\n        fromName: emailFromName,\n        toName: recipient.name,\n        replyTo: replyToEmail,\n        headers: headers || undefined,\n        attachments: attachments || undefined,\n        templateId: templateId,\n      });\n\n      emailResults.push({\n        contact: {\n          id: contact.id,\n          email: contact.email,\n        },\n        email: email.id,\n      });\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 73.05,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: , Object.entries(dataWithSystemVars)",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 2 async call(s) inside loop over Object.entries(dataWithSystemVars)\n// Problem: N+1 queries where N = Object.entries(dataWithSystemVars).length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = Object.entries(dataWithSystemVars).map( => .id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const recipient of recipients) {\n      // Merge recipient name with data if provided\n      const recipientData = recipient.name\n        ? {...(data as Record<string, unknown> | undefined), name: recipient.name}\n        : (data as Record<string, unknown> | undefined);\n\n      // Create or update contact with metadata\n      // Transactional emails should not subscribe contacts by default\n      // New contacts default to unsubscribed unless explicitly opted in\n      // Existing contacts preserve their subscription state unless explicitly changed\n      const contact = dataMap.get(.id);\n\n      // Get merged data including non-persistent fields for template rendering\n      const mergedData = ContactService.getMergedData(contact, data as Record<string, unknown> | undefined);\n\n      // Add system variables (email, unsubscribe URLs, etc.) to merged data\n      // These are always available for template rendering\n      const dataWithSystemVars = {\n        ...mergedData,\n        id: contact.id,\n        email: contact.email,\n        data: mergedData, // Also available as nested data for {{data.fieldName}} syntax\n        unsubscribeUrl: `${DASHBOARD_URI}/unsubscribe/${contact.id}`,\n        subscribeUrl: `${DASHBOARD_URI}/subscribe/${contact.id}`,\n        manageUrl: `${DASHBOARD_URI}/manage/${contact.id}`,\n      };\n\n      // Render template with contact data\n      // Simple template variable replacement: {{fieldname}}\n      let renderedSubject = emailSubject!;\n      let renderedBody = emailBody!;\n\n      for (const [key, value] of Object.entries(dataWithSystemVars)) {\n        const placeholder = new RegExp(`\\\\{\\\\{\\\\s*${key}\\\\s*\\\\}\\\\}`, 'g');\n        const fallbackPlaceholder = new RegExp(`\\\\{\\\\{\\\\s*${key}\\\\s*\\\\?\\\\?\\\\s*([^}]+)\\\\}\\\\}`, 'g');\n\n        // Replace with value\n        const stringValue = value !== null && value !== undefined ? String(value) : '';\n        renderedSubject = renderedSubject!.replace(placeholder, stringValue);\n        renderedBody = renderedBody!.replace(placeholder, stringValue);\n\n        // Handle fallback syntax: {{field ?? default}}\n        renderedSubject = renderedSubject!.replace(fallbackPlaceholder, stringValue || '$1');\n        renderedBody = renderedBody!.replace(fallbackPlaceholder, stringValue || '$1');\n      }\n\n      // Replace any remaining placeholders with empty string or fallback value\n      renderedSubject = renderedSubject!.replace(/\\{\\{\\s*(\\w+)\\s*\\}\\}/g, '');\n      renderedBody = renderedBody!.replace(/\\{\\{\\s*(\\w+)\\s*\\}\\}/g, '');\n\n      // Handle fallback placeholders that weren't matched\n      renderedSubject = renderedSubject!.replace(/\\{\\{\\s*\\w+\\s*\\?\\?\\s*([^}]+)\\}\\}/g, '$1');\n      renderedBody = renderedBody!.replace(/\\{\\{\\s*\\w+\\s*\\?\\?\\s*([^}]+)\\}\\}/g, '$1');\n\n      const email = dataMap.get(.id);\n\n      emailResults.push({\n        contact: {\n          id: contact.id,\n          email: contact.email,\n        },\n        email: email.id,\n      });\n    }\n\n// Performance: 1 query instead of N queries"
    }
   ]
  }
 ]
}