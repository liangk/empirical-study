{
 "filesScanned": 334,
 "n1Findings": 38,
 "findings": [
  {
   "file": "server/commands/teamCreator.ts",
   "line": 69,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findOne()) inside a for loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (;;) {\n    const existing = await Team.findOne({\n      where: { subdomain },\n      paranoid: false,\n      transaction: ctx.state.transaction,\n    });\n\n    if (existing) {\n      // subdomain was invalid or already used, try another\n      subdomain = `${baseSubdomain}${++append}`;\n    } else {\n      break;\n    }\n  }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 82.1,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: , ",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over \n// Problem: N+1 queries where N = .length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = .map( => .id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (;;) {\n    const existing = dataMap.get(.id);\n\n    if (existing) {\n      // subdomain was invalid or already used, try another\n      subdomain = `${baseSubdomain}${++append}`;\n    } else {\n      break;\n    }\n  }\n\n// Performance: 1 query instead of N queries"
    }
   ]
  },
  {
   "file": "server/commands/groupsSyncer.ts",
   "line": 52,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const eg of externalGroups) {\n    externalGroupIds.add(eg.id);\n\n    // Upsert ExternalGroup record\n    const [externalGroup, created] = await ExternalGroup.findOrCreate({\n      where: {\n        authenticationProviderId: authenticationProvider.id,\n        externalId: eg.id,\n      },\n      defaults: {\n        name: eg.name,\n        teamId: team.id,\n        lastSyncedAt: now,\n      },\n      transaction,\n    });\n\n    // Update name if changed, and always update lastSyncedAt\n    if (!created) {\n      const updates: Partial<{ name: string; lastSyncedAt: Date }> = {\n        lastSyncedAt: now,\n      };\n      if (externalGroup.name !== eg.name) {\n        updates.name = eg.name;\n\n        // Also update the linked internal Group name\n        if (externalGroup.groupId) {\n          const group = await Group.findByPk(externalGroup.groupId, {\n            transaction,\n          });\n          if (group) {\n            await group.update({ name: eg.name }, { transaction });\n          }\n        }\n      }\n      await externalGroup.update(updates, { transaction });\n    }\n\n    // Auto-create internal Group if one doesn't exist yet\n    if (!externalGroup.groupId) {\n      const group = await Group.createWithCtx(ctx, {\n        name: eg.name,\n        teamId: team.id,\n        createdById: user.id,\n      });\n      await externalGroup.update({ groupId: group.id }, { transaction });\n      externalGroup.groupId = group.id;\n      result.groupsCreated++;\n    }\n\n    // Add user to group if not already a member\n    const [, membershipCreated] = await GroupUser.findOrCreateWithCtx(ctx, {\n      where: { groupId: externalGroup.groupId!, userId: user.id },\n      defaults: { createdById: user.id },\n    });\n\n    if (membershipCreated) {\n      result.membershipsAdded++;\n    }\n  }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 78.6,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: eg, externalGroups",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 7 async call(s) inside loop over externalGroups\n// Problem: N+1 queries where N = externalGroups.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = externalGroups.map(eg => eg.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const eg of externalGroups) {\n    externalGroupIds.add(eg.id);\n\n    // Upsert ExternalGroup record\n    const [externalGroup, created] = dataMap.get(eg.id);\n\n    // Update name if changed, and always update lastSyncedAt\n    if (!created) {\n      const updates: Partial<{ name: string; lastSyncedAt: Date }> = {\n        lastSyncedAt: now,\n      };\n      if (externalGroup.name !== eg.name) {\n        updates.name = eg.name;\n\n        // Also update the linked internal Group name\n        if (externalGroup.groupId) {\n          const group = dataMap.get(eg.id);\n          if (group) {\n            dataMap.get(eg.id);\n          }\n        }\n      }\n      dataMap.get(eg.id);\n    }\n\n    // Auto-create internal Group if one doesn't exist yet\n    if (!externalGroup.groupId) {\n      const group = dataMap.get(eg.id);\n      dataMap.get(eg.id);\n      externalGroup.groupId = group.id;\n      result.groupsCreated++;\n    }\n\n    // Add user to group if not already a member\n    const [, membershipCreated] = dataMap.get(eg.id);\n\n    if (membershipCreated) {\n      result.membershipsAdded++;\n    }\n  }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 62.95,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const eg of externalGroups) {\n    externalGroupIds.add(eg.id);\n\n    // Upsert ExternalGroup record\n    const [externalGroup, created] = await ExternalGroup.findOrCreate({\n      where: {\n        authenticationProviderId: authenticationProvider.id,\n        externalId: eg.id,\n      },\n      defaults: {\n        name: eg.name,\n        teamId: team.id,\n        lastSyncedAt: now,\n      },\n      transaction,\n    });\n\n    // Update name if changed, and always update lastSyncedAt\n    if (!created) {\n      const updates: Partial<{ name: string; lastSyncedAt: Date }> = {\n        lastSyncedAt: now,\n      };\n      if (externalGroup.name !== eg.name) {\n        updates.name = eg.name;\n\n        // Also update the linked internal Group name\n        if (externalGroup.groupId) {\n          const group = await Group.findByPk(externalGroup.groupId, {\n            transaction,\n          });\n          if (group) {\n            await group.update({ name: eg.name }, { transaction });\n          }\n        }\n      }\n      await externalGroup.update(updates, { transaction });\n    }\n\n    // Auto-create internal Group if one doesn't exist yet\n    if (!externalGroup.groupId) {\n      const group = await Group.createWithCtx(ctx, {\n        name: eg.name,\n        teamId: team.id,\n        createdById: user.id,\n      });\n      await externalGroup.update({ groupId: group.id }, { transaction });\n      externalGroup.groupId = group.id;\n      result.groupsCreated++;\n    }\n\n    // Add user to group if not already a member\n    const [, membershipCreated] = await GroupUser.findOrCreateWithCtx(ctx, {\n      where: { groupId: externalGroup.groupId!, userId: user.id },\n      defaults: { createdById: user.id },\n    });\n\n    if (membershipCreated) {\n      result.membershipsAdded++;\n    }\n  }\n*/\n\n// Access: modelWithRelations[0].relations"
    },
    {
     "type": "memoization",
     "rank": 3,
     "fitnessScore": 60.7,
     "reasoning": "Add memoization cache for repeated expensive calls\nPreserved: externalGroup.update",
     "code": "// OPTIMIZED: Memoization for repeated expensive calls\n// Found 1 repeated async call(s)\n\n// Memoize externalGroup.update (called 2x)\nconst updateCache = new Map();\nasync function memoizedUpdate(key) {\n  if (updateCache.has(key)) return updateCache.get(key);\n  const result = await externalGroup.update(key);\n  updateCache.set(key, result);\n  return result;\n}\n\n// Original code (replace calls with memoized versions):\nfor (const eg of externalGroups) {\n    externalGroupIds.add(eg.id);\n\n    // Upsert ExternalGroup record\n    const [externalGroup, created] = await ExternalGroup.findOrCreate({\n      where: {\n        authenticationProviderId: authenticationProvider.id,\n        externalId: eg.id,\n      },\n      defaults: {\n        name: eg.name,\n        teamId: team.id,\n        lastSyncedAt: now,\n      },\n      transaction,\n    });\n\n    // Update name if changed, and always update lastSyncedAt\n    if (!created) {\n      const updates: Partial<{ name: string; lastSyncedAt: Date }> = {\n        lastSyncedAt: now,\n      };\n      if (externalGroup.name !== eg.name) {\n        updates.name = eg.name;\n\n        // Also update the linked internal Group name\n        if (externalGroup.groupId) {\n          const group = await Group.findByPk(externalGroup.groupId, {\n            transaction,\n          });\n          if (group) {\n            await group.update({ name: eg.name }, { transaction });\n          }\n        }\n      }\n      await externalGroup.update(updates, { transaction });\n    }\n\n    // Auto-create internal Group if one doesn't exist yet\n    if (!externalGroup.groupId) {\n      const group = await Group.createWithCtx(ctx, {\n        name: eg.name,\n        teamId: team.id,\n        createdById: user.id,\n      });\n      await externalGroup.update({ groupId: group.id }, { transaction });\n      externalGroup.groupId = group.id;\n      result.groupsCreated++;\n    }\n\n    // Add user to group if not already a member\n    const [, membershipCreated] = await GroupUser.findOrCreateWithCtx(ctx, {\n      where: { groupId: externalGroup.groupId!, userId: user.id },\n      defaults: { createdById: user.id },\n    });\n\n    if (membershipCreated) {\n      result.membershipsAdded++;\n    }\n  }"
    },
    {
     "type": "memoize",
     "rank": 4,
     "fitnessScore": 59.95,
     "reasoning": "Added memoization for 1 repeated calls",
     "code": "const updateCache = new Map(); // Memoize externalGroup.update\n\nfor (const eg of externalGroups) {\n    externalGroupIds.add(eg.id);\n\n    // Upsert ExternalGroup record\n    const [externalGroup, created] = await ExternalGroup.findOrCreate({\n      where: {\n        authenticationProviderId: authenticationProvider.id,\n        externalId: eg.id,\n      },\n      defaults: {\n        name: eg.name,\n        teamId: team.id,\n        lastSyncedAt: now,\n      },\n      transaction,\n    });\n\n    // Update name if changed, and always update lastSyncedAt\n    if (!created) {\n      const updates: Partial<{ name: string; lastSyncedAt: Date }> = {\n        lastSyncedAt: now,\n      };\n      if (externalGroup.name !== eg.name) {\n        updates.name = eg.name;\n\n        // Also update the linked internal Group name\n        if (externalGroup.groupId) {\n          const group = await Group.findByPk(externalGroup.groupId, {\n            transaction,\n          });\n          if (group) {\n            await group.update({ name: eg.name }, { transaction });\n          }\n        }\n      }\n      await externalGroup.update(updates, { transaction });\n    }\n\n    // Auto-create internal Group if one doesn't exist yet\n    if (!externalGroup.groupId) {\n      const group = await Group.createWithCtx(ctx, {\n        name: eg.name,\n        teamId: team.id,\n        createdById: user.id,\n      });\n      await externalGroup.update({ groupId: group.id }, { transaction });\n      externalGroup.groupId = group.id;\n      result.groupsCreated++;\n    }\n\n    // Add user to group if not already a member\n    const [, membershipCreated] = await GroupUser.findOrCreateWithCtx(ctx, {\n      where: { groupId: externalGroup.groupId!, userId: user.id },\n      defaults: { createdById: user.id },\n    });\n\n    if (membershipCreated) {\n      result.membershipsAdded++;\n    }\n  }"
    }
   ]
  },
  {
   "file": "server/commands/documentPermanentDeleter.ts",
   "line": 28,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findAll()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const document of documents) {\n    // Find any attachments that are referenced in the text content\n    const attachmentIdsInText = ProsemirrorHelper.parseAttachmentIds(\n      DocumentHelper.toProsemirror(document)\n    );\n\n    // Find any attachments that were originally uploaded to this document\n    const attachmentIdsForDocument = (\n      await Attachment.findAll({\n        attributes: [\"id\"],\n        where: {\n          teamId: document.teamId,\n          documentId: document.id,\n        },\n      })\n    ).map((attachment) => attachment.id);\n\n    const attachmentIds = uniq([\n      ...attachmentIdsInText,\n      ...attachmentIdsForDocument,\n    ]);\n\n    await Promise.all(\n      attachmentIds.map(async (attachmentId) => {\n        // Check if the attachment is referenced in any other documents – this\n        // is needed as it's easy to copy and paste content between documents.\n        // An uploaded attachment may end up referenced in multiple documents.\n        const [{ count }] = await sequelizeReadOnly.query<{ count: string }>(\n          query,\n          {\n            type: QueryTypes.SELECT,\n            replacements: {\n              documentId: document.id,\n              teamId: document.teamId,\n              query: attachmentId,\n            },\n          }\n        );\n\n        // If the attachment is not referenced in any other documents then\n        // delete it from the database and the storage provider.\n        if (parseInt(count) === 0) {\n          Logger.info(\n            \"commands\",\n            `Attachment ${attachmentId} scheduled for deletion`\n          );\n          await new DeleteAttachmentTask().schedule({\n            attachmentId,\n            teamId: document.teamId,\n          });\n        }\n      })\n    );\n  }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 77.3,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: document, documents",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 4 async call(s) inside loop over documents\n// Problem: N+1 queries where N = documents.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = documents.map(document => document.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const document of documents) {\n    // Find any attachments that are referenced in the text content\n    const attachmentIdsInText = ProsemirrorHelper.parseAttachmentIds(\n      DocumentHelper.toProsemirror(document)\n    );\n\n    // Find any attachments that were originally uploaded to this document\n    const attachmentIdsForDocument = (\n      dataMap.get(document.id)\n    ).map((attachment) => attachment.id);\n\n    const attachmentIds = uniq([\n      ...attachmentIdsInText,\n      ...attachmentIdsForDocument,\n    ]);\n\n    dataMap.get(document.id) => {\n        // Check if the attachment is referenced in any other documents – this\n        // is needed as it's easy to copy and paste content between documents.\n        // An uploaded attachment may end up referenced in multiple documents.\n        const [{ count }] = await sequelizeReadOnly.query<{ count: string }>(\n          query,\n          {\n            type: QueryTypes.SELECT,\n            replacements: {\n              documentId: document.id,\n              teamId: document.teamId,\n              query: attachmentId,\n            },\n          }\n        );\n\n        // If the attachment is not referenced in any other documents then\n        // delete it from the database and the storage provider.\n        if (parseInt(count) === 0) {\n          Logger.info(\n            \"commands\",\n            `Attachment ${attachmentId} scheduled for deletion`\n          );\n          await new DeleteAttachmentTask().schedule({\n            attachmentId,\n            teamId: document.teamId,\n          });\n        }\n      })\n    );\n  }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 76.15,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: Attachment",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for Attachment\n// Optimized: Single query with include\n\nconst attachmentWithRelations = await Attachment.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const document of documents) {\n    // Find any attachments that are referenced in the text content\n    const attachmentIdsInText = ProsemirrorHelper.parseAttachmentIds(\n      DocumentHelper.toProsemirror(document)\n    );\n\n    // Find any attachments that were originally uploaded to this document\n    const attachmentIdsForDocument = (\n      await Attachment.findAll({\n        attributes: [\"id\"],\n        where: {\n          teamId: document.teamId,\n          documentId: document.id,\n        },\n      })\n    ).map((attachment) => attachment.id);\n\n    const attachmentIds = uniq([\n      ...attachmentIdsInText,\n      ...attachmentIdsForDocument,\n    ]);\n\n    await Promise.all(\n      attachmentIds.map(async (attachmentId) => {\n        // Check if the attachment is referenced in any other documents – this\n        // is needed as it's easy to copy and paste content between documents.\n        // An uploaded attachment may end up referenced in multiple documents.\n        const [{ count }] = await sequelizeReadOnly.query<{ count: string }>(\n          query,\n          {\n            type: QueryTypes.SELECT,\n            replacements: {\n              documentId: document.id,\n              teamId: document.teamId,\n              query: attachmentId,\n            },\n          }\n        );\n\n        // If the attachment is not referenced in any other documents then\n        // delete it from the database and the storage provider.\n        if (parseInt(count) === 0) {\n          Logger.info(\n            \"commands\",\n            `Attachment ${attachmentId} scheduled for deletion`\n          );\n          await new DeleteAttachmentTask().schedule({\n            attachmentId,\n            teamId: document.teamId,\n          });\n        }\n      })\n    );\n  }\n*/\n\n// Access: attachmentWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/commands/documentPermanentDeleter.ts",
   "line": 35,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findAll()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "(\n      await Attachment.findAll({\n        attributes: [\"id\"],\n        where: {\n          teamId: document.teamId,\n          documentId: document.id,\n        },\n      })\n    ).map((attachment) => attachment.id)",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "sequelize-include",
     "rank": 1,
     "fitnessScore": 81.69999999999999,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: Attachment",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for Attachment\n// Optimized: Single query with include\n\nconst attachmentWithRelations = await Attachment.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\n(\n      await Attachment.findAll({\n        attributes: [\"id\"],\n        where: {\n          teamId: document.teamId,\n          documentId: document.id,\n        },\n      })\n    ).map((attachment) => attachment.id)\n*/\n\n// Access: attachmentWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/policies/cancan.ts",
   "line": 79,
   "severity": "high",
   "description": "Found 2 database queries (Unknown.get(), Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "(this.toArray(actions) as string[]).forEach((action) => {\n      (this.toArray(targets) as T[]).forEach((target) => {\n        const ability = { model, action, target, condition } as Ability;\n\n        // Add to index\n        if (!this.abilities.has(model)) {\n          this.abilities.set(model, new Map());\n        }\n        const actionMap = this.abilities.get(model)!;\n        if (!actionMap.has(action)) {\n          actionMap.set(action, []);\n        }\n        actionMap.get(action)!.push(ability);\n      });\n    })",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 82.55,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: ability, actionMap\n// Method calls: this.toArray(actions) as string[].forEach(), this.toArray(), this.toArray(targets) as T[].forEach(), this.toArray(), this.abilities.has(), this.abilities.set(), this.abilities.get(), actionMap.has(), actionMap.set(), actionMap.get(action)!.push(), actionMap.get()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\n(this.toArray(actions) as string[]).forEach((action) => {\n      (this.toArray(targets) as T[]).forEach((target) => {\n        const ability = { model, action, target, condition } as Ability;\n\n        // Add to index\n        if (!this.abilities.has(model)) {\n          this.abilities.set(model, new Map());\n        }\n        const actionMap = this.abilities.get(model)!;\n        if (!actionMap.has(action)) {\n          actionMap.set(action, []);\n        }\n        actionMap.get(action)!.push(ability);\n      });\n    })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "server/policies/cancan.ts",
   "line": 80,
   "severity": "high",
   "description": "Found 2 database queries (Unknown.get(), Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "(this.toArray(targets) as T[]).forEach((target) => {\n        const ability = { model, action, target, condition } as Ability;\n\n        // Add to index\n        if (!this.abilities.has(model)) {\n          this.abilities.set(model, new Map());\n        }\n        const actionMap = this.abilities.get(model)!;\n        if (!actionMap.has(action)) {\n          actionMap.set(action, []);\n        }\n        actionMap.get(action)!.push(ability);\n      })",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.15,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: ability, actionMap\n// Method calls: this.toArray(targets) as T[].forEach(), this.toArray(), this.abilities.has(), this.abilities.set(), this.abilities.get(), actionMap.has(), actionMap.set(), actionMap.get(action)!.push(), actionMap.get()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\n(this.toArray(targets) as T[]).forEach((target) => {\n        const ability = { model, action, target, condition } as Ability;\n\n        // Add to index\n        if (!this.abilities.has(model)) {\n          this.abilities.set(model, new Map());\n        }\n        const actionMap = this.abilities.get(model)!;\n        if (!actionMap.has(action)) {\n          actionMap.set(action, []);\n        }\n        actionMap.get(action)!.push(ability);\n      })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "server/policies/cancan.ts",
   "line": 334,
   "severity": "high",
   "description": "Found 2 database queries (Unknown.get(), Unknown.get()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const [model, actionMap] of this.abilities.entries()) {\n      if (!(performer instanceof model)) {\n        continue;\n      }\n\n      // Check for specific action\n      const specificAbilities = actionMap.get(action);\n      if (specificAbilities) {\n        for (const ability of specificAbilities) {\n          if (\n            ability.target === \"all\" ||\n            target === ability.target ||\n            target instanceof (ability.target as Constructor)\n          ) {\n            matchingAbilities.push(ability);\n          }\n        }\n      }\n\n      // Check for \"manage\" action (applies to all actions)\n      const manageAbilities = actionMap.get(\"manage\");\n      if (manageAbilities) {\n        for (const ability of manageAbilities) {\n          if (\n            ability.target === \"all\" ||\n            target === ability.target ||\n            target instanceof (ability.target as Constructor)\n          ) {\n            matchingAbilities.push(ability);\n          }\n        }\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 74.35,
     "reasoning": "Pattern analysis for loop-with-calls with angular-forms framework",
     "code": "// Analysis of original code:\n// Pattern type: loop-with-calls\n// ORM/Framework: angular-forms\n// Variables: specificAbilities, ability, manageAbilities\n// Method calls: this.abilities.entries(), actionMap.get(), matchingAbilities.push(), actionMap.get(), matchingAbilities.push()\n// Loop structure: for-of over manageAbilities\n// Async calls: 0\n\n// Original code:\nfor (const [model, actionMap] of this.abilities.entries()) {\n      if (!(performer instanceof model)) {\n        continue;\n      }\n\n      // Check for specific action\n      const specificAbilities = actionMap.get(action);\n      if (specificAbilities) {\n        for (const ability of specificAbilities) {\n          if (\n            ability.target === \"all\" ||\n            target === ability.target ||\n            target instanceof (ability.target as Constructor)\n          ) {\n            matchingAbilities.push(ability);\n          }\n        }\n      }\n\n      // Check for \"manage\" action (applies to all actions)\n      const manageAbilities = actionMap.get(\"manage\");\n      if (manageAbilities) {\n        for (const ability of manageAbilities) {\n          if (\n            ability.target === \"all\" ||\n            target === ability.target ||\n            target instanceof (ability.target as Constructor)\n          ) {\n            matchingAbilities.push(ability);\n          }\n        }\n      }\n    }\n\n// Recommended optimizations based on pattern:\n1. Extract queries before the loop\n// 2. Use batch queries with IN clause\n// 3. Build a Map for O(1) lookups inside the loop\n// - Use getRawValue() instead of multiple get() calls\n// - Cache form values before mapping"
    }
   ]
  },
  {
   "file": "server/queues/tasks/UploadAttachmentsForImportTask.ts",
   "line": 29,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "items.map(async (item) => {\n      try {\n        await sema.acquire();\n\n        const attachment = await Attachment.findByPk(item.attachmentId, {\n          rejectOnEmpty: true,\n        });\n\n        // This means the attachment has already been uploaded.\n        if (String(attachment.size) !== \"0\") {\n          return;\n        }\n\n        const res = await FileStorage.storeFromUrl(\n          item.url,\n          attachment.key,\n          attachment.acl,\n          {\n            timeout: env.FILE_STORAGE_IMPORT_TIMEOUT,\n          }\n        );\n\n        if (res) {\n          await attachment.update({\n            size: res.contentLength,\n            contentType: res.contentType,\n          });\n        }\n      } catch (err) {\n        Logger.error(\"error uploading attachments for import\", toError(err));\n        throw err;\n      } finally {\n        sema.release();\n      }\n    })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "sequelize-include",
     "rank": 1,
     "fitnessScore": 84.5,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nitems.map(async (item) => {\n      try {\n        await sema.acquire();\n\n        const attachment = await Attachment.findByPk(item.attachmentId, {\n          rejectOnEmpty: true,\n        });\n\n        // This means the attachment has already been uploaded.\n        if (String(attachment.size) !== \"0\") {\n          return;\n        }\n\n        const res = await FileStorage.storeFromUrl(\n          item.url,\n          attachment.key,\n          attachment.acl,\n          {\n            timeout: env.FILE_STORAGE_IMPORT_TIMEOUT,\n          }\n        );\n\n        if (res) {\n          await attachment.update({\n            size: res.contentLength,\n            contentType: res.contentType,\n          });\n        }\n      } catch (err) {\n        Logger.error(\"error uploading attachments for import\", toError(err));\n        throw err;\n      } finally {\n        sema.release();\n      }\n    })\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/RevokeUserNotificationsTask.ts",
   "line": 90,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const id of collectionIds) {\n      const collection = await Collection.findByPk(id, { userId: user.id });\n      if (collection && !can(user, \"read\", collection)) {\n        revokeIds.push(\n          ...notifications\n            .filter(\n              (notification) =>\n                !notification.documentId && notification.collectionId === id\n            )\n            .map((notification) => notification.id)\n        );\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 81.3,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: id, collectionIds",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over collectionIds\n// Problem: N+1 queries where N = collectionIds.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = collectionIds.map(id => id.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const id of collectionIds) {\n      const collection = dataMap.get(id.id);\n      if (collection && !can(user, \"read\", collection)) {\n        revokeIds.push(\n          ...notifications\n            .filter(\n              (notification) =>\n                !notification.documentId && notification.collectionId === id\n            )\n            .map((notification) => notification.id)\n        );\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 78.9,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const id of collectionIds) {\n      const collection = await Collection.findByPk(id, { userId: user.id });\n      if (collection && !can(user, \"read\", collection)) {\n        revokeIds.push(\n          ...notifications\n            .filter(\n              (notification) =>\n                !notification.documentId && notification.collectionId === id\n            )\n            .map((notification) => notification.id)\n        );\n      }\n    }\n*/\n\n// Access: modelWithRelations[0].relations"
    },
    {
     "type": "single-pass",
     "rank": 3,
     "fitnessScore": 78.2,
     "reasoning": "Converted filter().map() chain to single reduce() pass",
     "code": "for (const id of collectionIds) {\n  const collection = await Collection.findByPk(id, {\n    userId: user.id\n  });\n  if (collection && !can(user, \"read\", collection)) {\n    revokeIds.push(...notifications.reduce((acc, notification) => {\n      if (!notification.documentId && notification.collectionId === id) acc.push(notification.id);\n      return acc;\n    }, []));\n  }\n}"
    }
   ]
  },
  {
   "file": "server/queues/tasks/RevisionCreatedNotificationsTask.ts",
   "line": 56,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const mention of mentions) {\n      if (userIdsProcessed.has(mention.modelId)) {\n        continue;\n      }\n      userIdsProcessed.add(mention.modelId);\n\n      const recipient = await User.findByPk(mention.modelId);\n\n      if (\n        !recipient ||\n        recipient.id === mention.actorId ||\n        !(await canUserAccessDocument(recipient, document.id))\n      ) {\n        continue;\n      }\n\n      usersToSubscribe.push(recipient);\n\n      if (\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInDocument\n        )\n      ) {\n        await Notification.create({\n          event: NotificationEventType.MentionedInDocument,\n          userId: recipient.id,\n          revisionId: event.modelId,\n          actorId: mention.actorId,\n          teamId: document.teamId,\n          documentId: document.id,\n        });\n\n        userIdsMentioned.push(recipient.id);\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 84.6,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: mention, mentions",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over mentions\n// Problem: N+1 queries where N = mentions.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = mentions.map(mention => mention.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const mention of mentions) {\n      if (userIdsProcessed.has(mention.modelId)) {\n        continue;\n      }\n      userIdsProcessed.add(mention.modelId);\n\n      const recipient = dataMap.get(mention.id);\n\n      if (\n        !recipient ||\n        recipient.id === mention.actorId ||\n        !(await canUserAccessDocument(recipient, document.id))\n      ) {\n        continue;\n      }\n\n      usersToSubscribe.push(recipient);\n\n      if (\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInDocument\n        )\n      ) {\n        dataMap.get(mention.id);\n\n        userIdsMentioned.push(recipient.id);\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 80.75,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const mention of mentions) {\n      if (userIdsProcessed.has(mention.modelId)) {\n        continue;\n      }\n      userIdsProcessed.add(mention.modelId);\n\n      const recipient = await User.findByPk(mention.modelId);\n\n      if (\n        !recipient ||\n        recipient.id === mention.actorId ||\n        !(await canUserAccessDocument(recipient, document.id))\n      ) {\n        continue;\n      }\n\n      usersToSubscribe.push(recipient);\n\n      if (\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInDocument\n        )\n      ) {\n        await Notification.create({\n          event: NotificationEventType.MentionedInDocument,\n          userId: recipient.id,\n          revisionId: event.modelId,\n          actorId: mention.actorId,\n          teamId: document.teamId,\n          documentId: document.id,\n        });\n\n        userIdsMentioned.push(recipient.id);\n      }\n    }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/RevisionCreatedNotificationsTask.ts",
   "line": 108,
   "severity": "critical",
   "description": "Found 3 database queries (Sequelize.findByPk(), Sequelize.findAll(), Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = await Group.findByPk(group.modelId);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      const usersFromMentionedGroup = await GroupUser.findAll({\n        where: {\n          groupId: group.modelId,\n        },\n        order: [[\"permission\", \"ASC\"]],\n      });\n\n      const mentionedUser: string[] = [];\n      for (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = await User.findByPk(user.userId);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.GroupMentionedInDocument,\n            groupId: group.modelId,\n            userId: recipient.id,\n            revisionId: event.modelId,\n            actorId: group.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n          });\n\n          mentionedUser.push(user.userId);\n        }\n      }\n\n      mentionedGroup.push(group.modelId);\n    }",
   "estimatedImpact": {
    "severityScore": 9,
    "description": "301 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 301,
     "queriesOptimal": 1,
     "performanceGain": "301x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 81.3,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: user, usersFromMentionedGroup",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 5 async call(s) inside loop over usersFromMentionedGroup\n// Problem: N+1 queries where N = usersFromMentionedGroup.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = usersFromMentionedGroup.map(user => user.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = dataMap.get(user.id);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      const usersFromMentionedGroup = dataMap.get(user.id);\n\n      const mentionedUser: string[] = [];\n      for (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = dataMap.get(user.id);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          dataMap.get(user.id);\n\n          mentionedUser.push(user.userId);\n        }\n      }\n\n      mentionedGroup.push(group.modelId);\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 77.9,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: GroupUser",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for GroupUser\n// Optimized: Single query with include\n\nconst groupuserWithRelations = await GroupUser.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = await Group.findByPk(group.modelId);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      const usersFromMentionedGroup = await GroupUser.findAll({\n        where: {\n          groupId: group.modelId,\n        },\n        order: [[\"permission\", \"ASC\"]],\n      });\n\n      const mentionedUser: string[] = [];\n      for (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = await User.findByPk(user.userId);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.GroupMentionedInDocument,\n            groupId: group.modelId,\n            userId: recipient.id,\n            revisionId: event.modelId,\n            actorId: group.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n          });\n\n          mentionedUser.push(user.userId);\n        }\n      }\n\n      mentionedGroup.push(group.modelId);\n    }\n*/\n\n// Access: groupuserWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/RevisionCreatedNotificationsTask.ts",
   "line": 127,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = await User.findByPk(user.userId);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.GroupMentionedInDocument,\n            groupId: group.modelId,\n            userId: recipient.id,\n            revisionId: event.modelId,\n            actorId: group.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n          });\n\n          mentionedUser.push(user.userId);\n        }\n      }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 88.1,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: user, usersFromMentionedGroup",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over usersFromMentionedGroup\n// Problem: N+1 queries where N = usersFromMentionedGroup.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = usersFromMentionedGroup.map(user => user.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = dataMap.get(user.id);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          dataMap.get(user.id);\n\n          mentionedUser.push(user.userId);\n        }\n      }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 80.75,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = await User.findByPk(user.userId);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.GroupMentionedInDocument,\n            groupId: group.modelId,\n            userId: recipient.id,\n            revisionId: event.modelId,\n            actorId: group.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n          });\n\n          mentionedUser.push(user.userId);\n        }\n      }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/InviteReminderTask.ts",
   "line": 23,
   "severity": "high",
   "description": "Found 2 database queries (Sequelize.findByPk(), Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const userId of userIds) {\n      await sequelize.transaction(async (transaction) => {\n        const user = await User.scope(\"withTeam\").findByPk(userId, {\n          lock: {\n            level: transaction.LOCK.UPDATE,\n            of: User,\n          },\n          transaction,\n        });\n\n        const invitedBy = user?.invitedById\n          ? await User.findByPk(user?.invitedById, { transaction })\n          : undefined;\n\n        if (\n          user &&\n          invitedBy &&\n          user.getFlag(UserFlag.InviteReminderSent) === 0\n        ) {\n          await new InviteReminderEmail({\n            to: user.email,\n            language: user.language,\n            name: user.name,\n            actorName: invitedBy.name,\n            actorEmail: invitedBy.email,\n            teamName: user.team.name,\n            teamUrl: user.team.url,\n          }).schedule();\n\n          user.incrementFlag(UserFlag.InviteReminderSent);\n          await user.save({ transaction });\n        }\n      });\n    }",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 82.1,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: userId, userIds",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 5 async call(s) inside loop over userIds\n// Problem: N+1 queries where N = userIds.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = userIds.map(userId => userId.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const userId of userIds) {\n      dataMap.get(userId.id) => {\n        const user = dataMap.get(userId.id).findByPk(userId, {\n          lock: {\n            level: transaction.LOCK.UPDATE,\n            of: User,\n          },\n          transaction,\n        });\n\n        const invitedBy = user?.invitedById\n          ? dataMap.get(userId.id)\n          : undefined;\n\n        if (\n          user &&\n          invitedBy &&\n          user.getFlag(UserFlag.InviteReminderSent) === 0\n        ) {\n          await new InviteReminderEmail({\n            to: user.email,\n            language: user.language,\n            name: user.name,\n            actorName: invitedBy.name,\n            actorEmail: invitedBy.email,\n            teamName: user.team.name,\n            teamUrl: user.team.url,\n          }).schedule();\n\n          user.incrementFlag(UserFlag.InviteReminderSent);\n          dataMap.get(userId.id);\n        }\n      });\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 79.19999999999999,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const userId of userIds) {\n      await sequelize.transaction(async (transaction) => {\n        const user = await User.scope(\"withTeam\").findByPk(userId, {\n          lock: {\n            level: transaction.LOCK.UPDATE,\n            of: User,\n          },\n          transaction,\n        });\n\n        const invitedBy = user?.invitedById\n          ? await User.findByPk(user?.invitedById, { transaction })\n          : undefined;\n\n        if (\n          user &&\n          invitedBy &&\n          user.getFlag(UserFlag.InviteReminderSent) === 0\n        ) {\n          await new InviteReminderEmail({\n            to: user.email,\n            language: user.language,\n            name: user.name,\n            actorName: invitedBy.name,\n            actorEmail: invitedBy.email,\n            teamName: user.team.name,\n            teamUrl: user.team.url,\n          }).schedule();\n\n          user.incrementFlag(UserFlag.InviteReminderSent);\n          await user.save({ transaction });\n        }\n      });\n    }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/ExportJSONTask.ts",
   "line": 100,
   "severity": "high",
   "description": "Found 2 database queries (Sequelize.findByPk(), Sequelize.findAll()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const node of nodes) {\n        const result = await sequelizeReadOnly.transaction(\n          async (transaction) => {\n            const document = await Document.findByPk(node.id, {\n              includeState: true,\n              transaction,\n            });\n\n            if (!document) {\n              return;\n            }\n\n            const attachmentIds = includeAttachments\n              ? ProsemirrorHelper.parseAttachmentIds(\n                  DocumentHelper.toProsemirror(document)\n                )\n              : [];\n            const attachments = attachmentIds.length\n              ? await Attachment.findAll({\n                  where: {\n                    teamId: document.teamId,\n                    id: attachmentIds,\n                  },\n                  transaction,\n                })\n              : [];\n\n            return { attachments, document };\n          }\n        );\n\n        if (!result) {\n          continue;\n        }\n\n        const { attachments, document } = result;\n\n        addAttachments(attachments);\n\n        output.documents[document.id] = {\n          id: document.id,\n          urlId: document.urlId,\n          title: document.title,\n          icon: document.icon,\n          color: document.color,\n          data: DocumentHelper.toProsemirror(document).toJSON(),\n          createdById: document.createdById,\n          createdByName: document.createdBy.name,\n          createdByEmail: document.createdBy.email,\n          createdAt: document.createdAt.toISOString(),\n          updatedAt: document.updatedAt.toISOString(),\n          publishedAt: document.publishedAt\n            ? document.publishedAt.toISOString()\n            : null,\n          fullWidth: document.fullWidth,\n          parentDocumentId: document.parentDocumentId,\n        };\n\n        if (node.children?.length > 0) {\n          await addDocumentTree(node.children);\n        }\n      }",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 79.35,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: node, nodes",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 4 async call(s) inside loop over nodes\n// Problem: N+1 queries where N = nodes.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = nodes.map(node => node.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const node of nodes) {\n        const result = dataMap.get(node.id) => {\n            const document = dataMap.get(node.id);\n\n            if (!document) {\n              return;\n            }\n\n            const attachmentIds = includeAttachments\n              ? ProsemirrorHelper.parseAttachmentIds(\n                  DocumentHelper.toProsemirror(document)\n                )\n              : [];\n            const attachments = attachmentIds.length\n              ? dataMap.get(node.id)\n              : [];\n\n            return { attachments, document };\n          }\n        );\n\n        if (!result) {\n          continue;\n        }\n\n        const { attachments, document } = result;\n\n        addAttachments(attachments);\n\n        output.documents[document.id] = {\n          id: document.id,\n          urlId: document.urlId,\n          title: document.title,\n          icon: document.icon,\n          color: document.color,\n          data: DocumentHelper.toProsemirror(document).toJSON(),\n          createdById: document.createdById,\n          createdByName: document.createdBy.name,\n          createdByEmail: document.createdBy.email,\n          createdAt: document.createdAt.toISOString(),\n          updatedAt: document.updatedAt.toISOString(),\n          publishedAt: document.publishedAt\n            ? document.publishedAt.toISOString()\n            : null,\n          fullWidth: document.fullWidth,\n          parentDocumentId: document.parentDocumentId,\n        };\n\n        if (node.children?.length > 0) {\n          await addDocumentTree(node.children);\n        }\n      }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 74.19999999999999,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: Attachment",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for Attachment\n// Optimized: Single query with include\n\nconst attachmentWithRelations = await Attachment.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const node of nodes) {\n        const result = await sequelizeReadOnly.transaction(\n          async (transaction) => {\n            const document = await Document.findByPk(node.id, {\n              includeState: true,\n              transaction,\n            });\n\n            if (!document) {\n              return;\n            }\n\n            const attachmentIds = includeAttachments\n              ? ProsemirrorHelper.parseAttachmentIds(\n                  DocumentHelper.toProsemirror(document)\n                )\n              : [];\n            const attachments = attachmentIds.length\n              ? await Attachment.findAll({\n                  where: {\n                    teamId: document.teamId,\n                    id: attachmentIds,\n                  },\n                  transaction,\n                })\n              : [];\n\n            return { attachments, document };\n          }\n        );\n\n        if (!result) {\n          continue;\n        }\n\n        const { attachments, document } = result;\n\n        addAttachments(attachments);\n\n        output.documents[document.id] = {\n          id: document.id,\n          urlId: document.urlId,\n          title: document.title,\n          icon: document.icon,\n          color: document.color,\n          data: DocumentHelper.toProsemirror(document).toJSON(),\n          createdById: document.createdById,\n          createdByName: document.createdBy.name,\n          createdByEmail: document.createdBy.email,\n          createdAt: document.createdAt.toISOString(),\n          updatedAt: document.updatedAt.toISOString(),\n          publishedAt: document.publishedAt\n            ? document.publishedAt.toISOString()\n            : null,\n          fullWidth: document.fullWidth,\n          parentDocumentId: document.parentDocumentId,\n        };\n\n        if (node.children?.length > 0) {\n          await addDocumentTree(node.children);\n        }\n      }\n*/\n\n// Access: attachmentWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/ExportDocumentTreeTask.ts",
   "line": 195,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "internalLinks.forEach((match) => {\n      const matchedLink = match[0];\n      const matchedDocPath = pathMap.get(matchedLink);\n\n      if (matchedDocPath) {\n        const relativePath = path.relative(textPathInZip, matchedDocPath);\n        if (relativePath.startsWith(\".\")) {\n          text = text.replace(\n            matchedLink,\n            encodeURI(relativePath.substring(1))\n          );\n        }\n      }\n    })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.15,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: matchedLink, matchedDocPath, relativePath\n// Method calls: internalLinks.forEach(), pathMap.get(), path.relative(), relativePath.startsWith(), text.replace(), relativePath.substring()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\ninternalLinks.forEach((match) => {\n      const matchedLink = match[0];\n      const matchedDocPath = pathMap.get(matchedLink);\n\n      if (matchedDocPath) {\n        const relativePath = path.relative(textPathInZip, matchedDocPath);\n        if (relativePath.startsWith(\".\")) {\n          text = text.replace(\n            matchedLink,\n            encodeURI(relativePath.substring(1))\n          );\n        }\n      }\n    })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "server/queues/tasks/DocumentPublishedNotificationsTask.ts",
   "line": 32,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const mention of mentions) {\n      if (userIdsProcessed.has(mention.modelId)) {\n        continue;\n      }\n      userIdsProcessed.add(mention.modelId);\n\n      const recipient = await User.findByPk(mention.modelId);\n\n      if (\n        !recipient ||\n        recipient.id === mention.actorId ||\n        !(await canUserAccessDocument(recipient, document.id))\n      ) {\n        continue;\n      }\n\n      usersToSubscribe.push(recipient);\n\n      if (\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInDocument\n        )\n      ) {\n        await Notification.create({\n          event: NotificationEventType.MentionedInDocument,\n          userId: recipient.id,\n          actorId: mention.actorId,\n          teamId: document.teamId,\n          documentId: document.id,\n        });\n        userIdsMentioned.push(recipient.id);\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 84.6,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: mention, mentions",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over mentions\n// Problem: N+1 queries where N = mentions.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = mentions.map(mention => mention.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const mention of mentions) {\n      if (userIdsProcessed.has(mention.modelId)) {\n        continue;\n      }\n      userIdsProcessed.add(mention.modelId);\n\n      const recipient = dataMap.get(mention.id);\n\n      if (\n        !recipient ||\n        recipient.id === mention.actorId ||\n        !(await canUserAccessDocument(recipient, document.id))\n      ) {\n        continue;\n      }\n\n      usersToSubscribe.push(recipient);\n\n      if (\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInDocument\n        )\n      ) {\n        dataMap.get(mention.id);\n        userIdsMentioned.push(recipient.id);\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 80.75,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const mention of mentions) {\n      if (userIdsProcessed.has(mention.modelId)) {\n        continue;\n      }\n      userIdsProcessed.add(mention.modelId);\n\n      const recipient = await User.findByPk(mention.modelId);\n\n      if (\n        !recipient ||\n        recipient.id === mention.actorId ||\n        !(await canUserAccessDocument(recipient, document.id))\n      ) {\n        continue;\n      }\n\n      usersToSubscribe.push(recipient);\n\n      if (\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInDocument\n        )\n      ) {\n        await Notification.create({\n          event: NotificationEventType.MentionedInDocument,\n          userId: recipient.id,\n          actorId: mention.actorId,\n          teamId: document.teamId,\n          documentId: document.id,\n        });\n        userIdsMentioned.push(recipient.id);\n      }\n    }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/DocumentPublishedNotificationsTask.ts",
   "line": 73,
   "severity": "critical",
   "description": "Found 3 database queries (Sequelize.findByPk(), Sequelize.findAll(), Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = await Group.findByPk(group.modelId);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      const usersFromMentionedGroup = await GroupUser.findAll({\n        where: {\n          groupId: group.modelId,\n        },\n        order: [[\"permission\", \"ASC\"]],\n      });\n\n      const mentionedUser: string[] = [];\n      for (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = await User.findByPk(user.userId);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.GroupMentionedInDocument,\n            groupId: group.modelId,\n            userId: recipient.id,\n            actorId: group.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n          });\n\n          mentionedUser.push(user.userId);\n        }\n      }\n\n      mentionedGroup.push(group.modelId);\n    }",
   "estimatedImpact": {
    "severityScore": 9,
    "description": "301 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 301,
     "queriesOptimal": 1,
     "performanceGain": "301x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 81.3,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: user, usersFromMentionedGroup",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 5 async call(s) inside loop over usersFromMentionedGroup\n// Problem: N+1 queries where N = usersFromMentionedGroup.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = usersFromMentionedGroup.map(user => user.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = dataMap.get(user.id);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      const usersFromMentionedGroup = dataMap.get(user.id);\n\n      const mentionedUser: string[] = [];\n      for (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = dataMap.get(user.id);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          dataMap.get(user.id);\n\n          mentionedUser.push(user.userId);\n        }\n      }\n\n      mentionedGroup.push(group.modelId);\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 77.9,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: GroupUser",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for GroupUser\n// Optimized: Single query with include\n\nconst groupuserWithRelations = await GroupUser.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = await Group.findByPk(group.modelId);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      const usersFromMentionedGroup = await GroupUser.findAll({\n        where: {\n          groupId: group.modelId,\n        },\n        order: [[\"permission\", \"ASC\"]],\n      });\n\n      const mentionedUser: string[] = [];\n      for (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = await User.findByPk(user.userId);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.GroupMentionedInDocument,\n            groupId: group.modelId,\n            userId: recipient.id,\n            actorId: group.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n          });\n\n          mentionedUser.push(user.userId);\n        }\n      }\n\n      mentionedGroup.push(group.modelId);\n    }\n*/\n\n// Access: groupuserWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/DocumentPublishedNotificationsTask.ts",
   "line": 92,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = await User.findByPk(user.userId);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.GroupMentionedInDocument,\n            groupId: group.modelId,\n            userId: recipient.id,\n            actorId: group.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n          });\n\n          mentionedUser.push(user.userId);\n        }\n      }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 88.1,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: user, usersFromMentionedGroup",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over usersFromMentionedGroup\n// Problem: N+1 queries where N = usersFromMentionedGroup.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = usersFromMentionedGroup.map(user => user.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = dataMap.get(user.id);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          dataMap.get(user.id);\n\n          mentionedUser.push(user.userId);\n        }\n      }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 80.75,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const user of usersFromMentionedGroup) {\n        if (mentionedUser.includes(user.userId)) {\n          continue;\n        }\n\n        const recipient = await User.findByPk(user.userId);\n        if (\n          recipient &&\n          recipient.id !== group.actorId &&\n          recipient.subscribedToEventType(\n            NotificationEventType.GroupMentionedInDocument\n          ) &&\n          (await canUserAccessDocument(recipient, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.GroupMentionedInDocument,\n            groupId: group.modelId,\n            userId: recipient.id,\n            actorId: group.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n          });\n\n          mentionedUser.push(user.userId);\n        }\n      }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/CommentUpdatedNotificationsTask.ts",
   "line": 46,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const mention of mentions) {\n      if (userIdsMentioned.includes(mention.modelId)) {\n        continue;\n      }\n\n      const recipient = await User.findByPk(mention.modelId);\n\n      if (\n        mention.actorId &&\n        recipient &&\n        recipient.id !== mention.actorId &&\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInComment\n        ) &&\n        (await canUserAccessDocument(recipient, document.id))\n      ) {\n        await Notification.create({\n          event: NotificationEventType.MentionedInComment,\n          userId: recipient.id,\n          actorId: mention.actorId,\n          teamId: document.teamId,\n          documentId: document.id,\n          commentId: comment.id,\n        });\n        userIdsMentioned.push(mention.modelId);\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 86.85,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: mention, mentions",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over mentions\n// Problem: N+1 queries where N = mentions.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = mentions.map(mention => mention.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const mention of mentions) {\n      if (userIdsMentioned.includes(mention.modelId)) {\n        continue;\n      }\n\n      const recipient = dataMap.get(mention.id);\n\n      if (\n        mention.actorId &&\n        recipient &&\n        recipient.id !== mention.actorId &&\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInComment\n        ) &&\n        (await canUserAccessDocument(recipient, document.id))\n      ) {\n        dataMap.get(mention.id);\n        userIdsMentioned.push(mention.modelId);\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 80.25,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const mention of mentions) {\n      if (userIdsMentioned.includes(mention.modelId)) {\n        continue;\n      }\n\n      const recipient = await User.findByPk(mention.modelId);\n\n      if (\n        mention.actorId &&\n        recipient &&\n        recipient.id !== mention.actorId &&\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInComment\n        ) &&\n        (await canUserAccessDocument(recipient, document.id))\n      ) {\n        await Notification.create({\n          event: NotificationEventType.MentionedInComment,\n          userId: recipient.id,\n          actorId: mention.actorId,\n          teamId: document.teamId,\n          documentId: document.id,\n          commentId: comment.id,\n        });\n        userIdsMentioned.push(mention.modelId);\n      }\n    }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/CommentUpdatedNotificationsTask.ts",
   "line": 80,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = await Group.findByPk(group.modelId);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      // Schedule a separate task to handle group member notifications\n      await new GroupMentionedInCommentNotificationsTask().schedule({\n        ...event,\n        data: {\n          groupId: group.modelId,\n          actorId: group.actorId ?? event.actorId,\n        },\n      });\n\n      mentionedGroup.push(group.modelId);\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 83.85,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: group, groupMentions",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 2 async call(s) inside loop over groupMentions\n// Problem: N+1 queries where N = groupMentions.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = groupMentions.map(group => group.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = dataMap.get(group.id);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      // Schedule a separate task to handle group member notifications\n      await new GroupMentionedInCommentNotificationsTask().schedule({\n        ...event,\n        data: {\n          groupId: group.modelId,\n          actorId: group.actorId ?? event.actorId,\n        },\n      });\n\n      mentionedGroup.push(group.modelId);\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 77.94999999999999,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = await Group.findByPk(group.modelId);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      // Schedule a separate task to handle group member notifications\n      await new GroupMentionedInCommentNotificationsTask().schedule({\n        ...event,\n        data: {\n          groupId: group.modelId,\n          actorId: group.actorId ?? event.actorId,\n        },\n      });\n\n      mentionedGroup.push(group.modelId);\n    }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/CommentUpdatedNotificationsTask.ts",
   "line": 130,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const item of commentsAndReplies) {\n      // Mentions:\n      const proseCommentData = ProsemirrorHelper.toProsemirror(item.data);\n      const mentions = ProsemirrorHelper.parseMentions(proseCommentData, {\n        type: MentionType.User,\n      });\n      const userIds = mentions.map((mention) => mention.modelId);\n\n      // Comment author:\n      userIds.push(item.createdById);\n\n      for (const userId of userIds) {\n        if (userIdsNotified.includes(userId)) {\n          continue;\n        }\n\n        const user = await User.findByPk(userId);\n\n        if (\n          user &&\n          user.subscribedToEventType(NotificationEventType.ResolveComment) &&\n          (await canUserAccessDocument(user, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.ResolveComment,\n            userId: user.id,\n            actorId: event.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n            commentId: event.modelId,\n          });\n\n          userIdsNotified.push(userId);\n        }\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 81.75,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: userId, userIds",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over userIds\n// Problem: N+1 queries where N = userIds.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = userIds.map(userId => userId.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const item of commentsAndReplies) {\n      // Mentions:\n      const proseCommentData = ProsemirrorHelper.toProsemirror(item.data);\n      const mentions = ProsemirrorHelper.parseMentions(proseCommentData, {\n        type: MentionType.User,\n      });\n      const userIds = mentions.map((mention) => mention.modelId);\n\n      // Comment author:\n      userIds.push(item.createdById);\n\n      for (const userId of userIds) {\n        if (userIdsNotified.includes(userId)) {\n          continue;\n        }\n\n        const user = dataMap.get(userId.id);\n\n        if (\n          user &&\n          user.subscribedToEventType(NotificationEventType.ResolveComment) &&\n          (await canUserAccessDocument(user, document.id))\n        ) {\n          dataMap.get(userId.id);\n\n          userIdsNotified.push(userId);\n        }\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 78.35,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const item of commentsAndReplies) {\n      // Mentions:\n      const proseCommentData = ProsemirrorHelper.toProsemirror(item.data);\n      const mentions = ProsemirrorHelper.parseMentions(proseCommentData, {\n        type: MentionType.User,\n      });\n      const userIds = mentions.map((mention) => mention.modelId);\n\n      // Comment author:\n      userIds.push(item.createdById);\n\n      for (const userId of userIds) {\n        if (userIdsNotified.includes(userId)) {\n          continue;\n        }\n\n        const user = await User.findByPk(userId);\n\n        if (\n          user &&\n          user.subscribedToEventType(NotificationEventType.ResolveComment) &&\n          (await canUserAccessDocument(user, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.ResolveComment,\n            userId: user.id,\n            actorId: event.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n            commentId: event.modelId,\n          });\n\n          userIdsNotified.push(userId);\n        }\n      }\n    }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/CommentUpdatedNotificationsTask.ts",
   "line": 141,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const userId of userIds) {\n        if (userIdsNotified.includes(userId)) {\n          continue;\n        }\n\n        const user = await User.findByPk(userId);\n\n        if (\n          user &&\n          user.subscribedToEventType(NotificationEventType.ResolveComment) &&\n          (await canUserAccessDocument(user, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.ResolveComment,\n            userId: user.id,\n            actorId: event.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n            commentId: event.modelId,\n          });\n\n          userIdsNotified.push(userId);\n        }\n      }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 86.85,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: userId, userIds",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over userIds\n// Problem: N+1 queries where N = userIds.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = userIds.map(userId => userId.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const userId of userIds) {\n        if (userIdsNotified.includes(userId)) {\n          continue;\n        }\n\n        const user = dataMap.get(userId.id);\n\n        if (\n          user &&\n          user.subscribedToEventType(NotificationEventType.ResolveComment) &&\n          (await canUserAccessDocument(user, document.id))\n        ) {\n          dataMap.get(userId.id);\n\n          userIdsNotified.push(userId);\n        }\n      }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 80.25,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const userId of userIds) {\n        if (userIdsNotified.includes(userId)) {\n          continue;\n        }\n\n        const user = await User.findByPk(userId);\n\n        if (\n          user &&\n          user.subscribedToEventType(NotificationEventType.ResolveComment) &&\n          (await canUserAccessDocument(user, document.id))\n        ) {\n          await Notification.create({\n            event: NotificationEventType.ResolveComment,\n            userId: user.id,\n            actorId: event.actorId,\n            teamId: document.teamId,\n            documentId: document.id,\n            commentId: event.modelId,\n          });\n\n          userIdsNotified.push(userId);\n        }\n      }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/CommentCreatedNotificationsTask.ts",
   "line": 53,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const mention of mentions) {\n      if (userIdsMentioned.includes(mention.modelId)) {\n        continue;\n      }\n\n      const recipient = await User.findByPk(mention.modelId);\n\n      if (\n        mention.actorId &&\n        recipient &&\n        recipient.id !== mention.actorId &&\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInComment\n        ) &&\n        (await canUserAccessDocument(recipient, document.id))\n      ) {\n        await Notification.create({\n          event: NotificationEventType.MentionedInComment,\n          userId: recipient.id,\n          actorId: mention.actorId,\n          teamId: document.teamId,\n          commentId: comment.id,\n          documentId: document.id,\n        });\n        userIdsMentioned.push(recipient.id);\n      }\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 86.85,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: mention, mentions",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 3 async call(s) inside loop over mentions\n// Problem: N+1 queries where N = mentions.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = mentions.map(mention => mention.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const mention of mentions) {\n      if (userIdsMentioned.includes(mention.modelId)) {\n        continue;\n      }\n\n      const recipient = dataMap.get(mention.id);\n\n      if (\n        mention.actorId &&\n        recipient &&\n        recipient.id !== mention.actorId &&\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInComment\n        ) &&\n        (await canUserAccessDocument(recipient, document.id))\n      ) {\n        dataMap.get(mention.id);\n        userIdsMentioned.push(recipient.id);\n      }\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 80.25,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const mention of mentions) {\n      if (userIdsMentioned.includes(mention.modelId)) {\n        continue;\n      }\n\n      const recipient = await User.findByPk(mention.modelId);\n\n      if (\n        mention.actorId &&\n        recipient &&\n        recipient.id !== mention.actorId &&\n        recipient.subscribedToEventType(\n          NotificationEventType.MentionedInComment\n        ) &&\n        (await canUserAccessDocument(recipient, document.id))\n      ) {\n        await Notification.create({\n          event: NotificationEventType.MentionedInComment,\n          userId: recipient.id,\n          actorId: mention.actorId,\n          teamId: document.teamId,\n          commentId: comment.id,\n          documentId: document.id,\n        });\n        userIdsMentioned.push(recipient.id);\n      }\n    }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/tasks/CommentCreatedNotificationsTask.ts",
   "line": 90,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = await Group.findByPk(group.modelId);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      // Schedule a separate task to handle group member notifications\n      await new GroupMentionedInCommentNotificationsTask().schedule({\n        ...event,\n        data: {\n          groupId: group.modelId,\n          actorId: group.actorId ?? event.actorId,\n        },\n      });\n\n      mentionedGroup.push(group.modelId);\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 83.85,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: group, groupMentions",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 2 async call(s) inside loop over groupMentions\n// Problem: N+1 queries where N = groupMentions.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = groupMentions.map(group => group.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = dataMap.get(group.id);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      // Schedule a separate task to handle group member notifications\n      await new GroupMentionedInCommentNotificationsTask().schedule({\n        ...event,\n        data: {\n          groupId: group.modelId,\n          actorId: group.actorId ?? event.actorId,\n        },\n      });\n\n      mentionedGroup.push(group.modelId);\n    }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 77.94999999999999,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const group of groupMentions) {\n      if (mentionedGroup.includes(group.modelId)) {\n        continue;\n      }\n\n      // Check if the group has mentions disabled\n      const groupModel = await Group.findByPk(group.modelId);\n      if (groupModel?.disableMentions) {\n        continue;\n      }\n\n      // Schedule a separate task to handle group member notifications\n      await new GroupMentionedInCommentNotificationsTask().schedule({\n        ...event,\n        data: {\n          groupId: group.modelId,\n          actorId: group.actorId ?? event.actorId,\n        },\n      });\n\n      mentionedGroup.push(group.modelId);\n    }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/processors/WebsocketsProcessor.ts",
   "line": 544,
   "severity": "critical",
   "description": "Found 3 database queries (Unknown.all(), Sequelize.findByPk(), Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const groupUser of groupUsers) {\n              const [collection, user] = await Promise.all([\n                Collection.findByPk(event.collectionId, {\n                  userId: groupUser.userId,\n                }),\n                User.findByPk(groupUser.userId),\n              ]);\n              if (!user) {\n                continue;\n              }\n\n              if (cannot(user, \"read\", collection)) {\n                socketio\n                  .in(`user-${groupUser.userId}`)\n                  .socketsLeave(`collection-${event.collectionId}`);\n              }\n            }",
   "estimatedImpact": {
    "severityScore": 9,
    "description": "301 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 301,
     "queriesOptimal": 1,
     "performanceGain": "301x slower"
    }
   },
   "solutions": [
    {
     "type": "sequelize-include",
     "rank": 1,
     "fitnessScore": 84.5,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const groupUser of groupUsers) {\n              const [collection, user] = await Promise.all([\n                Collection.findByPk(event.collectionId, {\n                  userId: groupUser.userId,\n                }),\n                User.findByPk(groupUser.userId),\n              ]);\n              if (!user) {\n                continue;\n              }\n\n              if (cannot(user, \"read\", collection)) {\n                socketio\n                  .in(`user-${groupUser.userId}`)\n                  .socketsLeave(`collection-${event.collectionId}`);\n              }\n            }\n*/\n\n// Access: modelWithRelations[0].relations"
    },
    {
     "type": "batch-query-before-loop",
     "rank": 2,
     "fitnessScore": 82.1,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: groupUser, groupUsers",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over groupUsers\n// Problem: N+1 queries where N = groupUsers.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = groupUsers.map(groupUser => groupUser.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const groupUser of groupUsers) {\n              const [collection, user] = dataMap.get(groupUser.id),\n                User.findByPk(groupUser.userId),\n              ]);\n              if (!user) {\n                continue;\n              }\n\n              if (cannot(user, \"read\", collection)) {\n                socketio\n                  .in(`user-${groupUser.userId}`)\n                  .socketsLeave(`collection-${event.collectionId}`);\n              }\n            }\n\n// Performance: 1 query instead of N queries"
    }
   ]
  },
  {
   "file": "server/queues/processors/WebsocketsProcessor.ts",
   "line": 823,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const groupMembership of groupMemberships) {\n              if (!groupMembership.collectionId) {\n                continue;\n              }\n\n              socketio\n                .to(`user-${event.userId}`)\n                .emit(\n                  \"collections.remove_group\",\n                  presentGroupMembership(groupMembership)\n                );\n\n              const collection = await Collection.findByPk(\n                groupMembership.collectionId,\n                {\n                  userId: event.userId,\n                }\n              );\n\n              if (cannot(user, \"read\", collection)) {\n                socketio\n                  .in(`user-${event.userId}`)\n                  .socketsLeave(`collection-${groupMembership.collectionId}`);\n              }\n            }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 82.1,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: groupMembership, groupMemberships",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over groupMemberships\n// Problem: N+1 queries where N = groupMemberships.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = groupMemberships.map(groupMembership => groupMembership.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const groupMembership of groupMemberships) {\n              if (!groupMembership.collectionId) {\n                continue;\n              }\n\n              socketio\n                .to(`user-${event.userId}`)\n                .emit(\n                  \"collections.remove_group\",\n                  presentGroupMembership(groupMembership)\n                );\n\n              const collection = dataMap.get(groupMembership.id);\n\n              if (cannot(user, \"read\", collection)) {\n                socketio\n                  .in(`user-${event.userId}`)\n                  .socketsLeave(`collection-${groupMembership.collectionId}`);\n              }\n            }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 81.69999999999999,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const groupMembership of groupMemberships) {\n              if (!groupMembership.collectionId) {\n                continue;\n              }\n\n              socketio\n                .to(`user-${event.userId}`)\n                .emit(\n                  \"collections.remove_group\",\n                  presentGroupMembership(groupMembership)\n                );\n\n              const collection = await Collection.findByPk(\n                groupMembership.collectionId,\n                {\n                  userId: event.userId,\n                }\n              );\n\n              if (cannot(user, \"read\", collection)) {\n                socketio\n                  .in(`user-${event.userId}`)\n                  .socketsLeave(`collection-${groupMembership.collectionId}`);\n              }\n            }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/processors/WebsocketsProcessor.ts",
   "line": 882,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const groupMembership of groupMemberships) {\n              const payload = presentGroupMembership(groupMembership);\n\n              if (groupMembership.collectionId) {\n                for (const groupUser of groupUsers) {\n                  socketio\n                    .to(`user-${groupUser.userId}`)\n                    .emit(\"collections.remove_group\", payload);\n\n                  const collection = await Collection.findByPk(\n                    groupMembership.collectionId,\n                    {\n                      userId: groupUser.userId,\n                    }\n                  );\n\n                  if (cannot(groupUser.user, \"read\", collection)) {\n                    socketio\n                      .in(`user-${groupUser.userId}`)\n                      .socketsLeave(\n                        `collection-${groupMembership.collectionId}`\n                      );\n                  }\n                }\n              }\n\n              if (groupMembership.documentId) {\n                for (const groupUser of groupUsers) {\n                  socketio\n                    .to(`user-${groupUser.userId}`)\n                    .emit(\"documents.remove_group\", payload);\n                }\n              }\n            }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 78.75,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: groupUser, groupUsers",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over groupUsers\n// Problem: N+1 queries where N = groupUsers.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = groupUsers.map(groupUser => groupUser.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const groupMembership of groupMemberships) {\n              const payload = presentGroupMembership(groupMembership);\n\n              if (groupMembership.collectionId) {\n                for (const groupUser of groupUsers) {\n                  socketio\n                    .to(`user-${groupUser.userId}`)\n                    .emit(\"collections.remove_group\", payload);\n\n                  const collection = dataMap.get(groupUser.id);\n\n                  if (cannot(groupUser.user, \"read\", collection)) {\n                    socketio\n                      .in(`user-${groupUser.userId}`)\n                      .socketsLeave(\n                        `collection-${groupMembership.collectionId}`\n                      );\n                  }\n                }\n              }\n\n              if (groupMembership.documentId) {\n                for (const groupUser of groupUsers) {\n                  socketio\n                    .to(`user-${groupUser.userId}`)\n                    .emit(\"documents.remove_group\", payload);\n                }\n              }\n            }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 75.85,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const groupMembership of groupMemberships) {\n              const payload = presentGroupMembership(groupMembership);\n\n              if (groupMembership.collectionId) {\n                for (const groupUser of groupUsers) {\n                  socketio\n                    .to(`user-${groupUser.userId}`)\n                    .emit(\"collections.remove_group\", payload);\n\n                  const collection = await Collection.findByPk(\n                    groupMembership.collectionId,\n                    {\n                      userId: groupUser.userId,\n                    }\n                  );\n\n                  if (cannot(groupUser.user, \"read\", collection)) {\n                    socketio\n                      .in(`user-${groupUser.userId}`)\n                      .socketsLeave(\n                        `collection-${groupMembership.collectionId}`\n                      );\n                  }\n                }\n              }\n\n              if (groupMembership.documentId) {\n                for (const groupUser of groupUsers) {\n                  socketio\n                    .to(`user-${groupUser.userId}`)\n                    .emit(\"documents.remove_group\", payload);\n                }\n              }\n            }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/processors/WebsocketsProcessor.ts",
   "line": 886,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findByPk()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const groupUser of groupUsers) {\n                  socketio\n                    .to(`user-${groupUser.userId}`)\n                    .emit(\"collections.remove_group\", payload);\n\n                  const collection = await Collection.findByPk(\n                    groupMembership.collectionId,\n                    {\n                      userId: groupUser.userId,\n                    }\n                  );\n\n                  if (cannot(groupUser.user, \"read\", collection)) {\n                    socketio\n                      .in(`user-${groupUser.userId}`)\n                      .socketsLeave(\n                        `collection-${groupMembership.collectionId}`\n                      );\n                  }\n                }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 84.6,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: groupUser, groupUsers",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over groupUsers\n// Problem: N+1 queries where N = groupUsers.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = groupUsers.map(groupUser => groupUser.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const groupUser of groupUsers) {\n                  socketio\n                    .to(`user-${groupUser.userId}`)\n                    .emit(\"collections.remove_group\", payload);\n\n                  const collection = dataMap.get(groupUser.id);\n\n                  if (cannot(groupUser.user, \"read\", collection)) {\n                    socketio\n                      .in(`user-${groupUser.userId}`)\n                      .socketsLeave(\n                        `collection-${groupMembership.collectionId}`\n                      );\n                  }\n                }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 81.69999999999999,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const groupUser of groupUsers) {\n                  socketio\n                    .to(`user-${groupUser.userId}`)\n                    .emit(\"collections.remove_group\", payload);\n\n                  const collection = await Collection.findByPk(\n                    groupMembership.collectionId,\n                    {\n                      userId: groupUser.userId,\n                    }\n                  );\n\n                  if (cannot(groupUser.user, \"read\", collection)) {\n                    socketio\n                      .in(`user-${groupUser.userId}`)\n                      .socketsLeave(\n                        `collection-${groupMembership.collectionId}`\n                      );\n                  }\n                }\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/processors/ImportsProcessor.ts",
   "line": 397,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findAll()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const importTask of importTasks) {\n          const outputMap = keyBy(importTask.output ?? [], \"externalId\");\n\n          for (const input of importTask.input) {\n            const externalId = input.externalId;\n            const internalId = await this.getInternalId(externalId, idMap);\n            const parentExternalId = input.parentExternalId;\n            const parentInternalId = parentExternalId\n              ? await this.getInternalId(parentExternalId, idMap)\n              : undefined;\n\n            const collectionExternalId = input.collectionExternalId;\n            const collectionInternalId = collectionExternalId\n              ? await this.getInternalId(collectionExternalId, idMap)\n              : undefined;\n\n            const output = outputMap[externalId];\n\n            // Skip this item if it has no output (likely due to an error during processing)\n            if (!output) {\n              Logger.debug(\n                \"processor\",\n                `Skipping item with no output: ${externalId}`\n              );\n              continue;\n            }\n\n            const collectionItem = importInput[externalId];\n\n            const attachments = await Attachment.findAll({\n              attributes: [\"id\", \"size\"],\n              where: { documentId: externalId }, // This will be set for root pages too (which will be imported as collection)\n              transaction,\n            });\n\n            const transformedContent = await this.rewriteReferences({\n              content: output.content,\n              attachments,\n              importInput,\n              idMap,\n              urlIdMap,\n              actorId: importModel.createdById,\n              teamId: importModel.teamId,\n            });\n\n            const resolvedCreatedById =\n              (await this.resolveExternalUserId(\n                output,\n                importModel.teamId,\n                userIdCache,\n                transaction\n              )) ?? importModel.createdById;\n\n            if (collectionItem) {\n              // imported collection will be placed in the beginning.\n              collectionIdx = fractionalIndex(null, collectionIdx);\n\n              const description = await DocumentHelper.toMarkdown(\n                transformedContent,\n                {\n                  includeTitle: false,\n                }\n              );\n\n              // Build sourceMetadata for the collection\n              const sourceMetadata: SourceMetadata = {\n                externalId: externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              };\n\n              const urlId = output.urlId\n                ? urlIdMap[output.urlId]?.urlId\n                : undefined;\n\n              const collection = Collection.build({\n                id: internalId,\n                urlId,\n                name: output.title,\n                icon: output.icon ?? \"collection\",\n                color:\n                  output.color ??\n                  (output.icon ? undefined : randomElement(colorPalette)),\n                content: transformedContent,\n                description: truncate(description, {\n                  length: CollectionValidation.maxDescriptionLength,\n                }),\n                createdById: resolvedCreatedById,\n                teamId: importModel.createdBy.teamId,\n                apiImportId: importModel.id,\n                index: collectionIdx,\n                sort: Collection.DEFAULT_SORT,\n                permission: collectionItem.permission,\n                sourceMetadata,\n                createdAt: output.createdAt ?? now,\n                updatedAt: output.updatedAt ?? now,\n              });\n\n              await collection.saveWithCtx(\n                ctx,\n                { silent: true },\n                {\n                  name: \"create\",\n                }\n              );\n\n              createdCollections.push(collection);\n\n              // Unset documentId for attachments in collection overview.\n              await Attachment.update(\n                { documentId: null },\n                { where: { documentId: externalId }, silent: true, transaction }\n              );\n\n              continue;\n            }\n\n            // Document at the root of a collection when there's no parent (or) the parent is the collection.\n            const isRootDocument =\n              !parentExternalId || !!importInput[parentExternalId];\n\n            const urlId = output.urlId\n              ? urlIdMap[output.urlId]?.urlId\n              : undefined;\n\n            const defaults = {\n              title: output.title,\n              urlId,\n              icon: output.icon,\n              color: output.color,\n              content: transformedContent,\n              text: await DocumentHelper.toMarkdown(transformedContent, {\n                includeTitle: false,\n              }),\n              collectionId: collectionInternalId,\n              parentDocumentId: isRootDocument ? undefined : parentInternalId,\n              createdById: resolvedCreatedById,\n              lastModifiedById: resolvedCreatedById,\n              teamId: importModel.createdBy.teamId,\n              apiImportId: importModel.id,\n              sourceMetadata: {\n                externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              },\n              createdAt: output.createdAt ?? now,\n              updatedAt: output.updatedAt ?? now,\n              publishedAt:\n                output.publishedAt ??\n                output.updatedAt ??\n                output.createdAt ??\n                now,\n            };\n\n            try {\n              await Document.findOrCreateWithCtx(\n                ctx,\n                {\n                  where: {\n                    id: internalId,\n                  },\n                  defaults,\n                  silent: true,\n                },\n                {\n                  name: \"create\",\n                  data: { title: output.title, source: \"import\" },\n                }\n              );\n            } catch (err) {\n              if (err instanceof UniqueConstraintError) {\n                Logger.error(\n                  `ImportsProcessor document creation failed due to unique constraint error (${internalId}: ${defaults.title})`,\n                  err,\n                  {\n                    fields: err.fields,\n                    documentId: internalId,\n                    title: defaults.title,\n                    collectionId: defaults.collectionId,\n                    parentDocumentId: defaults.parentDocumentId,\n                  }\n                );\n              }\n              throw err;\n            }\n\n            // Update document id for attachments in document content.\n            await Attachment.update(\n              { documentId: internalId },\n              { where: { documentId: externalId }, silent: true, transaction }\n            );\n          }\n        }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 56.8,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: input, importTask.input",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 12 async call(s) inside loop over importTask.input\n// Problem: N+1 queries where N = importTask.input.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = importTask.input.map(input => input.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const importTask of importTasks) {\n          const outputMap = keyBy(importTask.output ?? [], \"externalId\");\n\n          for (const input of importTask.input) {\n            const externalId = input.externalId;\n            const internalId = dataMap.get(input.id);\n            const parentExternalId = input.parentExternalId;\n            const parentInternalId = parentExternalId\n              ? dataMap.get(input.id)\n              : undefined;\n\n            const collectionExternalId = input.collectionExternalId;\n            const collectionInternalId = collectionExternalId\n              ? dataMap.get(input.id)\n              : undefined;\n\n            const output = outputMap[externalId];\n\n            // Skip this item if it has no output (likely due to an error during processing)\n            if (!output) {\n              Logger.debug(\n                \"processor\",\n                `Skipping item with no output: ${externalId}`\n              );\n              continue;\n            }\n\n            const collectionItem = importInput[externalId];\n\n            const attachments = dataMap.get(input.id)\n              transaction,\n            });\n\n            const transformedContent = dataMap.get(input.id);\n\n            const resolvedCreatedById =\n              (dataMap.get(input.id)) ?? importModel.createdById;\n\n            if (collectionItem) {\n              // imported collection will be placed in the beginning.\n              collectionIdx = fractionalIndex(null, collectionIdx);\n\n              const description = dataMap.get(input.id);\n\n              // Build sourceMetadata for the collection\n              const sourceMetadata: SourceMetadata = {\n                externalId: externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              };\n\n              const urlId = output.urlId\n                ? urlIdMap[output.urlId]?.urlId\n                : undefined;\n\n              const collection = Collection.build({\n                id: internalId,\n                urlId,\n                name: output.title,\n                icon: output.icon ?? \"collection\",\n                color:\n                  output.color ??\n                  (output.icon ? undefined : randomElement(colorPalette)),\n                content: transformedContent,\n                description: truncate(description, {\n                  length: CollectionValidation.maxDescriptionLength,\n                }),\n                createdById: resolvedCreatedById,\n                teamId: importModel.createdBy.teamId,\n                apiImportId: importModel.id,\n                index: collectionIdx,\n                sort: Collection.DEFAULT_SORT,\n                permission: collectionItem.permission,\n                sourceMetadata,\n                createdAt: output.createdAt ?? now,\n                updatedAt: output.updatedAt ?? now,\n              });\n\n              dataMap.get(input.id);\n\n              createdCollections.push(collection);\n\n              // Unset documentId for attachments in collection overview.\n              dataMap.get(input.id);\n\n              continue;\n            }\n\n            // Document at the root of a collection when there's no parent (or) the parent is the collection.\n            const isRootDocument =\n              !parentExternalId || !!importInput[parentExternalId];\n\n            const urlId = output.urlId\n              ? urlIdMap[output.urlId]?.urlId\n              : undefined;\n\n            const defaults = {\n              title: output.title,\n              urlId,\n              icon: output.icon,\n              color: output.color,\n              content: transformedContent,\n              text: dataMap.get(input.id),\n              collectionId: collectionInternalId,\n              parentDocumentId: isRootDocument ? undefined : parentInternalId,\n              createdById: resolvedCreatedById,\n              lastModifiedById: resolvedCreatedById,\n              teamId: importModel.createdBy.teamId,\n              apiImportId: importModel.id,\n              sourceMetadata: {\n                externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              },\n              createdAt: output.createdAt ?? now,\n              updatedAt: output.updatedAt ?? now,\n              publishedAt:\n                output.publishedAt ??\n                output.updatedAt ??\n                output.createdAt ??\n                now,\n            };\n\n            try {\n              dataMap.get(input.id);\n            } catch (err) {\n              if (err instanceof UniqueConstraintError) {\n                Logger.error(\n                  `ImportsProcessor document creation failed due to unique constraint error (${internalId}: ${defaults.title})`,\n                  err,\n                  {\n                    fields: err.fields,\n                    documentId: internalId,\n                    title: defaults.title,\n                    collectionId: defaults.collectionId,\n                    parentDocumentId: defaults.parentDocumentId,\n                  }\n                );\n              }\n              throw err;\n            }\n\n            // Update document id for attachments in document content.\n            dataMap.get(input.id);\n          }\n        }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "memoize",
     "rank": 2,
     "fitnessScore": 55.4,
     "reasoning": "Added memoization for 3 repeated calls",
     "code": "const getInternalIdCache = new Map(); // Memoize this.getInternalId\nconst toMarkdownCache = new Map(); // Memoize DocumentHelper.toMarkdown\nconst updateCache = new Map(); // Memoize Attachment.update\n\nfor (const importTask of importTasks) {\n          const outputMap = keyBy(importTask.output ?? [], \"externalId\");\n\n          for (const input of importTask.input) {\n            const externalId = input.externalId;\n            const internalId = await this.getInternalId(externalId, idMap);\n            const parentExternalId = input.parentExternalId;\n            const parentInternalId = parentExternalId\n              ? await this.getInternalId(parentExternalId, idMap)\n              : undefined;\n\n            const collectionExternalId = input.collectionExternalId;\n            const collectionInternalId = collectionExternalId\n              ? await this.getInternalId(collectionExternalId, idMap)\n              : undefined;\n\n            const output = outputMap[externalId];\n\n            // Skip this item if it has no output (likely due to an error during processing)\n            if (!output) {\n              Logger.debug(\n                \"processor\",\n                `Skipping item with no output: ${externalId}`\n              );\n              continue;\n            }\n\n            const collectionItem = importInput[externalId];\n\n            const attachments = await Attachment.findAll({\n              attributes: [\"id\", \"size\"],\n              where: { documentId: externalId }, // This will be set for root pages too (which will be imported as collection)\n              transaction,\n            });\n\n            const transformedContent = await this.rewriteReferences({\n              content: output.content,\n              attachments,\n              importInput,\n              idMap,\n              urlIdMap,\n              actorId: importModel.createdById,\n              teamId: importModel.teamId,\n            });\n\n            const resolvedCreatedById =\n              (await this.resolveExternalUserId(\n                output,\n                importModel.teamId,\n                userIdCache,\n                transaction\n              )) ?? importModel.createdById;\n\n            if (collectionItem) {\n              // imported collection will be placed in the beginning.\n              collectionIdx = fractionalIndex(null, collectionIdx);\n\n              const description = await DocumentHelper.toMarkdown(\n                transformedContent,\n                {\n                  includeTitle: false,\n                }\n              );\n\n              // Build sourceMetadata for the collection\n              const sourceMetadata: SourceMetadata = {\n                externalId: externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              };\n\n              const urlId = output.urlId\n                ? urlIdMap[output.urlId]?.urlId\n                : undefined;\n\n              const collection = Collection.build({\n                id: internalId,\n                urlId,\n                name: output.title,\n                icon: output.icon ?? \"collection\",\n                color:\n                  output.color ??\n                  (output.icon ? undefined : randomElement(colorPalette)),\n                content: transformedContent,\n                description: truncate(description, {\n                  length: CollectionValidation.maxDescriptionLength,\n                }),\n                createdById: resolvedCreatedById,\n                teamId: importModel.createdBy.teamId,\n                apiImportId: importModel.id,\n                index: collectionIdx,\n                sort: Collection.DEFAULT_SORT,\n                permission: collectionItem.permission,\n                sourceMetadata,\n                createdAt: output.createdAt ?? now,\n                updatedAt: output.updatedAt ?? now,\n              });\n\n              await collection.saveWithCtx(\n                ctx,\n                { silent: true },\n                {\n                  name: \"create\",\n                }\n              );\n\n              createdCollections.push(collection);\n\n              // Unset documentId for attachments in collection overview.\n              await Attachment.update(\n                { documentId: null },\n                { where: { documentId: externalId }, silent: true, transaction }\n              );\n\n              continue;\n            }\n\n            // Document at the root of a collection when there's no parent (or) the parent is the collection.\n            const isRootDocument =\n              !parentExternalId || !!importInput[parentExternalId];\n\n            const urlId = output.urlId\n              ? urlIdMap[output.urlId]?.urlId\n              : undefined;\n\n            const defaults = {\n              title: output.title,\n              urlId,\n              icon: output.icon,\n              color: output.color,\n              content: transformedContent,\n              text: await DocumentHelper.toMarkdown(transformedContent, {\n                includeTitle: false,\n              }),\n              collectionId: collectionInternalId,\n              parentDocumentId: isRootDocument ? undefined : parentInternalId,\n              createdById: resolvedCreatedById,\n              lastModifiedById: resolvedCreatedById,\n              teamId: importModel.createdBy.teamId,\n              apiImportId: importModel.id,\n              sourceMetadata: {\n                externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              },\n              createdAt: output.createdAt ?? now,\n              updatedAt: output.updatedAt ?? now,\n              publishedAt:\n                output.publishedAt ??\n                output.updatedAt ??\n                output.createdAt ??\n                now,\n            };\n\n            try {\n              await Document.findOrCreateWithCtx(\n                ctx,\n                {\n                  where: {\n                    id: internalId,\n                  },\n                  defaults,\n                  silent: true,\n                },\n                {\n                  name: \"create\",\n                  data: { title: output.title, source: \"import\" },\n                }\n              );\n            } catch (err) {\n              if (err instanceof UniqueConstraintError) {\n                Logger.error(\n                  `ImportsProcessor document creation failed due to unique constraint error (${internalId}: ${defaults.title})`,\n                  err,\n                  {\n                    fields: err.fields,\n                    documentId: internalId,\n                    title: defaults.title,\n                    collectionId: defaults.collectionId,\n                    parentDocumentId: defaults.parentDocumentId,\n                  }\n                );\n              }\n              throw err;\n            }\n\n            // Update document id for attachments in document content.\n            await Attachment.update(\n              { documentId: internalId },\n              { where: { documentId: externalId }, silent: true, transaction }\n            );\n          }\n        }"
    },
    {
     "type": "memoization",
     "rank": 3,
     "fitnessScore": 52.15,
     "reasoning": "Add memoization cache for repeated expensive calls\nPreserved: this.getInternalId, DocumentHelper.toMarkdown, Attachment.update",
     "code": "// OPTIMIZED: Memoization for repeated expensive calls\n// Found 3 repeated async call(s)\n\n// Memoize this.getInternalId (called 3x)\nconst getInternalIdCache = new Map();\nasync function memoizedGetInternalId(key) {\n  if (getInternalIdCache.has(key)) return getInternalIdCache.get(key);\n  const result = await this.getInternalId(key);\n  getInternalIdCache.set(key, result);\n  return result;\n}\n\n// Memoize DocumentHelper.toMarkdown (called 2x)\nconst toMarkdownCache = new Map();\nasync function memoizedToMarkdown(key) {\n  if (toMarkdownCache.has(key)) return toMarkdownCache.get(key);\n  const result = await DocumentHelper.toMarkdown(key);\n  toMarkdownCache.set(key, result);\n  return result;\n}\n\n// Memoize Attachment.update (called 2x)\nconst updateCache = new Map();\nasync function memoizedUpdate(key) {\n  if (updateCache.has(key)) return updateCache.get(key);\n  const result = await Attachment.update(key);\n  updateCache.set(key, result);\n  return result;\n}\n\n// Original code (replace calls with memoized versions):\nfor (const importTask of importTasks) {\n          const outputMap = keyBy(importTask.output ?? [], \"externalId\");\n\n          for (const input of importTask.input) {\n            const externalId = input.externalId;\n            const internalId = await this.getInternalId(externalId, idMap);\n            const parentExternalId = input.parentExternalId;\n            const parentInternalId = parentExternalId\n              ? await this.getInternalId(parentExternalId, idMap)\n              : undefined;\n\n            const collectionExternalId = input.collectionExternalId;\n            const collectionInternalId = collectionExternalId\n              ? await this.getInternalId(collectionExternalId, idMap)\n              : undefined;\n\n            const output = outputMap[externalId];\n\n            // Skip this item if it has no output (likely due to an error during processing)\n            if (!output) {\n              Logger.debug(\n                \"processor\",\n                `Skipping item with no output: ${externalId}`\n              );\n              continue;\n            }\n\n            const collectionItem = importInput[externalId];\n\n            const attachments = await Attachment.findAll({\n              attributes: [\"id\", \"size\"],\n              where: { documentId: externalId }, // This will be set for root pages too (which will be imported as collection)\n              transaction,\n            });\n\n            const transformedContent = await this.rewriteReferences({\n              content: output.content,\n              attachments,\n              importInput,\n              idMap,\n              urlIdMap,\n              actorId: importModel.createdById,\n              teamId: importModel.teamId,\n            });\n\n            const resolvedCreatedById =\n              (await this.resolveExternalUserId(\n                output,\n                importModel.teamId,\n                userIdCache,\n                transaction\n              )) ?? importModel.createdById;\n\n            if (collectionItem) {\n              // imported collection will be placed in the beginning.\n              collectionIdx = fractionalIndex(null, collectionIdx);\n\n              const description = await DocumentHelper.toMarkdown(\n                transformedContent,\n                {\n                  includeTitle: false,\n                }\n              );\n\n              // Build sourceMetadata for the collection\n              const sourceMetadata: SourceMetadata = {\n                externalId: externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              };\n\n              const urlId = output.urlId\n                ? urlIdMap[output.urlId]?.urlId\n                : undefined;\n\n              const collection = Collection.build({\n                id: internalId,\n                urlId,\n                name: output.title,\n                icon: output.icon ?? \"collection\",\n                color:\n                  output.color ??\n                  (output.icon ? undefined : randomElement(colorPalette)),\n                content: transformedContent,\n                description: truncate(description, {\n                  length: CollectionValidation.maxDescriptionLength,\n                }),\n                createdById: resolvedCreatedById,\n                teamId: importModel.createdBy.teamId,\n                apiImportId: importModel.id,\n                index: collectionIdx,\n                sort: Collection.DEFAULT_SORT,\n                permission: collectionItem.permission,\n                sourceMetadata,\n                createdAt: output.createdAt ?? now,\n                updatedAt: output.updatedAt ?? now,\n              });\n\n              await collection.saveWithCtx(\n                ctx,\n                { silent: true },\n                {\n                  name: \"create\",\n                }\n              );\n\n              createdCollections.push(collection);\n\n              // Unset documentId for attachments in collection overview.\n              await Attachment.update(\n                { documentId: null },\n                { where: { documentId: externalId }, silent: true, transaction }\n              );\n\n              continue;\n            }\n\n            // Document at the root of a collection when there's no parent (or) the parent is the collection.\n            const isRootDocument =\n              !parentExternalId || !!importInput[parentExternalId];\n\n            const urlId = output.urlId\n              ? urlIdMap[output.urlId]?.urlId\n              : undefined;\n\n            const defaults = {\n              title: output.title,\n              urlId,\n              icon: output.icon,\n              color: output.color,\n              content: transformedContent,\n              text: await DocumentHelper.toMarkdown(transformedContent, {\n                includeTitle: false,\n              }),\n              collectionId: collectionInternalId,\n              parentDocumentId: isRootDocument ? undefined : parentInternalId,\n              createdById: resolvedCreatedById,\n              lastModifiedById: resolvedCreatedById,\n              teamId: importModel.createdBy.teamId,\n              apiImportId: importModel.id,\n              sourceMetadata: {\n                externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              },\n              createdAt: output.createdAt ?? now,\n              updatedAt: output.updatedAt ?? now,\n              publishedAt:\n                output.publishedAt ??\n                output.updatedAt ??\n                output.createdAt ??\n                now,\n            };\n\n            try {\n              await Document.findOrCreateWithCtx(\n                ctx,\n                {\n                  where: {\n                    id: internalId,\n                  },\n                  defaults,\n                  silent: true,\n                },\n                {\n                  name: \"create\",\n                  data: { title: output.title, source: \"import\" },\n                }\n              );\n            } catch (err) {\n              if (err instanceof UniqueConstraintError) {\n                Logger.error(\n                  `ImportsProcessor document creation failed due to unique constraint error (${internalId}: ${defaults.title})`,\n                  err,\n                  {\n                    fields: err.fields,\n                    documentId: internalId,\n                    title: defaults.title,\n                    collectionId: defaults.collectionId,\n                    parentDocumentId: defaults.parentDocumentId,\n                  }\n                );\n              }\n              throw err;\n            }\n\n            // Update document id for attachments in document content.\n            await Attachment.update(\n              { documentId: internalId },\n              { where: { documentId: externalId }, silent: true, transaction }\n            );\n          }\n        }"
    },
    {
     "type": "sequelize-include",
     "rank": 4,
     "fitnessScore": 49.9,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: Attachment",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for Attachment\n// Optimized: Single query with include\n\nconst attachmentWithRelations = await Attachment.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const importTask of importTasks) {\n          const outputMap = keyBy(importTask.output ?? [], \"externalId\");\n\n          for (const input of importTask.input) {\n            const externalId = input.externalId;\n            const internalId = await this.getInternalId(externalId, idMap);\n            const parentExternalId = input.parentExternalId;\n            const parentInternalId = parentExternalId\n              ? await this.getInternalId(parentExternalId, idMap)\n              : undefined;\n\n            const collectionExternalId = input.collectionExternalId;\n            const collectionInternalId = collectionExternalId\n              ? await this.getInternalId(collectionExternalId, idMap)\n              : undefined;\n\n            const output = outputMap[externalId];\n\n            // Skip this item if it has no output (likely due to an error during processing)\n            if (!output) {\n              Logger.debug(\n                \"processor\",\n                `Skipping item with no output: ${externalId}`\n              );\n              continue;\n            }\n\n            const collectionItem = importInput[externalId];\n\n            const attachments = await Attachment.findAll({\n              attributes: [\"id\", \"size\"],\n              where: { documentId: externalId }, // This will be set for root pages too (which will be imported as collection)\n              transaction,\n            });\n\n            const transformedContent = await this.rewriteReferences({\n              content: output.content,\n              attachments,\n              importInput,\n              idMap,\n              urlIdMap,\n              actorId: importModel.createdById,\n              teamId: importModel.teamId,\n            });\n\n            const resolvedCreatedById =\n              (await this.resolveExternalUserId(\n                output,\n                importModel.teamId,\n                userIdCache,\n                transaction\n              )) ?? importModel.createdById;\n\n            if (collectionItem) {\n              // imported collection will be placed in the beginning.\n              collectionIdx = fractionalIndex(null, collectionIdx);\n\n              const description = await DocumentHelper.toMarkdown(\n                transformedContent,\n                {\n                  includeTitle: false,\n                }\n              );\n\n              // Build sourceMetadata for the collection\n              const sourceMetadata: SourceMetadata = {\n                externalId: externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              };\n\n              const urlId = output.urlId\n                ? urlIdMap[output.urlId]?.urlId\n                : undefined;\n\n              const collection = Collection.build({\n                id: internalId,\n                urlId,\n                name: output.title,\n                icon: output.icon ?? \"collection\",\n                color:\n                  output.color ??\n                  (output.icon ? undefined : randomElement(colorPalette)),\n                content: transformedContent,\n                description: truncate(description, {\n                  length: CollectionValidation.maxDescriptionLength,\n                }),\n                createdById: resolvedCreatedById,\n                teamId: importModel.createdBy.teamId,\n                apiImportId: importModel.id,\n                index: collectionIdx,\n                sort: Collection.DEFAULT_SORT,\n                permission: collectionItem.permission,\n                sourceMetadata,\n                createdAt: output.createdAt ?? now,\n                updatedAt: output.updatedAt ?? now,\n              });\n\n              await collection.saveWithCtx(\n                ctx,\n                { silent: true },\n                {\n                  name: \"create\",\n                }\n              );\n\n              createdCollections.push(collection);\n\n              // Unset documentId for attachments in collection overview.\n              await Attachment.update(\n                { documentId: null },\n                { where: { documentId: externalId }, silent: true, transaction }\n              );\n\n              continue;\n            }\n\n            // Document at the root of a collection when there's no parent (or) the parent is the collection.\n            const isRootDocument =\n              !parentExternalId || !!importInput[parentExternalId];\n\n            const urlId = output.urlId\n              ? urlIdMap[output.urlId]?.urlId\n              : undefined;\n\n            const defaults = {\n              title: output.title,\n              urlId,\n              icon: output.icon,\n              color: output.color,\n              content: transformedContent,\n              text: await DocumentHelper.toMarkdown(transformedContent, {\n                includeTitle: false,\n              }),\n              collectionId: collectionInternalId,\n              parentDocumentId: isRootDocument ? undefined : parentInternalId,\n              createdById: resolvedCreatedById,\n              lastModifiedById: resolvedCreatedById,\n              teamId: importModel.createdBy.teamId,\n              apiImportId: importModel.id,\n              sourceMetadata: {\n                externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              },\n              createdAt: output.createdAt ?? now,\n              updatedAt: output.updatedAt ?? now,\n              publishedAt:\n                output.publishedAt ??\n                output.updatedAt ??\n                output.createdAt ??\n                now,\n            };\n\n            try {\n              await Document.findOrCreateWithCtx(\n                ctx,\n                {\n                  where: {\n                    id: internalId,\n                  },\n                  defaults,\n                  silent: true,\n                },\n                {\n                  name: \"create\",\n                  data: { title: output.title, source: \"import\" },\n                }\n              );\n            } catch (err) {\n              if (err instanceof UniqueConstraintError) {\n                Logger.error(\n                  `ImportsProcessor document creation failed due to unique constraint error (${internalId}: ${defaults.title})`,\n                  err,\n                  {\n                    fields: err.fields,\n                    documentId: internalId,\n                    title: defaults.title,\n                    collectionId: defaults.collectionId,\n                    parentDocumentId: defaults.parentDocumentId,\n                  }\n                );\n              }\n              throw err;\n            }\n\n            // Update document id for attachments in document content.\n            await Attachment.update(\n              { documentId: internalId },\n              { where: { documentId: externalId }, silent: true, transaction }\n            );\n          }\n        }\n*/\n\n// Access: attachmentWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/processors/ImportsProcessor.ts",
   "line": 400,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findAll()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const input of importTask.input) {\n            const externalId = input.externalId;\n            const internalId = await this.getInternalId(externalId, idMap);\n            const parentExternalId = input.parentExternalId;\n            const parentInternalId = parentExternalId\n              ? await this.getInternalId(parentExternalId, idMap)\n              : undefined;\n\n            const collectionExternalId = input.collectionExternalId;\n            const collectionInternalId = collectionExternalId\n              ? await this.getInternalId(collectionExternalId, idMap)\n              : undefined;\n\n            const output = outputMap[externalId];\n\n            // Skip this item if it has no output (likely due to an error during processing)\n            if (!output) {\n              Logger.debug(\n                \"processor\",\n                `Skipping item with no output: ${externalId}`\n              );\n              continue;\n            }\n\n            const collectionItem = importInput[externalId];\n\n            const attachments = await Attachment.findAll({\n              attributes: [\"id\", \"size\"],\n              where: { documentId: externalId }, // This will be set for root pages too (which will be imported as collection)\n              transaction,\n            });\n\n            const transformedContent = await this.rewriteReferences({\n              content: output.content,\n              attachments,\n              importInput,\n              idMap,\n              urlIdMap,\n              actorId: importModel.createdById,\n              teamId: importModel.teamId,\n            });\n\n            const resolvedCreatedById =\n              (await this.resolveExternalUserId(\n                output,\n                importModel.teamId,\n                userIdCache,\n                transaction\n              )) ?? importModel.createdById;\n\n            if (collectionItem) {\n              // imported collection will be placed in the beginning.\n              collectionIdx = fractionalIndex(null, collectionIdx);\n\n              const description = await DocumentHelper.toMarkdown(\n                transformedContent,\n                {\n                  includeTitle: false,\n                }\n              );\n\n              // Build sourceMetadata for the collection\n              const sourceMetadata: SourceMetadata = {\n                externalId: externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              };\n\n              const urlId = output.urlId\n                ? urlIdMap[output.urlId]?.urlId\n                : undefined;\n\n              const collection = Collection.build({\n                id: internalId,\n                urlId,\n                name: output.title,\n                icon: output.icon ?? \"collection\",\n                color:\n                  output.color ??\n                  (output.icon ? undefined : randomElement(colorPalette)),\n                content: transformedContent,\n                description: truncate(description, {\n                  length: CollectionValidation.maxDescriptionLength,\n                }),\n                createdById: resolvedCreatedById,\n                teamId: importModel.createdBy.teamId,\n                apiImportId: importModel.id,\n                index: collectionIdx,\n                sort: Collection.DEFAULT_SORT,\n                permission: collectionItem.permission,\n                sourceMetadata,\n                createdAt: output.createdAt ?? now,\n                updatedAt: output.updatedAt ?? now,\n              });\n\n              await collection.saveWithCtx(\n                ctx,\n                { silent: true },\n                {\n                  name: \"create\",\n                }\n              );\n\n              createdCollections.push(collection);\n\n              // Unset documentId for attachments in collection overview.\n              await Attachment.update(\n                { documentId: null },\n                { where: { documentId: externalId }, silent: true, transaction }\n              );\n\n              continue;\n            }\n\n            // Document at the root of a collection when there's no parent (or) the parent is the collection.\n            const isRootDocument =\n              !parentExternalId || !!importInput[parentExternalId];\n\n            const urlId = output.urlId\n              ? urlIdMap[output.urlId]?.urlId\n              : undefined;\n\n            const defaults = {\n              title: output.title,\n              urlId,\n              icon: output.icon,\n              color: output.color,\n              content: transformedContent,\n              text: await DocumentHelper.toMarkdown(transformedContent, {\n                includeTitle: false,\n              }),\n              collectionId: collectionInternalId,\n              parentDocumentId: isRootDocument ? undefined : parentInternalId,\n              createdById: resolvedCreatedById,\n              lastModifiedById: resolvedCreatedById,\n              teamId: importModel.createdBy.teamId,\n              apiImportId: importModel.id,\n              sourceMetadata: {\n                externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              },\n              createdAt: output.createdAt ?? now,\n              updatedAt: output.updatedAt ?? now,\n              publishedAt:\n                output.publishedAt ??\n                output.updatedAt ??\n                output.createdAt ??\n                now,\n            };\n\n            try {\n              await Document.findOrCreateWithCtx(\n                ctx,\n                {\n                  where: {\n                    id: internalId,\n                  },\n                  defaults,\n                  silent: true,\n                },\n                {\n                  name: \"create\",\n                  data: { title: output.title, source: \"import\" },\n                }\n              );\n            } catch (err) {\n              if (err instanceof UniqueConstraintError) {\n                Logger.error(\n                  `ImportsProcessor document creation failed due to unique constraint error (${internalId}: ${defaults.title})`,\n                  err,\n                  {\n                    fields: err.fields,\n                    documentId: internalId,\n                    title: defaults.title,\n                    collectionId: defaults.collectionId,\n                    parentDocumentId: defaults.parentDocumentId,\n                  }\n                );\n              }\n              throw err;\n            }\n\n            // Update document id for attachments in document content.\n            await Attachment.update(\n              { documentId: internalId },\n              { where: { documentId: externalId }, silent: true, transaction }\n            );\n          }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 59.349999999999994,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: input, importTask.input",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 12 async call(s) inside loop over importTask.input\n// Problem: N+1 queries where N = importTask.input.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = importTask.input.map(input => input.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const input of importTask.input) {\n            const externalId = input.externalId;\n            const internalId = dataMap.get(input.id);\n            const parentExternalId = input.parentExternalId;\n            const parentInternalId = parentExternalId\n              ? dataMap.get(input.id)\n              : undefined;\n\n            const collectionExternalId = input.collectionExternalId;\n            const collectionInternalId = collectionExternalId\n              ? dataMap.get(input.id)\n              : undefined;\n\n            const output = outputMap[externalId];\n\n            // Skip this item if it has no output (likely due to an error during processing)\n            if (!output) {\n              Logger.debug(\n                \"processor\",\n                `Skipping item with no output: ${externalId}`\n              );\n              continue;\n            }\n\n            const collectionItem = importInput[externalId];\n\n            const attachments = dataMap.get(input.id)\n              transaction,\n            });\n\n            const transformedContent = dataMap.get(input.id);\n\n            const resolvedCreatedById =\n              (dataMap.get(input.id)) ?? importModel.createdById;\n\n            if (collectionItem) {\n              // imported collection will be placed in the beginning.\n              collectionIdx = fractionalIndex(null, collectionIdx);\n\n              const description = dataMap.get(input.id);\n\n              // Build sourceMetadata for the collection\n              const sourceMetadata: SourceMetadata = {\n                externalId: externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              };\n\n              const urlId = output.urlId\n                ? urlIdMap[output.urlId]?.urlId\n                : undefined;\n\n              const collection = Collection.build({\n                id: internalId,\n                urlId,\n                name: output.title,\n                icon: output.icon ?? \"collection\",\n                color:\n                  output.color ??\n                  (output.icon ? undefined : randomElement(colorPalette)),\n                content: transformedContent,\n                description: truncate(description, {\n                  length: CollectionValidation.maxDescriptionLength,\n                }),\n                createdById: resolvedCreatedById,\n                teamId: importModel.createdBy.teamId,\n                apiImportId: importModel.id,\n                index: collectionIdx,\n                sort: Collection.DEFAULT_SORT,\n                permission: collectionItem.permission,\n                sourceMetadata,\n                createdAt: output.createdAt ?? now,\n                updatedAt: output.updatedAt ?? now,\n              });\n\n              dataMap.get(input.id);\n\n              createdCollections.push(collection);\n\n              // Unset documentId for attachments in collection overview.\n              dataMap.get(input.id);\n\n              continue;\n            }\n\n            // Document at the root of a collection when there's no parent (or) the parent is the collection.\n            const isRootDocument =\n              !parentExternalId || !!importInput[parentExternalId];\n\n            const urlId = output.urlId\n              ? urlIdMap[output.urlId]?.urlId\n              : undefined;\n\n            const defaults = {\n              title: output.title,\n              urlId,\n              icon: output.icon,\n              color: output.color,\n              content: transformedContent,\n              text: dataMap.get(input.id),\n              collectionId: collectionInternalId,\n              parentDocumentId: isRootDocument ? undefined : parentInternalId,\n              createdById: resolvedCreatedById,\n              lastModifiedById: resolvedCreatedById,\n              teamId: importModel.createdBy.teamId,\n              apiImportId: importModel.id,\n              sourceMetadata: {\n                externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              },\n              createdAt: output.createdAt ?? now,\n              updatedAt: output.updatedAt ?? now,\n              publishedAt:\n                output.publishedAt ??\n                output.updatedAt ??\n                output.createdAt ??\n                now,\n            };\n\n            try {\n              dataMap.get(input.id);\n            } catch (err) {\n              if (err instanceof UniqueConstraintError) {\n                Logger.error(\n                  `ImportsProcessor document creation failed due to unique constraint error (${internalId}: ${defaults.title})`,\n                  err,\n                  {\n                    fields: err.fields,\n                    documentId: internalId,\n                    title: defaults.title,\n                    collectionId: defaults.collectionId,\n                    parentDocumentId: defaults.parentDocumentId,\n                  }\n                );\n              }\n              throw err;\n            }\n\n            // Update document id for attachments in document content.\n            dataMap.get(input.id);\n          }\n\n// Performance: 1 query instead of N queries"
    },
    {
     "type": "memoize",
     "rank": 2,
     "fitnessScore": 59.2,
     "reasoning": "Added memoization for 3 repeated calls",
     "code": "const getInternalIdCache = new Map(); // Memoize this.getInternalId\nconst toMarkdownCache = new Map(); // Memoize DocumentHelper.toMarkdown\nconst updateCache = new Map(); // Memoize Attachment.update\n\nfor (const input of importTask.input) {\n            const externalId = input.externalId;\n            const internalId = await this.getInternalId(externalId, idMap);\n            const parentExternalId = input.parentExternalId;\n            const parentInternalId = parentExternalId\n              ? await this.getInternalId(parentExternalId, idMap)\n              : undefined;\n\n            const collectionExternalId = input.collectionExternalId;\n            const collectionInternalId = collectionExternalId\n              ? await this.getInternalId(collectionExternalId, idMap)\n              : undefined;\n\n            const output = outputMap[externalId];\n\n            // Skip this item if it has no output (likely due to an error during processing)\n            if (!output) {\n              Logger.debug(\n                \"processor\",\n                `Skipping item with no output: ${externalId}`\n              );\n              continue;\n            }\n\n            const collectionItem = importInput[externalId];\n\n            const attachments = await Attachment.findAll({\n              attributes: [\"id\", \"size\"],\n              where: { documentId: externalId }, // This will be set for root pages too (which will be imported as collection)\n              transaction,\n            });\n\n            const transformedContent = await this.rewriteReferences({\n              content: output.content,\n              attachments,\n              importInput,\n              idMap,\n              urlIdMap,\n              actorId: importModel.createdById,\n              teamId: importModel.teamId,\n            });\n\n            const resolvedCreatedById =\n              (await this.resolveExternalUserId(\n                output,\n                importModel.teamId,\n                userIdCache,\n                transaction\n              )) ?? importModel.createdById;\n\n            if (collectionItem) {\n              // imported collection will be placed in the beginning.\n              collectionIdx = fractionalIndex(null, collectionIdx);\n\n              const description = await DocumentHelper.toMarkdown(\n                transformedContent,\n                {\n                  includeTitle: false,\n                }\n              );\n\n              // Build sourceMetadata for the collection\n              const sourceMetadata: SourceMetadata = {\n                externalId: externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              };\n\n              const urlId = output.urlId\n                ? urlIdMap[output.urlId]?.urlId\n                : undefined;\n\n              const collection = Collection.build({\n                id: internalId,\n                urlId,\n                name: output.title,\n                icon: output.icon ?? \"collection\",\n                color:\n                  output.color ??\n                  (output.icon ? undefined : randomElement(colorPalette)),\n                content: transformedContent,\n                description: truncate(description, {\n                  length: CollectionValidation.maxDescriptionLength,\n                }),\n                createdById: resolvedCreatedById,\n                teamId: importModel.createdBy.teamId,\n                apiImportId: importModel.id,\n                index: collectionIdx,\n                sort: Collection.DEFAULT_SORT,\n                permission: collectionItem.permission,\n                sourceMetadata,\n                createdAt: output.createdAt ?? now,\n                updatedAt: output.updatedAt ?? now,\n              });\n\n              await collection.saveWithCtx(\n                ctx,\n                { silent: true },\n                {\n                  name: \"create\",\n                }\n              );\n\n              createdCollections.push(collection);\n\n              // Unset documentId for attachments in collection overview.\n              await Attachment.update(\n                { documentId: null },\n                { where: { documentId: externalId }, silent: true, transaction }\n              );\n\n              continue;\n            }\n\n            // Document at the root of a collection when there's no parent (or) the parent is the collection.\n            const isRootDocument =\n              !parentExternalId || !!importInput[parentExternalId];\n\n            const urlId = output.urlId\n              ? urlIdMap[output.urlId]?.urlId\n              : undefined;\n\n            const defaults = {\n              title: output.title,\n              urlId,\n              icon: output.icon,\n              color: output.color,\n              content: transformedContent,\n              text: await DocumentHelper.toMarkdown(transformedContent, {\n                includeTitle: false,\n              }),\n              collectionId: collectionInternalId,\n              parentDocumentId: isRootDocument ? undefined : parentInternalId,\n              createdById: resolvedCreatedById,\n              lastModifiedById: resolvedCreatedById,\n              teamId: importModel.createdBy.teamId,\n              apiImportId: importModel.id,\n              sourceMetadata: {\n                externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              },\n              createdAt: output.createdAt ?? now,\n              updatedAt: output.updatedAt ?? now,\n              publishedAt:\n                output.publishedAt ??\n                output.updatedAt ??\n                output.createdAt ??\n                now,\n            };\n\n            try {\n              await Document.findOrCreateWithCtx(\n                ctx,\n                {\n                  where: {\n                    id: internalId,\n                  },\n                  defaults,\n                  silent: true,\n                },\n                {\n                  name: \"create\",\n                  data: { title: output.title, source: \"import\" },\n                }\n              );\n            } catch (err) {\n              if (err instanceof UniqueConstraintError) {\n                Logger.error(\n                  `ImportsProcessor document creation failed due to unique constraint error (${internalId}: ${defaults.title})`,\n                  err,\n                  {\n                    fields: err.fields,\n                    documentId: internalId,\n                    title: defaults.title,\n                    collectionId: defaults.collectionId,\n                    parentDocumentId: defaults.parentDocumentId,\n                  }\n                );\n              }\n              throw err;\n            }\n\n            // Update document id for attachments in document content.\n            await Attachment.update(\n              { documentId: internalId },\n              { where: { documentId: externalId }, silent: true, transaction }\n            );\n          }"
    },
    {
     "type": "memoization",
     "rank": 3,
     "fitnessScore": 54.2,
     "reasoning": "Add memoization cache for repeated expensive calls\nPreserved: this.getInternalId, DocumentHelper.toMarkdown, Attachment.update",
     "code": "// OPTIMIZED: Memoization for repeated expensive calls\n// Found 3 repeated async call(s)\n\n// Memoize this.getInternalId (called 3x)\nconst getInternalIdCache = new Map();\nasync function memoizedGetInternalId(key) {\n  if (getInternalIdCache.has(key)) return getInternalIdCache.get(key);\n  const result = await this.getInternalId(key);\n  getInternalIdCache.set(key, result);\n  return result;\n}\n\n// Memoize DocumentHelper.toMarkdown (called 2x)\nconst toMarkdownCache = new Map();\nasync function memoizedToMarkdown(key) {\n  if (toMarkdownCache.has(key)) return toMarkdownCache.get(key);\n  const result = await DocumentHelper.toMarkdown(key);\n  toMarkdownCache.set(key, result);\n  return result;\n}\n\n// Memoize Attachment.update (called 2x)\nconst updateCache = new Map();\nasync function memoizedUpdate(key) {\n  if (updateCache.has(key)) return updateCache.get(key);\n  const result = await Attachment.update(key);\n  updateCache.set(key, result);\n  return result;\n}\n\n// Original code (replace calls with memoized versions):\nfor (const input of importTask.input) {\n            const externalId = input.externalId;\n            const internalId = await this.getInternalId(externalId, idMap);\n            const parentExternalId = input.parentExternalId;\n            const parentInternalId = parentExternalId\n              ? await this.getInternalId(parentExternalId, idMap)\n              : undefined;\n\n            const collectionExternalId = input.collectionExternalId;\n            const collectionInternalId = collectionExternalId\n              ? await this.getInternalId(collectionExternalId, idMap)\n              : undefined;\n\n            const output = outputMap[externalId];\n\n            // Skip this item if it has no output (likely due to an error during processing)\n            if (!output) {\n              Logger.debug(\n                \"processor\",\n                `Skipping item with no output: ${externalId}`\n              );\n              continue;\n            }\n\n            const collectionItem = importInput[externalId];\n\n            const attachments = await Attachment.findAll({\n              attributes: [\"id\", \"size\"],\n              where: { documentId: externalId }, // This will be set for root pages too (which will be imported as collection)\n              transaction,\n            });\n\n            const transformedContent = await this.rewriteReferences({\n              content: output.content,\n              attachments,\n              importInput,\n              idMap,\n              urlIdMap,\n              actorId: importModel.createdById,\n              teamId: importModel.teamId,\n            });\n\n            const resolvedCreatedById =\n              (await this.resolveExternalUserId(\n                output,\n                importModel.teamId,\n                userIdCache,\n                transaction\n              )) ?? importModel.createdById;\n\n            if (collectionItem) {\n              // imported collection will be placed in the beginning.\n              collectionIdx = fractionalIndex(null, collectionIdx);\n\n              const description = await DocumentHelper.toMarkdown(\n                transformedContent,\n                {\n                  includeTitle: false,\n                }\n              );\n\n              // Build sourceMetadata for the collection\n              const sourceMetadata: SourceMetadata = {\n                externalId: externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              };\n\n              const urlId = output.urlId\n                ? urlIdMap[output.urlId]?.urlId\n                : undefined;\n\n              const collection = Collection.build({\n                id: internalId,\n                urlId,\n                name: output.title,\n                icon: output.icon ?? \"collection\",\n                color:\n                  output.color ??\n                  (output.icon ? undefined : randomElement(colorPalette)),\n                content: transformedContent,\n                description: truncate(description, {\n                  length: CollectionValidation.maxDescriptionLength,\n                }),\n                createdById: resolvedCreatedById,\n                teamId: importModel.createdBy.teamId,\n                apiImportId: importModel.id,\n                index: collectionIdx,\n                sort: Collection.DEFAULT_SORT,\n                permission: collectionItem.permission,\n                sourceMetadata,\n                createdAt: output.createdAt ?? now,\n                updatedAt: output.updatedAt ?? now,\n              });\n\n              await collection.saveWithCtx(\n                ctx,\n                { silent: true },\n                {\n                  name: \"create\",\n                }\n              );\n\n              createdCollections.push(collection);\n\n              // Unset documentId for attachments in collection overview.\n              await Attachment.update(\n                { documentId: null },\n                { where: { documentId: externalId }, silent: true, transaction }\n              );\n\n              continue;\n            }\n\n            // Document at the root of a collection when there's no parent (or) the parent is the collection.\n            const isRootDocument =\n              !parentExternalId || !!importInput[parentExternalId];\n\n            const urlId = output.urlId\n              ? urlIdMap[output.urlId]?.urlId\n              : undefined;\n\n            const defaults = {\n              title: output.title,\n              urlId,\n              icon: output.icon,\n              color: output.color,\n              content: transformedContent,\n              text: await DocumentHelper.toMarkdown(transformedContent, {\n                includeTitle: false,\n              }),\n              collectionId: collectionInternalId,\n              parentDocumentId: isRootDocument ? undefined : parentInternalId,\n              createdById: resolvedCreatedById,\n              lastModifiedById: resolvedCreatedById,\n              teamId: importModel.createdBy.teamId,\n              apiImportId: importModel.id,\n              sourceMetadata: {\n                externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              },\n              createdAt: output.createdAt ?? now,\n              updatedAt: output.updatedAt ?? now,\n              publishedAt:\n                output.publishedAt ??\n                output.updatedAt ??\n                output.createdAt ??\n                now,\n            };\n\n            try {\n              await Document.findOrCreateWithCtx(\n                ctx,\n                {\n                  where: {\n                    id: internalId,\n                  },\n                  defaults,\n                  silent: true,\n                },\n                {\n                  name: \"create\",\n                  data: { title: output.title, source: \"import\" },\n                }\n              );\n            } catch (err) {\n              if (err instanceof UniqueConstraintError) {\n                Logger.error(\n                  `ImportsProcessor document creation failed due to unique constraint error (${internalId}: ${defaults.title})`,\n                  err,\n                  {\n                    fields: err.fields,\n                    documentId: internalId,\n                    title: defaults.title,\n                    collectionId: defaults.collectionId,\n                    parentDocumentId: defaults.parentDocumentId,\n                  }\n                );\n              }\n              throw err;\n            }\n\n            // Update document id for attachments in document content.\n            await Attachment.update(\n              { documentId: internalId },\n              { where: { documentId: externalId }, silent: true, transaction }\n            );\n          }"
    },
    {
     "type": "sequelize-include",
     "rank": 4,
     "fitnessScore": 53.7,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: Attachment",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for Attachment\n// Optimized: Single query with include\n\nconst attachmentWithRelations = await Attachment.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nfor (const input of importTask.input) {\n            const externalId = input.externalId;\n            const internalId = await this.getInternalId(externalId, idMap);\n            const parentExternalId = input.parentExternalId;\n            const parentInternalId = parentExternalId\n              ? await this.getInternalId(parentExternalId, idMap)\n              : undefined;\n\n            const collectionExternalId = input.collectionExternalId;\n            const collectionInternalId = collectionExternalId\n              ? await this.getInternalId(collectionExternalId, idMap)\n              : undefined;\n\n            const output = outputMap[externalId];\n\n            // Skip this item if it has no output (likely due to an error during processing)\n            if (!output) {\n              Logger.debug(\n                \"processor\",\n                `Skipping item with no output: ${externalId}`\n              );\n              continue;\n            }\n\n            const collectionItem = importInput[externalId];\n\n            const attachments = await Attachment.findAll({\n              attributes: [\"id\", \"size\"],\n              where: { documentId: externalId }, // This will be set for root pages too (which will be imported as collection)\n              transaction,\n            });\n\n            const transformedContent = await this.rewriteReferences({\n              content: output.content,\n              attachments,\n              importInput,\n              idMap,\n              urlIdMap,\n              actorId: importModel.createdById,\n              teamId: importModel.teamId,\n            });\n\n            const resolvedCreatedById =\n              (await this.resolveExternalUserId(\n                output,\n                importModel.teamId,\n                userIdCache,\n                transaction\n              )) ?? importModel.createdById;\n\n            if (collectionItem) {\n              // imported collection will be placed in the beginning.\n              collectionIdx = fractionalIndex(null, collectionIdx);\n\n              const description = await DocumentHelper.toMarkdown(\n                transformedContent,\n                {\n                  includeTitle: false,\n                }\n              );\n\n              // Build sourceMetadata for the collection\n              const sourceMetadata: SourceMetadata = {\n                externalId: externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              };\n\n              const urlId = output.urlId\n                ? urlIdMap[output.urlId]?.urlId\n                : undefined;\n\n              const collection = Collection.build({\n                id: internalId,\n                urlId,\n                name: output.title,\n                icon: output.icon ?? \"collection\",\n                color:\n                  output.color ??\n                  (output.icon ? undefined : randomElement(colorPalette)),\n                content: transformedContent,\n                description: truncate(description, {\n                  length: CollectionValidation.maxDescriptionLength,\n                }),\n                createdById: resolvedCreatedById,\n                teamId: importModel.createdBy.teamId,\n                apiImportId: importModel.id,\n                index: collectionIdx,\n                sort: Collection.DEFAULT_SORT,\n                permission: collectionItem.permission,\n                sourceMetadata,\n                createdAt: output.createdAt ?? now,\n                updatedAt: output.updatedAt ?? now,\n              });\n\n              await collection.saveWithCtx(\n                ctx,\n                { silent: true },\n                {\n                  name: \"create\",\n                }\n              );\n\n              createdCollections.push(collection);\n\n              // Unset documentId for attachments in collection overview.\n              await Attachment.update(\n                { documentId: null },\n                { where: { documentId: externalId }, silent: true, transaction }\n              );\n\n              continue;\n            }\n\n            // Document at the root of a collection when there's no parent (or) the parent is the collection.\n            const isRootDocument =\n              !parentExternalId || !!importInput[parentExternalId];\n\n            const urlId = output.urlId\n              ? urlIdMap[output.urlId]?.urlId\n              : undefined;\n\n            const defaults = {\n              title: output.title,\n              urlId,\n              icon: output.icon,\n              color: output.color,\n              content: transformedContent,\n              text: await DocumentHelper.toMarkdown(transformedContent, {\n                includeTitle: false,\n              }),\n              collectionId: collectionInternalId,\n              parentDocumentId: isRootDocument ? undefined : parentInternalId,\n              createdById: resolvedCreatedById,\n              lastModifiedById: resolvedCreatedById,\n              teamId: importModel.createdBy.teamId,\n              apiImportId: importModel.id,\n              sourceMetadata: {\n                externalId,\n                externalName: output.title,\n                createdByName: output.author,\n              },\n              createdAt: output.createdAt ?? now,\n              updatedAt: output.updatedAt ?? now,\n              publishedAt:\n                output.publishedAt ??\n                output.updatedAt ??\n                output.createdAt ??\n                now,\n            };\n\n            try {\n              await Document.findOrCreateWithCtx(\n                ctx,\n                {\n                  where: {\n                    id: internalId,\n                  },\n                  defaults,\n                  silent: true,\n                },\n                {\n                  name: \"create\",\n                  data: { title: output.title, source: \"import\" },\n                }\n              );\n            } catch (err) {\n              if (err instanceof UniqueConstraintError) {\n                Logger.error(\n                  `ImportsProcessor document creation failed due to unique constraint error (${internalId}: ${defaults.title})`,\n                  err,\n                  {\n                    fields: err.fields,\n                    documentId: internalId,\n                    title: defaults.title,\n                    collectionId: defaults.collectionId,\n                    parentDocumentId: defaults.parentDocumentId,\n                  }\n                );\n              }\n              throw err;\n            }\n\n            // Update document id for attachments in document content.\n            await Attachment.update(\n              { documentId: internalId },\n              { where: { documentId: externalId }, silent: true, transaction }\n            );\n          }\n*/\n\n// Access: attachmentWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/processors/BacklinksProcessor.ts",
   "line": 27,
   "severity": "high",
   "description": "Found 2 database queries (Sequelize.findByPk(), Sequelize.findOne()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "linkIds.map(async (linkId) => {\n            const linkedDocument = await Document.findByPk(linkId, {\n              attributes: [\"id\", \"teamId\"],\n            });\n\n            if (\n              !linkedDocument ||\n              linkedDocument.id === event.documentId ||\n              linkedDocument.teamId !== document.teamId\n            ) {\n              return;\n            }\n\n            const existing = await Relationship.findOne({\n              where: {\n                documentId: linkedDocument.id,\n                reverseDocumentId: event.documentId,\n                type: RelationshipType.Backlink,\n              },\n            });\n\n            if (!existing) {\n              await Relationship.create({\n                documentId: linkedDocument.id,\n                reverseDocumentId: event.documentId,\n                userId: document.lastModifiedById,\n                type: RelationshipType.Backlink,\n              });\n            }\n          })",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "sequelize-include",
     "rank": 1,
     "fitnessScore": 77.45,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nlinkIds.map(async (linkId) => {\n            const linkedDocument = await Document.findByPk(linkId, {\n              attributes: [\"id\", \"teamId\"],\n            });\n\n            if (\n              !linkedDocument ||\n              linkedDocument.id === event.documentId ||\n              linkedDocument.teamId !== document.teamId\n            ) {\n              return;\n            }\n\n            const existing = await Relationship.findOne({\n              where: {\n                documentId: linkedDocument.id,\n                reverseDocumentId: event.documentId,\n                type: RelationshipType.Backlink,\n              },\n            });\n\n            if (!existing) {\n              await Relationship.create({\n                documentId: linkedDocument.id,\n                reverseDocumentId: event.documentId,\n                userId: document.lastModifiedById,\n                type: RelationshipType.Backlink,\n              });\n            }\n          })\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/processors/BacklinksProcessor.ts",
   "line": 77,
   "severity": "high",
   "description": "Found 2 database queries (Sequelize.findByPk(), Sequelize.findOne()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "linkIds.map(async (linkId) => {\n            const linkedDocument = await Document.findByPk(linkId, {\n              attributes: [\"id\", \"teamId\"],\n            });\n\n            if (\n              !linkedDocument ||\n              linkedDocument.id === event.documentId ||\n              linkedDocument.teamId !== document.teamId\n            ) {\n              return;\n            }\n\n            const existing = await Relationship.findOne({\n              where: {\n                documentId: linkedDocument.id,\n                reverseDocumentId: event.documentId,\n                type: RelationshipType.Backlink,\n              },\n            });\n\n            if (!existing) {\n              await Relationship.create({\n                documentId: linkedDocument.id,\n                reverseDocumentId: event.documentId,\n                userId: document.lastModifiedById,\n                type: RelationshipType.Backlink,\n              });\n            }\n            linkedDocumentIds.push(linkedDocument.id);\n          })",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "sequelize-include",
     "rank": 1,
     "fitnessScore": 77.45,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\nlinkIds.map(async (linkId) => {\n            const linkedDocument = await Document.findByPk(linkId, {\n              attributes: [\"id\", \"teamId\"],\n            });\n\n            if (\n              !linkedDocument ||\n              linkedDocument.id === event.documentId ||\n              linkedDocument.teamId !== document.teamId\n            ) {\n              return;\n            }\n\n            const existing = await Relationship.findOne({\n              where: {\n                documentId: linkedDocument.id,\n                reverseDocumentId: event.documentId,\n                type: RelationshipType.Backlink,\n              },\n            });\n\n            if (!existing) {\n              await Relationship.create({\n                documentId: linkedDocument.id,\n                reverseDocumentId: event.documentId,\n                userId: document.lastModifiedById,\n                type: RelationshipType.Backlink,\n              });\n            }\n            linkedDocumentIds.push(linkedDocument.id);\n          })\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/queues/processors/ApiKeyCleanupProcessor.ts",
   "line": 19,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findAll()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "(\n        await User.findAll({\n          attributes: [\"id\"],\n          where: {\n            teamId: event.teamId,\n            role: {\n              [Op.ne]: UserRole.Admin,\n            },\n          },\n        })\n      ).map((u) => u.id)",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "sequelize-include",
     "rank": 1,
     "fitnessScore": 81.69999999999999,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: User",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for User\n// Optimized: Single query with include\n\nconst userWithRelations = await User.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\n(\n        await User.findAll({\n          attributes: [\"id\"],\n          where: {\n            teamId: event.teamId,\n            role: {\n              [Op.ne]: UserRole.Admin,\n            },\n          },\n        })\n      ).map((u) => u.id)\n*/\n\n// Access: userWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/routes/api/groups/groups.ts",
   "line": 167,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findAll()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "(\n          await Promise.all(\n            groups.map((group) =>\n              GroupUser.scope(\"withUser\").findAll({\n                where: {\n                  groupId: group.id,\n                },\n                order: [[\"permission\", \"ASC\"]],\n                limit: MAX_AVATAR_DISPLAY,\n              })\n            )\n          )\n        )\n          .flat()\n          .filter((groupUser) => groupUser.user)\n          .map((groupUser) =>\n            presentGroupUser(groupUser, { includeUser: true })\n          )",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "single-pass",
     "rank": 1,
     "fitnessScore": 89.5,
     "reasoning": "Converted filter().map() chain to single reduce() pass",
     "code": "(await Promise.all(groups.map(group => GroupUser.scope(\"withUser\").findAll({\n  where: {\n    groupId: group.id\n  },\n  order: [[\"permission\", \"ASC\"]],\n  limit: MAX_AVATAR_DISPLAY\n})))).flat().reduce((acc, groupUser) => {\n  if (groupUser.user) acc.push(presentGroupUser(groupUser, {\n    includeUser: true\n  }));\n  return acc;\n}, []);"
    },
    {
     "type": "sequelize-include",
     "rank": 2,
     "fitnessScore": 83.44999999999999,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\n(\n          await Promise.all(\n            groups.map((group) =>\n              GroupUser.scope(\"withUser\").findAll({\n                where: {\n                  groupId: group.id,\n                },\n                order: [[\"permission\", \"ASC\"]],\n                limit: MAX_AVATAR_DISPLAY,\n              })\n            )\n          )\n        )\n          .flat()\n          .filter((groupUser) => groupUser.user)\n          .map((groupUser) =>\n            presentGroupUser(groupUser, { includeUser: true })\n          )\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/routes/api/groups/groups.ts",
   "line": 169,
   "severity": "medium",
   "description": "Found 1 database query (Sequelize.findAll()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "groups.map((group) =>\n              GroupUser.scope(\"withUser\").findAll({\n                where: {\n                  groupId: group.id,\n                },\n                order: [[\"permission\", \"ASC\"]],\n                limit: MAX_AVATAR_DISPLAY,\n              })\n            )",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "sequelize-include",
     "rank": 1,
     "fitnessScore": 81.69999999999999,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\ngroups.map((group) =>\n              GroupUser.scope(\"withUser\").findAll({\n                where: {\n                  groupId: group.id,\n                },\n                order: [[\"permission\", \"ASC\"]],\n                limit: MAX_AVATAR_DISPLAY,\n              })\n            )\n*/\n\n// Access: modelWithRelations[0].relations"
    }
   ]
  },
  {
   "file": "server/routes/api/documents/documents.ts",
   "line": 140,
   "severity": "high",
   "description": "Found 2 database queries (Sequelize.findByPk(), Sequelize.findOne()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "documentIds.map(async (documentId) => {\n      if (\"user\" in context) {\n        const document = await Document.findByPk(documentId, {\n          userId: context.user.id,\n        });\n        authorize(context.user, \"read\", document);\n        expandedIds.set(documentId, [\n          documentId,\n          ...(await document.findAllChildDocumentIds()),\n        ]);\n      } else {\n        const document = await Document.unscoped().findOne({\n          where: { id: documentId, teamId: context.teamId },\n        });\n        expandedIds.set(\n          documentId,\n          document\n            ? [documentId, ...(await document.findAllChildDocumentIds())]\n            : [documentId]\n        );\n      }\n    })",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "sequelize-include",
     "rank": 1,
     "fitnessScore": 84.5,
     "reasoning": "Use Sequelize include for eager loading related data\nPreserved: ",
     "code": "// OPTIMIZED: Sequelize eager loading with include\n// Original: Separate findAll() calls for models\n// Optimized: Single query with include\n\nconst modelWithRelations = await Model.findAll({\n  include: [\n    // Add models here\n  ]\n});\n\n// Original code reference:\n/*\ndocumentIds.map(async (documentId) => {\n      if (\"user\" in context) {\n        const document = await Document.findByPk(documentId, {\n          userId: context.user.id,\n        });\n        authorize(context.user, \"read\", document);\n        expandedIds.set(documentId, [\n          documentId,\n          ...(await document.findAllChildDocumentIds()),\n        ]);\n      } else {\n        const document = await Document.unscoped().findOne({\n          where: { id: documentId, teamId: context.teamId },\n        });\n        expandedIds.set(\n          documentId,\n          document\n            ? [documentId, ...(await document.findAllChildDocumentIds())]\n            : [documentId]\n        );\n      }\n    })\n*/\n\n// Access: modelWithRelations[0].relations"
    },
    {
     "type": "memoization",
     "rank": 2,
     "fitnessScore": 82,
     "reasoning": "Add memoization cache for repeated expensive calls\nPreserved: document.findAllChildDocumentIds",
     "code": "// OPTIMIZED: Memoization for repeated expensive calls\n// Found 1 repeated async call(s)\n\n// Memoize document.findAllChildDocumentIds (called 2x)\nconst findAllChildDocumentIdsCache = new Map();\nasync function memoizedFindAllChildDocumentIds(key) {\n  if (findAllChildDocumentIdsCache.has(key)) return findAllChildDocumentIdsCache.get(key);\n  const result = await document.findAllChildDocumentIds(key);\n  findAllChildDocumentIdsCache.set(key, result);\n  return result;\n}\n\n// Original code (replace calls with memoized versions):\ndocumentIds.map(async (documentId) => {\n      if (\"user\" in context) {\n        const document = await Document.findByPk(documentId, {\n          userId: context.user.id,\n        });\n        authorize(context.user, \"read\", document);\n        expandedIds.set(documentId, [\n          documentId,\n          ...(await document.findAllChildDocumentIds()),\n        ]);\n      } else {\n        const document = await Document.unscoped().findOne({\n          where: { id: documentId, teamId: context.teamId },\n        });\n        expandedIds.set(\n          documentId,\n          document\n            ? [documentId, ...(await document.findAllChildDocumentIds())]\n            : [documentId]\n        );\n      }\n    })"
    },
    {
     "type": "memoize",
     "rank": 3,
     "fitnessScore": 81.69999999999999,
     "reasoning": "Added memoization for 1 repeated calls",
     "code": "const findAllChildDocumentIdsCache = new Map(); // Memoize document.findAllChildDocumentIds\n\ndocumentIds.map(async (documentId) => {\n      if (\"user\" in context) {\n        const document = await Document.findByPk(documentId, {\n          userId: context.user.id,\n        });\n        authorize(context.user, \"read\", document);\n        expandedIds.set(documentId, [\n          documentId,\n          ...(await document.findAllChildDocumentIds()),\n        ]);\n      } else {\n        const document = await Document.unscoped().findOne({\n          where: { id: documentId, teamId: context.teamId },\n        });\n        expandedIds.set(\n          documentId,\n          document\n            ? [documentId, ...(await document.findAllChildDocumentIds())]\n            : [documentId]\n        );\n      }\n    })"
    }
   ]
  },
  {
   "file": "server/routes/api/batch/batch.ts",
   "line": 222,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.all()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const group of chunk(requests, BatchConcurrency)) {\n        const results = await Promise.all(\n          group.map((request) =>\n            dispatch(ctx, request.method, request.body ?? {})\n          )\n        );\n        data.push(...results);\n      }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 84.6,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: group, chunk(requests, BatchConcurrency)",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over chunk(requests, BatchConcurrency)\n// Problem: N+1 queries where N = chunk(requests, BatchConcurrency).length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = chunk(requests, BatchConcurrency).map(group => group.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const group of chunk(requests, BatchConcurrency)) {\n        const results = dataMap.get(group.id) =>\n            dispatch(ctx, request.method, request.body ?? {})\n          )\n        );\n        data.push(...results);\n      }\n\n// Performance: 1 query instead of N queries"
    }
   ]
  },
  {
   "file": "server/routes/api/authenticationProviders/authenticationProviders.ts",
   "line": 127,
   "severity": "medium",
   "description": "Found 1 database query (Mongoose.find()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "AuthenticationHelper.providers\n      .filter((p) => p.value.id !== \"email\" && p.value.id !== \"passkeys\")\n      .map((p) => {\n        const row = teamAuthenticationProviders.find(\n          (t) => t.name === p.value.id\n        );\n        const groupSyncProvider = PluginManager.getGroupSyncProvider(\n          p.value.id\n        );\n\n        return {\n          id: p.value.id,\n          name: p.value.id,\n          displayName: p.name,\n          isEnabled: false,\n          isConnected: false,\n          groupSyncSupported: !!groupSyncProvider,\n          groupSyncUsesClaim: groupSyncProvider?.useGroupClaim ?? false,\n          ...(row ? presentAuthenticationProvider(row) : {}),\n        };\n      })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 81.65,
     "reasoning": "Pattern analysis for chained-methods with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: chained-methods\n// ORM/Framework: unknown\n// Variables: row, groupSyncProvider\n// Method calls: AuthenticationHelper.providers.filter(p => p.value.id !== \"email\" && p.value.id !== \"passkeys\").map(), AuthenticationHelper.providers.filter(), teamAuthenticationProviders.find(), PluginManager.getGroupSyncProvider()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\nAuthenticationHelper.providers\n      .filter((p) => p.value.id !== \"email\" && p.value.id !== \"passkeys\")\n      .map((p) => {\n        const row = teamAuthenticationProviders.find(\n          (t) => t.name === p.value.id\n        );\n        const groupSyncProvider = PluginManager.getGroupSyncProvider(\n          p.value.id\n        );\n\n        return {\n          id: p.value.id,\n          name: p.value.id,\n          displayName: p.name,\n          isEnabled: false,\n          isConnected: false,\n          groupSyncSupported: !!groupSyncProvider,\n          groupSyncUsesClaim: groupSyncProvider?.useGroupClaim ?? false,\n          ...(row ? presentAuthenticationProvider(row) : {}),\n        };\n      })\n\n// Recommended optimizations based on pattern:\n1. Consider using reduce() for single-pass processing\n// 2. Avoid creating intermediate arrays\n// 3. Use for...of loop for better performance on large arrays"
    }
   ]
  }
 ]
}