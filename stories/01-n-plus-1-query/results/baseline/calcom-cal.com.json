{
 "filesScanned": 398,
 "n1Findings": 22,
 "findings": [
  {
   "file": "packages/trpc/server/routers/loggedInViewer/connectAndJoin.handler.ts",
   "line": 186,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "updatedBooking.attendees.map(async (attendee) => {\n    const locale = attendee.locale ?? \"en\";\n    let translate = translations.get(locale);\n    if (!translate) {\n      translate = await getTranslation(locale, \"common\");\n      translations.set(locale, translate);\n    }\n    return {\n      name: attendee.name,\n      email: attendee.email,\n      timeZone: attendee.timeZone,\n      language: {\n        translate,\n        locale,\n      },\n    };\n  })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.15,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: locale, translate\n// Method calls: updatedBooking.attendees.map(), translations.get(), translations.set()\n// Loop structure: none\n// Async calls: 1\n\n// Original code:\nupdatedBooking.attendees.map(async (attendee) => {\n    const locale = attendee.locale ?? \"en\";\n    let translate = translations.get(locale);\n    if (!translate) {\n      translate = await getTranslation(locale, \"common\");\n      translations.set(locale, translate);\n    }\n    return {\n      name: attendee.name,\n      email: attendee.email,\n      timeZone: attendee.timeZone,\n      language: {\n        translate,\n        locale,\n      },\n    };\n  })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/slots/util.ts",
   "line": 220,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "occupiedSeats.forEach((item) => {\n      const dateKey = item.slotUtcStartDate.toISOString();\n      occupiedSeatsMap.set(dateKey, (occupiedSeatsMap.get(dateKey) || 0) + 1);\n    })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.15,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: dateKey\n// Method calls: occupiedSeats.forEach(), item.slotUtcStartDate.toISOString(), occupiedSeatsMap.set(), occupiedSeatsMap.get()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\noccupiedSeats.forEach((item) => {\n      const dateKey = item.slotUtcStartDate.toISOString();\n      occupiedSeatsMap.set(dateKey, (occupiedSeatsMap.get(dateKey) || 0) + 1);\n    })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/slots/util.ts",
   "line": 478,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const user of users) {\n      const userBookings = busyTimesFromLimitsBookings.filter((booking) => booking.userId === user.id);\n      const limitManager = new LimitManager();\n\n      limitManager.mergeBusyTimes(globalLimitManager);\n\n      if (bookingLimits) {\n        for (const key of descendingLimitKeys) {\n          const limit = bookingLimits?.[key];\n          if (!limit) continue;\n\n          const unit = intervalLimitKeyToUnit(key);\n          const periodStartDates = this.dependencies.userAvailabilityService.getPeriodStartDatesBetween(\n            dateFrom,\n            dateTo,\n            unit,\n            timeZone\n          );\n\n          for (const periodStart of periodStartDates) {\n            if (limitManager.isAlreadyBusy(periodStart, unit, timeZone)) continue;\n\n            const { title, source } = LimitSources.eventBookingLimit({ limit, unit });\n\n            if (unit === \"year\") {\n              try {\n                await this.dependencies.checkBookingLimitsService.checkBookingLimit({\n                  eventStartDate: periodStart.toDate(),\n                  limitingNumber: limit,\n                  eventId: eventType.id,\n                  key,\n                  user,\n                  rescheduleUid,\n                  timeZone,\n                });\n              } catch {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title,\n                  source,\n                });\n                if (\n                  periodStartDates.every((start: Dayjs) => limitManager.isAlreadyBusy(start, unit, timeZone))\n                ) {\n                  break;\n                }\n              }\n              continue;\n            }\n\n            const periodEnd = periodStart.endOf(unit);\n            let totalBookings = 0;\n\n            for (const booking of userBookings) {\n              if (!isBookingWithinPeriod(booking, periodStart, periodEnd, timeZone)) {\n                continue;\n              }\n\n              totalBookings++;\n              if (totalBookings >= limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title,\n                  source,\n                });\n                break;\n              }\n            }\n          }\n        }\n      }\n\n      if (durationLimits) {\n        for (const key of descendingLimitKeys) {\n          const limit = durationLimits?.[key];\n          if (!limit) continue;\n\n          const unit = intervalLimitKeyToUnit(key);\n          const periodStartDates = this.dependencies.userAvailabilityService.getPeriodStartDatesBetween(\n            dateFrom,\n            dateTo,\n            unit,\n            timeZone\n          );\n\n          for (const periodStart of periodStartDates) {\n            if (limitManager.isAlreadyBusy(periodStart, unit, timeZone)) continue;\n\n            const selectedDuration = (duration || eventType.length) ?? 0;\n\n            const { title: durationTitle, source: durationSource } = LimitSources.eventDurationLimit({ limit, unit });\n\n            if (selectedDuration > limit) {\n              limitManager.addBusyTime({\n                start: periodStart,\n                unit,\n                timeZone,\n                title: durationTitle,\n                source: durationSource,\n              });\n              continue;\n            }\n\n            if (unit === \"year\") {\n              const yearKey = periodStart.format(\"YYYY\");\n              if (!yearlyDurationTotals.has(yearKey) && !missingYearlyDurationTotals.has(yearKey)) {\n                missingYearlyDurationTotals.add(yearKey);\n                log.warn(\"[DURATION LIMIT CACHE MISS] Missing pre-fetched yearly duration total\", {\n                  eventTypeId: eventType.id,\n                  durationLimitKey: key,\n                  yearKey,\n                  dateFrom: dateFrom.toISOString(),\n                  dateTo: dateTo.toISOString(),\n                  timeZone,\n                });\n              }\n              const totalYearlyDuration = yearlyDurationTotals.get(yearKey) ?? 0;\n              if (totalYearlyDuration + selectedDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                if (\n                  periodStartDates.every((start: Dayjs) => limitManager.isAlreadyBusy(start, unit, timeZone))\n                ) {\n                  break;\n                }\n              }\n              continue;\n            }\n\n            const periodEnd = periodStart.endOf(unit);\n            let totalDuration = selectedDuration;\n\n            for (const booking of userBookings) {\n              if (!isBookingWithinPeriod(booking, periodStart, periodEnd, timeZone)) {\n                continue;\n              }\n              totalDuration += dayjs(booking.end).diff(dayjs(booking.start), \"minute\");\n              if (totalDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                break;\n              }\n            }\n          }\n        }\n      }\n\n      userBusyTimesMap.set(user.id, limitManager.getBusyTimes());\n    }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "batch-query-before-loop",
     "rank": 1,
     "fitnessScore": 54.55,
     "reasoning": "Extract queries from loop and batch them before iteration\nPreserved: booking, userBookings",
     "code": "// OPTIMIZED: Batch query before loop\n// Original: 1 async call(s) inside loop over userBookings\n// Problem: N+1 queries where N = userBookings.length\n\n// Step 1: Collect all IDs/keys needed\nconst allIds = userBookings.map(booking => booking.id);\n\n// Step 2: Batch query (single database call)\nconst allData = await batchQuery(allIds); // Replace with actual batch query\nconst dataMap = new Map(allData.map(d => [d.id, d]));\n\n// Step 3: Original loop (now uses cached data)\nfor (const user of users) {\n      const userBookings = busyTimesFromLimitsBookings.filter((booking) => booking.userId === user.id);\n      const limitManager = new LimitManager();\n\n      limitManager.mergeBusyTimes(globalLimitManager);\n\n      if (bookingLimits) {\n        for (const key of descendingLimitKeys) {\n          const limit = bookingLimits?.[key];\n          if (!limit) continue;\n\n          const unit = intervalLimitKeyToUnit(key);\n          const periodStartDates = this.dependencies.userAvailabilityService.getPeriodStartDatesBetween(\n            dateFrom,\n            dateTo,\n            unit,\n            timeZone\n          );\n\n          for (const periodStart of periodStartDates) {\n            if (limitManager.isAlreadyBusy(periodStart, unit, timeZone)) continue;\n\n            const { title, source } = LimitSources.eventBookingLimit({ limit, unit });\n\n            if (unit === \"year\") {\n              try {\n                await this.dependencies.checkBookingLimitsService.checkBookingLimit({\n                  eventStartDate: periodStart.toDate(),\n                  limitingNumber: limit,\n                  eventId: eventType.id,\n                  key,\n                  user,\n                  rescheduleUid,\n                  timeZone,\n                });\n              } catch {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title,\n                  source,\n                });\n                if (\n                  periodStartDates.every((start: Dayjs) => limitManager.isAlreadyBusy(start, unit, timeZone))\n                ) {\n                  break;\n                }\n              }\n              continue;\n            }\n\n            const periodEnd = periodStart.endOf(unit);\n            let totalBookings = 0;\n\n            for (const booking of userBookings) {\n              if (!isBookingWithinPeriod(booking, periodStart, periodEnd, timeZone)) {\n                continue;\n              }\n\n              totalBookings++;\n              if (totalBookings >= limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title,\n                  source,\n                });\n                break;\n              }\n            }\n          }\n        }\n      }\n\n      if (durationLimits) {\n        for (const key of descendingLimitKeys) {\n          const limit = durationLimits?.[key];\n          if (!limit) continue;\n\n          const unit = intervalLimitKeyToUnit(key);\n          const periodStartDates = this.dependencies.userAvailabilityService.getPeriodStartDatesBetween(\n            dateFrom,\n            dateTo,\n            unit,\n            timeZone\n          );\n\n          for (const periodStart of periodStartDates) {\n            if (limitManager.isAlreadyBusy(periodStart, unit, timeZone)) continue;\n\n            const selectedDuration = (duration || eventType.length) ?? 0;\n\n            const { title: durationTitle, source: durationSource } = LimitSources.eventDurationLimit({ limit, unit });\n\n            if (selectedDuration > limit) {\n              limitManager.addBusyTime({\n                start: periodStart,\n                unit,\n                timeZone,\n                title: durationTitle,\n                source: durationSource,\n              });\n              continue;\n            }\n\n            if (unit === \"year\") {\n              const yearKey = periodStart.format(\"YYYY\");\n              if (!yearlyDurationTotals.has(yearKey) && !missingYearlyDurationTotals.has(yearKey)) {\n                missingYearlyDurationTotals.add(yearKey);\n                log.warn(\"[DURATION LIMIT CACHE MISS] Missing pre-fetched yearly duration total\", {\n                  eventTypeId: eventType.id,\n                  durationLimitKey: key,\n                  yearKey,\n                  dateFrom: dateFrom.toISOString(),\n                  dateTo: dateTo.toISOString(),\n                  timeZone,\n                });\n              }\n              const totalYearlyDuration = yearlyDurationTotals.get(yearKey) ?? 0;\n              if (totalYearlyDuration + selectedDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                if (\n                  periodStartDates.every((start: Dayjs) => limitManager.isAlreadyBusy(start, unit, timeZone))\n                ) {\n                  break;\n                }\n              }\n              continue;\n            }\n\n            const periodEnd = periodStart.endOf(unit);\n            let totalDuration = selectedDuration;\n\n            for (const booking of userBookings) {\n              if (!isBookingWithinPeriod(booking, periodStart, periodEnd, timeZone)) {\n                continue;\n              }\n              totalDuration += dayjs(booking.end).diff(dayjs(booking.start), \"minute\");\n              if (totalDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                break;\n              }\n            }\n          }\n        }\n      }\n\n      userBusyTimesMap.set(user.id, limitManager.getBusyTimes());\n    }\n\n// Performance: 1 query instead of N queries"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/slots/util.ts",
   "line": 555,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const key of descendingLimitKeys) {\n          const limit = durationLimits?.[key];\n          if (!limit) continue;\n\n          const unit = intervalLimitKeyToUnit(key);\n          const periodStartDates = this.dependencies.userAvailabilityService.getPeriodStartDatesBetween(\n            dateFrom,\n            dateTo,\n            unit,\n            timeZone\n          );\n\n          for (const periodStart of periodStartDates) {\n            if (limitManager.isAlreadyBusy(periodStart, unit, timeZone)) continue;\n\n            const selectedDuration = (duration || eventType.length) ?? 0;\n\n            const { title: durationTitle, source: durationSource } = LimitSources.eventDurationLimit({ limit, unit });\n\n            if (selectedDuration > limit) {\n              limitManager.addBusyTime({\n                start: periodStart,\n                unit,\n                timeZone,\n                title: durationTitle,\n                source: durationSource,\n              });\n              continue;\n            }\n\n            if (unit === \"year\") {\n              const yearKey = periodStart.format(\"YYYY\");\n              if (!yearlyDurationTotals.has(yearKey) && !missingYearlyDurationTotals.has(yearKey)) {\n                missingYearlyDurationTotals.add(yearKey);\n                log.warn(\"[DURATION LIMIT CACHE MISS] Missing pre-fetched yearly duration total\", {\n                  eventTypeId: eventType.id,\n                  durationLimitKey: key,\n                  yearKey,\n                  dateFrom: dateFrom.toISOString(),\n                  dateTo: dateTo.toISOString(),\n                  timeZone,\n                });\n              }\n              const totalYearlyDuration = yearlyDurationTotals.get(yearKey) ?? 0;\n              if (totalYearlyDuration + selectedDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                if (\n                  periodStartDates.every((start: Dayjs) => limitManager.isAlreadyBusy(start, unit, timeZone))\n                ) {\n                  break;\n                }\n              }\n              continue;\n            }\n\n            const periodEnd = periodStart.endOf(unit);\n            let totalDuration = selectedDuration;\n\n            for (const booking of userBookings) {\n              if (!isBookingWithinPeriod(booking, periodStart, periodEnd, timeZone)) {\n                continue;\n              }\n              totalDuration += dayjs(booking.end).diff(dayjs(booking.start), \"minute\");\n              if (totalDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                break;\n              }\n            }\n          }\n        }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 68.35,
     "reasoning": "Pattern analysis for loop-with-calls with angular-forms framework",
     "code": "// Analysis of original code:\n// Pattern type: loop-with-calls\n// ORM/Framework: angular-forms\n// Variables: key, limit, unit, periodStartDates, periodStart, selectedDuration, yearKey, totalYearlyDuration, periodEnd, totalDuration, booking\n// Method calls: this.dependencies.userAvailabilityService.getPeriodStartDatesBetween(), limitManager.isAlreadyBusy(), LimitSources.eventDurationLimit(), limitManager.addBusyTime(), periodStart.format(), yearlyDurationTotals.has(), missingYearlyDurationTotals.has(), missingYearlyDurationTotals.add(), log.warn(), dateFrom.toISOString(), dateTo.toISOString(), yearlyDurationTotals.get(), limitManager.addBusyTime(), periodStartDates.every(), limitManager.isAlreadyBusy(), periodStart.endOf(), dayjs(booking.end).diff(), limitManager.addBusyTime()\n// Loop structure: for-of over userBookings\n// Async calls: 0\n\n// Original code:\nfor (const key of descendingLimitKeys) {\n          const limit = durationLimits?.[key];\n          if (!limit) continue;\n\n          const unit = intervalLimitKeyToUnit(key);\n          const periodStartDates = this.dependencies.userAvailabilityService.getPeriodStartDatesBetween(\n            dateFrom,\n            dateTo,\n            unit,\n            timeZone\n          );\n\n          for (const periodStart of periodStartDates) {\n            if (limitManager.isAlreadyBusy(periodStart, unit, timeZone)) continue;\n\n            const selectedDuration = (duration || eventType.length) ?? 0;\n\n            const { title: durationTitle, source: durationSource } = LimitSources.eventDurationLimit({ limit, unit });\n\n            if (selectedDuration > limit) {\n              limitManager.addBusyTime({\n                start: periodStart,\n                unit,\n                timeZone,\n                title: durationTitle,\n                source: durationSource,\n              });\n              continue;\n            }\n\n            if (unit === \"year\") {\n              const yearKey = periodStart.format(\"YYYY\");\n              if (!yearlyDurationTotals.has(yearKey) && !missingYearlyDurationTotals.has(yearKey)) {\n                missingYearlyDurationTotals.add(yearKey);\n                log.warn(\"[DURATION LIMIT CACHE MISS] Missing pre-fetched yearly duration total\", {\n                  eventTypeId: eventType.id,\n                  durationLimitKey: key,\n                  yearKey,\n                  dateFrom: dateFrom.toISOString(),\n                  dateTo: dateTo.toISOString(),\n                  timeZone,\n                });\n              }\n              const totalYearlyDuration = yearlyDurationTotals.get(yearKey) ?? 0;\n              if (totalYearlyDuration + selectedDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                if (\n                  periodStartDates.every((start: Dayjs) => limitManager.isAlreadyBusy(start, unit, timeZone))\n                ) {\n                  break;\n                }\n              }\n              continue;\n            }\n\n            const periodEnd = periodStart.endOf(unit);\n            let totalDuration = selectedDuration;\n\n            for (const booking of userBookings) {\n              if (!isBookingWithinPeriod(booking, periodStart, periodEnd, timeZone)) {\n                continue;\n              }\n              totalDuration += dayjs(booking.end).diff(dayjs(booking.start), \"minute\");\n              if (totalDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                break;\n              }\n            }\n          }\n        }\n\n// Recommended optimizations based on pattern:\n1. Extract queries before the loop\n// 2. Use batch queries with IN clause\n// 3. Build a Map for O(1) lookups inside the loop\n// - Use getRawValue() instead of multiple get() calls\n// - Cache form values before mapping"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/slots/util.ts",
   "line": 567,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const periodStart of periodStartDates) {\n            if (limitManager.isAlreadyBusy(periodStart, unit, timeZone)) continue;\n\n            const selectedDuration = (duration || eventType.length) ?? 0;\n\n            const { title: durationTitle, source: durationSource } = LimitSources.eventDurationLimit({ limit, unit });\n\n            if (selectedDuration > limit) {\n              limitManager.addBusyTime({\n                start: periodStart,\n                unit,\n                timeZone,\n                title: durationTitle,\n                source: durationSource,\n              });\n              continue;\n            }\n\n            if (unit === \"year\") {\n              const yearKey = periodStart.format(\"YYYY\");\n              if (!yearlyDurationTotals.has(yearKey) && !missingYearlyDurationTotals.has(yearKey)) {\n                missingYearlyDurationTotals.add(yearKey);\n                log.warn(\"[DURATION LIMIT CACHE MISS] Missing pre-fetched yearly duration total\", {\n                  eventTypeId: eventType.id,\n                  durationLimitKey: key,\n                  yearKey,\n                  dateFrom: dateFrom.toISOString(),\n                  dateTo: dateTo.toISOString(),\n                  timeZone,\n                });\n              }\n              const totalYearlyDuration = yearlyDurationTotals.get(yearKey) ?? 0;\n              if (totalYearlyDuration + selectedDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                if (\n                  periodStartDates.every((start: Dayjs) => limitManager.isAlreadyBusy(start, unit, timeZone))\n                ) {\n                  break;\n                }\n              }\n              continue;\n            }\n\n            const periodEnd = periodStart.endOf(unit);\n            let totalDuration = selectedDuration;\n\n            for (const booking of userBookings) {\n              if (!isBookingWithinPeriod(booking, periodStart, periodEnd, timeZone)) {\n                continue;\n              }\n              totalDuration += dayjs(booking.end).diff(dayjs(booking.start), \"minute\");\n              if (totalDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                break;\n              }\n            }\n          }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 72.15,
     "reasoning": "Pattern analysis for loop-with-calls with angular-forms framework",
     "code": "// Analysis of original code:\n// Pattern type: loop-with-calls\n// ORM/Framework: angular-forms\n// Variables: periodStart, selectedDuration, yearKey, totalYearlyDuration, periodEnd, totalDuration, booking\n// Method calls: limitManager.isAlreadyBusy(), LimitSources.eventDurationLimit(), limitManager.addBusyTime(), periodStart.format(), yearlyDurationTotals.has(), missingYearlyDurationTotals.has(), missingYearlyDurationTotals.add(), log.warn(), dateFrom.toISOString(), dateTo.toISOString(), yearlyDurationTotals.get(), limitManager.addBusyTime(), periodStartDates.every(), limitManager.isAlreadyBusy(), periodStart.endOf(), dayjs(booking.end).diff(), limitManager.addBusyTime()\n// Loop structure: for-of over userBookings\n// Async calls: 0\n\n// Original code:\nfor (const periodStart of periodStartDates) {\n            if (limitManager.isAlreadyBusy(periodStart, unit, timeZone)) continue;\n\n            const selectedDuration = (duration || eventType.length) ?? 0;\n\n            const { title: durationTitle, source: durationSource } = LimitSources.eventDurationLimit({ limit, unit });\n\n            if (selectedDuration > limit) {\n              limitManager.addBusyTime({\n                start: periodStart,\n                unit,\n                timeZone,\n                title: durationTitle,\n                source: durationSource,\n              });\n              continue;\n            }\n\n            if (unit === \"year\") {\n              const yearKey = periodStart.format(\"YYYY\");\n              if (!yearlyDurationTotals.has(yearKey) && !missingYearlyDurationTotals.has(yearKey)) {\n                missingYearlyDurationTotals.add(yearKey);\n                log.warn(\"[DURATION LIMIT CACHE MISS] Missing pre-fetched yearly duration total\", {\n                  eventTypeId: eventType.id,\n                  durationLimitKey: key,\n                  yearKey,\n                  dateFrom: dateFrom.toISOString(),\n                  dateTo: dateTo.toISOString(),\n                  timeZone,\n                });\n              }\n              const totalYearlyDuration = yearlyDurationTotals.get(yearKey) ?? 0;\n              if (totalYearlyDuration + selectedDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                if (\n                  periodStartDates.every((start: Dayjs) => limitManager.isAlreadyBusy(start, unit, timeZone))\n                ) {\n                  break;\n                }\n              }\n              continue;\n            }\n\n            const periodEnd = periodStart.endOf(unit);\n            let totalDuration = selectedDuration;\n\n            for (const booking of userBookings) {\n              if (!isBookingWithinPeriod(booking, periodStart, periodEnd, timeZone)) {\n                continue;\n              }\n              totalDuration += dayjs(booking.end).diff(dayjs(booking.start), \"minute\");\n              if (totalDuration > limit) {\n                limitManager.addBusyTime({\n                  start: periodStart,\n                  unit,\n                  timeZone,\n                  title: durationTitle,\n                  source: durationSource,\n                });\n                break;\n              }\n            }\n          }\n\n// Recommended optimizations based on pattern:\n1. Extract queries before the loop\n// 2. Use batch queries with IN clause\n// 3. Build a Map for O(1) lookups inside the loop\n// - Use getRawValue() instead of multiple get() calls\n// - Cache form values before mapping"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/eventTypes/util.ts",
   "line": 321,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.all()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "(eventType.children || []).map(async (c) => ({\n      ...c,\n      users: await Promise.all(\n        c.users.map(\n          async (u) =>\n            await new UserRepository(prisma).enrichUserWithItsProfile({\n              user: u,\n            })\n        )\n      ),\n    }))",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.85,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: none detected\n// Method calls: eventType.children || [].map(), Promise.all(), c.users.map(), new UserRepository(prisma).enrichUserWithItsProfile()\n// Loop structure: none\n// Async calls: 2\n\n// Original code:\n(eventType.children || []).map(async (c) => ({\n      ...c,\n      users: await Promise.all(\n        c.users.map(\n          async (u) =>\n            await new UserRepository(prisma).enrichUserWithItsProfile({\n              user: u,\n            })\n        )\n      ),\n    }))\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/eventTypes/getHostsWithLocationOptions.handler.ts",
   "line": 72,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "hosts.map((host, index) => ({\n    userId: host.userId,\n    name: host.user.name,\n    email: host.user.email,\n    avatarUrl: host.user.avatarUrl,\n    defaultConferencingApp: userMetadata.safeParse(host.user.metadata).data?.defaultConferencingApp ?? null,\n    location: host.location,\n    installedApps: enrichedUsers[index].credentials.map((cred) => {\n      const appMeta = cred.appId ? appMetadataBySlug.get(cred.appId) : null;\n      const locationData = appMeta?.appData?.location;\n\n      return {\n        appId: cred.appId,\n        credentialId: cred.id,\n        type: cred.type,\n        locationOption: locationData\n          ? {\n              value: locationData.type,\n              label: locationData.label || appMeta?.name || cred.appId || \"\",\n              icon: appMeta?.logo,\n            }\n          : undefined,\n      };\n    }),\n  }))",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 80.3,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: appMeta, locationData\n// Method calls: hosts.map(), userMetadata.safeParse(), enrichedUsers[index].credentials.map(), appMetadataBySlug.get()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\nhosts.map((host, index) => ({\n    userId: host.userId,\n    name: host.user.name,\n    email: host.user.email,\n    avatarUrl: host.user.avatarUrl,\n    defaultConferencingApp: userMetadata.safeParse(host.user.metadata).data?.defaultConferencingApp ?? null,\n    location: host.location,\n    installedApps: enrichedUsers[index].credentials.map((cred) => {\n      const appMeta = cred.appId ? appMetadataBySlug.get(cred.appId) : null;\n      const locationData = appMeta?.appData?.location;\n\n      return {\n        appId: cred.appId,\n        credentialId: cred.id,\n        type: cred.type,\n        locationOption: locationData\n          ? {\n              value: locationData.type,\n              label: locationData.label || appMeta?.name || cred.appId || \"\",\n              icon: appMeta?.logo,\n            }\n          : undefined,\n      };\n    }),\n  }))\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/eventTypes/getHostsWithLocationOptions.handler.ts",
   "line": 79,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "enrichedUsers[index].credentials.map((cred) => {\n      const appMeta = cred.appId ? appMetadataBySlug.get(cred.appId) : null;\n      const locationData = appMeta?.appData?.location;\n\n      return {\n        appId: cred.appId,\n        credentialId: cred.id,\n        type: cred.type,\n        locationOption: locationData\n          ? {\n              value: locationData.type,\n              label: locationData.label || appMeta?.name || cred.appId || \"\",\n              icon: appMeta?.logo,\n            }\n          : undefined,\n      };\n    })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 84.65,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: appMeta, locationData\n// Method calls: enrichedUsers[index].credentials.map(), appMetadataBySlug.get()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\nenrichedUsers[index].credentials.map((cred) => {\n      const appMeta = cred.appId ? appMetadataBySlug.get(cred.appId) : null;\n      const locationData = appMeta?.appData?.location;\n\n      return {\n        appId: cred.appId,\n        credentialId: cred.id,\n        type: cred.type,\n        locationOption: locationData\n          ? {\n              value: locationData.type,\n              label: locationData.label || appMeta?.name || cred.appId || \"\",\n              icon: appMeta?.logo,\n            }\n          : undefined,\n      };\n    })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/bookings/get.handler.ts",
   "line": 748,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.findUnique()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "plainBookings.map(async (booking) => {\n      // If seats are enabled, the event is not set to show attendees, and the current user is not the host, filter out attendees who are not the current user\n      if (\n        booking.seatsReferences.length &&\n        !booking.eventType?.seatsShowAttendees &&\n        !checkIfUserIsHost(user.id, booking)\n      ) {\n        booking.attendees = booking.attendees.filter((attendee) => attendee.email === user.email);\n      }\n\n      let rescheduler = null;\n      if (booking.fromReschedule) {\n        const rescheduledBooking = await prisma.booking.findUnique({\n          where: {\n            uid: booking.fromReschedule,\n          },\n          select: {\n            rescheduledBy: true,\n          },\n        });\n        if (rescheduledBooking) {\n          rescheduler = rescheduledBooking.rescheduledBy;\n        }\n      }\n\n      return {\n        ...booking,\n        rescheduler,\n        eventType: {\n          ...booking.eventType,\n          recurringEvent: parseRecurringEvent(booking.eventType?.recurringEvent),\n          eventTypeColor: parseEventTypeColor(booking.eventType?.eventTypeColor),\n          price: booking.eventType?.price || 0,\n          currency: booking.eventType?.currency || \"usd\",\n          metadata: EventTypeMetaDataSchema.parse(booking.eventType?.metadata || {}),\n        },\n        startTime: booking.startTime.toISOString(),\n        endTime: booking.endTime.toISOString(),\n      };\n    })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "prisma-include",
     "rank": 1,
     "fitnessScore": 77.94999999999999,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: booking",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for booking and related data\n// Optimized: Single query with include\n\nconst bookingWithRelations = await prisma.booking.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\nplainBookings.map(async (booking) => {\n      // If seats are enabled, the event is not set to show attendees, and the current user is not the host, filter out attendees who are not the current user\n      if (\n        booking.seatsReferences.length &&\n        !booking.eventType?.seatsShowAttendees &&\n        !checkIfUserIsHost(user.id, booking)\n      ) {\n        booking.attendees = booking.attendees.filter((attendee) => attendee.email === user.email);\n      }\n\n      let rescheduler = null;\n      if (booking.fromReschedule) {\n        const rescheduledBooking = await prisma.booking.findUnique({\n          where: {\n            uid: booking.fromReschedule,\n          },\n          select: {\n            rescheduledBy: true,\n          },\n        });\n        if (rescheduledBooking) {\n          rescheduler = rescheduledBooking.rescheduledBy;\n        }\n      }\n\n      return {\n        ...booking,\n        rescheduler,\n        eventType: {\n          ...booking.eventType,\n          recurringEvent: parseRecurringEvent(booking.eventType?.recurringEvent),\n          eventTypeColor: parseEventTypeColor(booking.eventType?.eventTypeColor),\n          price: booking.eventType?.price || 0,\n          currency: booking.eventType?.currency || \"usd\",\n          metadata: EventTypeMetaDataSchema.parse(booking.eventType?.metadata || {}),\n        },\n        startTime: booking.startTime.toISOString(),\n        endTime: booking.endTime.toISOString(),\n      };\n    })\n*/\n\n// Access related data directly: bookingWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/bookings/get.handler.ts",
   "line": 852,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "bookings.map((booking) => ({\n    ...booking,\n    attendees: booking.attendees.map((attendee) => ({\n      ...attendee,\n      user: attendeeUserDataMap.get(attendee.id) || null,\n    })),\n  }))",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 85.05,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: none detected\n// Method calls: bookings.map(), booking.attendees.map(), attendeeUserDataMap.get()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\nbookings.map((booking) => ({\n    ...booking,\n    attendees: booking.attendees.map((attendee) => ({\n      ...attendee,\n      user: attendeeUserDataMap.get(attendee.id) || null,\n    })),\n  }))\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/bookings/get.handler.ts",
   "line": 854,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "booking.attendees.map((attendee) => ({\n      ...attendee,\n      user: attendeeUserDataMap.get(attendee.id) || null,\n    }))",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.15,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: none detected\n// Method calls: booking.attendees.map(), attendeeUserDataMap.get()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\nbooking.attendees.map((attendee) => ({\n      ...attendee,\n      user: attendeeUserDataMap.get(attendee.id) || null,\n    }))\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/bookings/confirm.handler.ts",
   "line": 201,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "booking.attendees.map(async (attendee) => {\n    const locale = attendee.locale ?? \"en\";\n    let translate = translations.get(locale);\n    if (!translate) {\n      translate = await getTranslation(locale, \"common\");\n      translations.set(locale, translate);\n    }\n    return {\n      name: attendee.name,\n      email: attendee.email,\n      timeZone: attendee.timeZone,\n      phoneNumber: attendee.phoneNumber,\n      language: {\n        translate,\n        locale,\n      },\n    };\n  })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.15,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: locale, translate\n// Method calls: booking.attendees.map(), translations.get(), translations.set()\n// Loop structure: none\n// Async calls: 1\n\n// Original code:\nbooking.attendees.map(async (attendee) => {\n    const locale = attendee.locale ?? \"en\";\n    let translate = translations.get(locale);\n    if (!translate) {\n      translate = await getTranslation(locale, \"common\");\n      translations.set(locale, translate);\n    }\n    return {\n      name: attendee.name,\n      email: attendee.email,\n      timeZone: attendee.timeZone,\n      phoneNumber: attendee.phoneNumber,\n      language: {\n        translate,\n        locale,\n      },\n    };\n  })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/bookings/addGuests.handler.ts",
   "line": 232,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "uniqueGuestEmails\n    .map((email) => emailToGuestMap.get(extractBaseEmail(email).toLowerCase()))",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.15,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: none detected\n// Method calls: uniqueGuestEmails.map(), emailToGuestMap.get(), extractBaseEmail(email).toLowerCase()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\nuniqueGuestEmails\n    .map((email) => emailToGuestMap.get(extractBaseEmail(email).toLowerCase()))\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/availability/calendarOverlay.handler.ts",
   "line": 72,
   "severity": "medium",
   "description": "Found 1 database query (Mongoose.find()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "calendarsToLoad.map((calendar) => {\n    const credential = credentials.find((item) => item.id === calendar.credentialId);\n    if (!credential) {\n      throw new TRPCError({\n        code: \"UNAUTHORIZED\",\n        message: \"Unauthorized - These credentials do not belong to you\",\n      });\n    }\n    return {\n      ...calendar,\n      userId: user.id,\n      integration: credential.type,\n    };\n  })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 85.4,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: credential\n// Method calls: calendarsToLoad.map(), credentials.find()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\ncalendarsToLoad.map((calendar) => {\n    const credential = credentials.find((item) => item.id === calendar.credentialId);\n    if (!credential) {\n      throw new TRPCError({\n        code: \"UNAUTHORIZED\",\n        message: \"Unauthorized - These credentials do not belong to you\",\n      });\n    }\n    return {\n      ...calendar,\n      userId: user.id,\n      integration: credential.type,\n    };\n  })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/apps/toggle.handler.ts",
   "line": 79,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "appCredentials.map(async (credential) => {\n          // No need to continue if credential does not have a user\n          if (!credential.user || !credential.user.email) return;\n\n          const locale = credential.user.locale ?? \"en\";\n          let t = translations.get(locale);\n\n          if (!t) {\n            t = await getTranslation(locale, \"common\");\n            translations.set(locale, t);\n          }\n\n          await sendDisabledAppEmail({\n            email: credential.user.email,\n            appName: appMetadata?.name || app.slug,\n            appType: app.categories,\n            t,\n          });\n        })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.15,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: locale, t\n// Method calls: appCredentials.map(), translations.get(), translations.set()\n// Loop structure: none\n// Async calls: 2\n\n// Original code:\nappCredentials.map(async (credential) => {\n          // No need to continue if credential does not have a user\n          if (!credential.user || !credential.user.email) return;\n\n          const locale = credential.user.locale ?? \"en\";\n          let t = translations.get(locale);\n\n          if (!t) {\n            t = await getTranslation(locale, \"common\");\n            translations.set(locale, t);\n          }\n\n          await sendDisabledAppEmail({\n            email: credential.user.email,\n            appName: appMetadata?.name || app.slug,\n            appType: app.categories,\n            t,\n          });\n        })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/apps/toggle.handler.ts",
   "line": 122,
   "severity": "high",
   "description": "Found 2 database queries (Unknown.all(), Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "eventTypesWithApp.map(async (eventType) => {\n          // TODO: This update query can be removed by merging it with\n          // the previous `findMany` query, if that query returns certain values.\n          await prisma.eventType.update({\n            where: {\n              id: eventType.id,\n            },\n            data: {\n              metadata: {\n                ...(eventType.metadata as object),\n                apps: {\n                  // From this comment we can not type JSON fields in Prisma https://github.com/prisma/prisma/issues/3219#issuecomment-670202980\n                  // @ts-expect-error\n                  ...eventType.metadata?.apps,\n                  // @ts-expect-error\n                  [app.slug]: { ...eventType.metadata?.apps[app.slug], enabled: false },\n                },\n              },\n            },\n          });\n\n          return Promise.all(\n            eventType.users.map(async (user) => {\n              const locale = user.locale ?? \"en\";\n              let t = translations.get(locale);\n\n              if (!t) {\n                t = await getTranslation(locale, \"common\");\n                translations.set(locale, t);\n              }\n\n              await sendDisabledAppEmail({\n                email: user.email,\n                appName: appMetadata?.name || app.slug,\n                appType: app.categories,\n                t,\n                title: eventType.title,\n                eventTypeId: eventType.id,\n              });\n            })\n          );\n        })",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "prisma-include",
     "rank": 1,
     "fitnessScore": 79.19999999999999,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: eventType",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for eventType and related data\n// Optimized: Single query with include\n\nconst eventTypeWithRelations = await prisma.eventType.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\neventTypesWithApp.map(async (eventType) => {\n          // TODO: This update query can be removed by merging it with\n          // the previous `findMany` query, if that query returns certain values.\n          await prisma.eventType.update({\n            where: {\n              id: eventType.id,\n            },\n            data: {\n              metadata: {\n                ...(eventType.metadata as object),\n                apps: {\n                  // From this comment we can not type JSON fields in Prisma https://github.com/prisma/prisma/issues/3219#issuecomment-670202980\n                  // @ts-expect-error\n                  ...eventType.metadata?.apps,\n                  // @ts-expect-error\n                  [app.slug]: { ...eventType.metadata?.apps[app.slug], enabled: false },\n                },\n              },\n            },\n          });\n\n          return Promise.all(\n            eventType.users.map(async (user) => {\n              const locale = user.locale ?? \"en\";\n              let t = translations.get(locale);\n\n              if (!t) {\n                t = await getTranslation(locale, \"common\");\n                translations.set(locale, t);\n              }\n\n              await sendDisabledAppEmail({\n                email: user.email,\n                appName: appMetadata?.name || app.slug,\n                appType: app.categories,\n                t,\n                title: eventType.title,\n                eventTypeId: eventType.id,\n              });\n            })\n          );\n        })\n*/\n\n// Access related data directly: eventTypeWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/apps/toggle.handler.ts",
   "line": 144,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "eventType.users.map(async (user) => {\n              const locale = user.locale ?? \"en\";\n              let t = translations.get(locale);\n\n              if (!t) {\n                t = await getTranslation(locale, \"common\");\n                translations.set(locale, t);\n              }\n\n              await sendDisabledAppEmail({\n                email: user.email,\n                appName: appMetadata?.name || app.slug,\n                appType: app.categories,\n                t,\n                title: eventType.title,\n                eventTypeId: eventType.id,\n              });\n            })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.15,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: locale, t\n// Method calls: eventType.users.map(), translations.get(), translations.set()\n// Loop structure: none\n// Async calls: 2\n\n// Original code:\neventType.users.map(async (user) => {\n              const locale = user.locale ?? \"en\";\n              let t = translations.get(locale);\n\n              if (!t) {\n                t = await getTranslation(locale, \"common\");\n                translations.set(locale, t);\n              }\n\n              await sendDisabledAppEmail({\n                email: user.email,\n                appName: appMetadata?.name || app.slug,\n                appType: app.categories,\n                t,\n                title: eventType.title,\n                eventTypeId: eventType.id,\n              });\n            })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/apps/queryForDependencies.handler.ts",
   "line": 21,
   "severity": "medium",
   "description": "Found 1 database query (Prisma.findFirst()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "input.map(async (dependency) => {\n      const appId = dependency;\n      const dbCredential = await prisma.credential.findFirst({\n        where: {\n          appId,\n          userId: ctx.user.id,\n        },\n      });\n\n      const delegationCredentials = await getAllDelegationCredentialsForUserByAppSlug({\n        user: ctx.user,\n        appSlug: appId,\n      });\n      const appInstalled = !!dbCredential || !!delegationCredentials.length;\n\n      const app = getAppFromSlug(dependency);\n\n      dependencyData.push({ name: app?.name || dependency, slug: dependency, installed: !!appInstalled });\n    })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "prisma-include",
     "rank": 1,
     "fitnessScore": 84.5,
     "reasoning": "Use Prisma include for eager loading related data\nPreserved: credential",
     "code": "// OPTIMIZED: Prisma eager loading with include\n// Original: Separate queries for credential and related data\n// Optimized: Single query with include\n\nconst credentialWithRelations = await prisma.credential.findMany({\n  include: {\n    // Add relations here\n  }\n});\n\n// Original code reference:\n/*\ninput.map(async (dependency) => {\n      const appId = dependency;\n      const dbCredential = await prisma.credential.findFirst({\n        where: {\n          appId,\n          userId: ctx.user.id,\n        },\n      });\n\n      const delegationCredentials = await getAllDelegationCredentialsForUserByAppSlug({\n        user: ctx.user,\n        appSlug: appId,\n      });\n      const appInstalled = !!dbCredential || !!delegationCredentials.length;\n\n      const app = getAppFromSlug(dependency);\n\n      dependencyData.push({ name: app?.name || dependency, slug: dependency, installed: !!appInstalled });\n    })\n*/\n\n// Access related data directly: credentialWithRelations[0].relation"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/eventTypes/usecases/EventGroupBuilder.ts",
   "line": 91,
   "severity": "high",
   "description": "Found 2 database queries (Mongoose.find(), Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "memberships\n        .filter((membership) => shouldIncludeTeamMembership(membership, filters))\n        .map(async (membership) => {\n          const orgMembership = teamMemberships.find(\n            (teamM) => teamM.teamId === membership.team.parentId\n          )?.membershipRole;\n\n          const effectiveRole = getEffectiveRole(orgMembership, membership.role);\n          const permissions = teamPermissionsMap.get(membership.team.id);\n\n          if (!permissions) {\n            throw new Error(`Permissions not found for team ${membership.team.id}`);\n          }\n\n          const teamSlug = createTeamSlug(\n            membership.team.slug,\n            !!membership.team.parentId\n          );\n\n          return createTeamEventGroup(membership, effectiveRole, teamSlug, permissions);\n        })",
   "estimatedImpact": {
    "severityScore": 7,
    "description": "201 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 201,
     "queriesOptimal": 1,
     "performanceGain": "201x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 82.9,
     "reasoning": "Pattern analysis for chained-methods with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: chained-methods\n// ORM/Framework: unknown\n// Variables: orgMembership, effectiveRole, permissions, teamSlug\n// Method calls: memberships.filter(membership => shouldIncludeTeamMembership(membership, filters)).map(), memberships.filter(), teamMemberships.find(), teamPermissionsMap.get()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\nmemberships\n        .filter((membership) => shouldIncludeTeamMembership(membership, filters))\n        .map(async (membership) => {\n          const orgMembership = teamMemberships.find(\n            (teamM) => teamM.teamId === membership.team.parentId\n          )?.membershipRole;\n\n          const effectiveRole = getEffectiveRole(orgMembership, membership.role);\n          const permissions = teamPermissionsMap.get(membership.team.id);\n\n          if (!permissions) {\n            throw new Error(`Permissions not found for team ${membership.team.id}`);\n          }\n\n          const teamSlug = createTeamSlug(\n            membership.team.slug,\n            !!membership.team.parentId\n          );\n\n          return createTeamEventGroup(membership, effectiveRole, teamSlug, permissions);\n        })\n\n// Recommended optimizations based on pattern:\n1. Consider using reduce() for single-pass processing\n// 2. Avoid creating intermediate arrays\n// 3. Use for...of loop for better performance on large arrays"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/eventTypes/utils/transformUtils.ts",
   "line": 125,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "eventTypeGroups.map((group) => {\n    let canCreateEventTypes: boolean | undefined;\n    let canUpdateEventTypes: boolean | undefined;\n    let canDeleteEventTypes: boolean | undefined;\n\n    if (group.teamId) {\n      const permissions = teamPermissionsMap.get(group.teamId);\n      canCreateEventTypes = permissions?.canCreate;\n      canUpdateEventTypes = permissions?.canEdit;\n      canDeleteEventTypes = permissions?.canDelete;\n    }\n\n    return {\n      ...group.profile,\n      ...group.metadata,\n      teamId: group.teamId,\n      membershipRole: group.membershipRole,\n      canCreateEventTypes,\n      canUpdateEventTypes,\n      canDeleteEventTypes,\n    };\n  })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 87.15,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: canCreateEventTypes, canUpdateEventTypes, canDeleteEventTypes, permissions\n// Method calls: eventTypeGroups.map(), teamPermissionsMap.get()\n// Loop structure: none\n// Async calls: 0\n\n// Original code:\neventTypeGroups.map((group) => {\n    let canCreateEventTypes: boolean | undefined;\n    let canUpdateEventTypes: boolean | undefined;\n    let canDeleteEventTypes: boolean | undefined;\n\n    if (group.teamId) {\n      const permissions = teamPermissionsMap.get(group.teamId);\n      canCreateEventTypes = permissions?.canCreate;\n      canUpdateEventTypes = permissions?.canEdit;\n      canDeleteEventTypes = permissions?.canDelete;\n    }\n\n    return {\n      ...group.profile,\n      ...group.metadata,\n      teamId: group.teamId,\n      membershipRole: group.membershipRole,\n      canCreateEventTypes,\n      canUpdateEventTypes,\n      canDeleteEventTypes,\n    };\n  })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/eventTypes/utils/permissionUtils.ts",
   "line": 97,
   "severity": "medium",
   "description": "Found 1 database query (Mongoose.find()) inside a forEach loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "memberships.map(async (membership) => {\n    const orgMembership = teamMemberships.find(\n      (teamM) => teamM.teamId === membership.team.parentId\n    )?.membershipRole;\n\n    const effectiveRole = getEffectiveRole(orgMembership, membership.role);\n    const permissions = await getTeamPermissions(userId, membership.team.id, effectiveRole);\n\n    return [membership.team.id, permissions] as const;\n  })",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 85.4,
     "reasoning": "Pattern analysis for unknown with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: unknown\n// ORM/Framework: unknown\n// Variables: orgMembership, effectiveRole, permissions\n// Method calls: memberships.map(), teamMemberships.find()\n// Loop structure: none\n// Async calls: 1\n\n// Original code:\nmemberships.map(async (membership) => {\n    const orgMembership = teamMemberships.find(\n      (teamM) => teamM.teamId === membership.team.parentId\n    )?.membershipRole;\n\n    const effectiveRole = getEffectiveRole(orgMembership, membership.role);\n    const permissions = await getTeamPermissions(userId, membership.team.id, effectiveRole);\n\n    return [membership.team.id, permissions] as const;\n  })\n\n// Recommended optimizations based on pattern:\n"
    }
   ]
  },
  {
   "file": "packages/trpc/server/routers/viewer/availability/team/listTeamAvailability.handler.ts",
   "line": 81,
   "severity": "medium",
   "description": "Found 1 database query (Unknown.get()) inside a for-of loop. This creates an N+1 query problem where each iteration makes a separate database call. Consider using eager loading or batch queries instead.",
   "codeBefore": "for (const membership of memberships) {\n    const enrichedUser = enrichedUserMap.get(membership.user.id);\n    if (!enrichedUser) continue;\n    membershipWithUserProfile.push({\n      ...membership,\n      user: enrichedUser,\n    });\n  }",
   "estimatedImpact": {
    "severityScore": 5,
    "description": "101 queries for 100 items vs 1 optimal query",
    "confidenceScore": 85,
    "category": "performance",
    "fixDifficulty": "moderate",
    "metrics": {
     "queriesIfN100": 101,
     "queriesOptimal": 1,
     "performanceGain": "101x slower"
    }
   },
   "solutions": [
    {
     "type": "pattern-analysis",
     "rank": 1,
     "fitnessScore": 88.2,
     "reasoning": "Pattern analysis for loop-with-calls with unknown framework",
     "code": "// Analysis of original code:\n// Pattern type: loop-with-calls\n// ORM/Framework: unknown\n// Variables: membership, enrichedUser\n// Method calls: enrichedUserMap.get(), membershipWithUserProfile.push()\n// Loop structure: for-of over memberships\n// Async calls: 0\n\n// Original code:\nfor (const membership of memberships) {\n    const enrichedUser = enrichedUserMap.get(membership.user.id);\n    if (!enrichedUser) continue;\n    membershipWithUserProfile.push({\n      ...membership,\n      user: enrichedUser,\n    });\n  }\n\n// Recommended optimizations based on pattern:\n1. Extract queries before the loop\n// 2. Use batch queries with IN clause\n// 3. Build a Map for O(1) lookups inside the loop"
    }
   ]
  }
 ]
}